refactor(models): standardize representation_dim across all models: - Rename all plural dimension parameters to singular (`input_dim`, `hidden_dim`, etc.) across 10 models - Unify output-dimension parameter to `representation_dim` tree-wide - Add `representation_dim` property to all models returning `encode()` output width - Enforce keyword-only signatures on multi-param functions - Update `RepresentationBackbone` protocol, factory wiring, and test suite - CHANGELOG v0.1.0a13 entry with full rename map
