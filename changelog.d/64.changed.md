refactor(models): replace norm: str with NormalizationLayerType enum: Replace duplicated string-based `norm: str` parameter across MCL, TSTCC, Series2Vec, and TST models with a shared `NormalizationLayerType(StrEnum)` enum (CHANNEL/BATCH). Removed 8 copy-pasted string validation blocks. Added missing Args docstrings to 8 model classes and Series2VecNetwork.
