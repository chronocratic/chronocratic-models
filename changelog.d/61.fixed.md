fix(series2vec): Series2Vec batch_size=1 crash + representation_dim consistency: Fixes Series2Vec training crashes at `batch_size=1` and aligns `representation_dims` with actual encoding output. Adds norm strategy toggle and reorganizes test files.
