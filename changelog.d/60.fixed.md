fix: Series2Vec training crash at batch_size=1: Fixed Series2Vec training_step crash at batch_size=1. pretraining_loss returned new_tensor(0.0) (disconnected from graph) when no distance pairs existed, causing "element 0 of tensors does not require grad" during backward().
