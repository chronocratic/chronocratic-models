TimeVAE: new `residual_projection` option (`'crop'` default, `'dense'` = upstream). The default residual branch now ends in a linear deconvolution cropped to `sequence_length`. `series2vec.losses.pairwise_soft_dtw_distances` now takes `(time_series, *, gamma, bandwidth=None)` instead of `(soft_dtw_module, time_series)`.
