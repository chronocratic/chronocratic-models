Added ``max_train_length`` parameter to TST and TimeVAE models, enabling training on datasets with variable-length batches via random cropping. TST's ``FixedPositionalEncoding`` now computes positional encodings at forward time and no longer caps input length.
