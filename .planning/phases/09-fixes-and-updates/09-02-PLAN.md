---
phase: 09-fixes-and-updates
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/chronocratic/models/_mixin/encoding.py
  - src/chronocratic/models/convolutional/dilated/_mixin/encoding.py
  - src/chronocratic/models/generative/timevae/model.py
  - src/chronocratic/models/recurrent/timenet/model.py
  - src/chronocratic/models/recurrent/recurrentae/model.py
  - src/chronocratic/models/convolutional/standard/mcl/model.py
  - src/chronocratic/models/convolutional/standard/tstcc/model.py
  - src/chronocratic/models/supervised/_adapters.py
autonomous: true
requirements: [D-02, D-03]
user_setup: []

must_haves:
  truths:
    - "BasicEncodingMixin has exactly 2 hooks: _get_encoder() -> nn.Module and _encode_batch(encoder, batch_x) -> Tensor"
    - "_get_encoder_module, _prepare_inputs, _postprocess are deleted from BasicEncodingMixin and all 5 simple model overrides"
    - "encode() output shape is unchanged for all 5 BasicEncodingMixin models"
    - "gradient_enabled=True keeps autograd graph alive, encoder stays in eval()"
    - "gradient_enabled=False (default) uses inference_mode, severs graph"
    - "Dilated BaseEncodingMixin.encode() accepts gradient_enabled kwarg with same behavior"
  artifacts:
    - path: "src/chronocratic/models/_mixin/encoding.py"
      provides: "Rewritten BasicEncodingMixin with 2-hook contract and gradient_enabled"
      contains: "_encode_batch"
    - path: "src/chronocratic/models/convolutional/dilated/_mixin/encoding.py"
      provides: "Dilated mixin with gradient_enabled support"
      contains: "gradient_enabled"
    - path: "src/chronocratic/models/convolutional/standard/mcl/model.py"
      provides: "FCN with 2-hook contract"
      min_lines: 60
    - path: "src/chronocratic/models/convolutional/standard/tstcc/model.py"
      provides: "TSTCC with 2-hook contract"
      min_lines: 190
  key_links:
    - from: "BasicEncodingMixin.encode()"
      to: "_encode_batch()"
      via: "encode() loop calls self._encode_batch(encoder, batch_x.to(self.device)) for each batch"
      pattern: "_encode_batch\\(encoder,.*batch_x"
    - from: "BasicEncodingMixin.encode()"
      to: "contextlib.nullcontext"
      via: "gradient_enabled=True selects nullcontext() instead of inference_mode()"
      pattern: "nullcontext.*gradient_enabled"
    - from: "FCN._encode_batch()"
      to: "FCNEncoder.forward()"
      via: "_encode_batch calls encoder(batch_x).unsqueeze(1) to produce (B, 1, D)"
      pattern: "encoder\\(batch_x\\)\\.unsqueeze"
    - from: "TSTCC._encode_batch()"
      to: "pool_feature_map()"
      via: "_encode_batch applies pool_feature_map to encoder output"
      pattern: "pool_feature_map"
---

<objective>
Collapse BasicEncodingMixin from a 4-hook contract to a 2-hook contract (_get_encoder -> nn.Module, _encode_batch) and add gradient_enabled: bool = False to encode() on both mixin families. Then refactor 5 simple models that follow straightforward encoder(batch_x) patterns.

Purpose: The 4-hook contract (_get_encoder + _get_encoder_module + _prepare_inputs + _postprocess) is broken by design — TST and Series2Vec returned bound methods from _get_encoder(), defeating the auto-detect in _get_encoder_module(). The 2-hook contract separates concerns honestly: module for eval/train state toggle, callable for compute. gradient_enabled enables adversarial attacks and contrastive-view comparison that need gradients through the encoder.

Output: Rewritten BasicEncodingMixin, updated dilated mixin, 5 simple model refactors, _adapters.py docstring cleaned.
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsmodels/.claude/gsd-core/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/tsmodels/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/chronocratic/models/_mixin/encoding.py
@src/chronocratic/models/convolutional/dilated/_mixin/encoding.py
@src/chronocratic/models/generative/timevae/model.py
@src/chronocratic/models/recurrent/timenet/model.py
@src/chronocratic/models/recurrent/recurrentae/model.py
@src/chronocratic/models/convolutional/standard/mcl/model.py
@src/chronocratic/models/convolutional/standard/tstcc/model.py
@src/chronocratic/models/supervised/_adapters.py
@tests/unit/test_recurrent_autoencoder.py
@tests/unit/test_mcl_config_and_encoder.py
@tests/unit/test_timenet_param_rename.py
@tests/unit/test_tstcc_supervised.py
@.planning/todos/specs/encoding-mixin-refactor-and-gradient-encode.md
@.planning/phases/09-fixes-and-updates/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite BasicEncodingMixin core (4-hook -> 2-hook) with gradient_enabled</name>
  <files>src/chronocratic/models/_mixin/encoding.py</files>
  <read_first>
    - src/chronocratic/models/_mixin/encoding.py — full file, current encode() body (lines 112-147), all 4 hooks
  </read_first>
  <behavior>
    - encode(data, batch_size, num_workers, *, gradient_enabled=False) returns same shape as before when gradient_enabled=False
    - gradient_enabled=True: output.requires_grad is True when data.requires_grad is True
    - gradient_enabled=False (default): output.requires_grad is False (graph severed by inference_mode)
    - Encoder train/eval state saved and restored correctly (try/finally)
    - pin_memory=not gradient_enabled in DataLoader
    - nullcontext used (not enable_grad) when gradient_enabled=True
    - _get_encoder returns nn.Module (not Callable[..., Any])
    - _encode_batch(encoder, batch_x) has default implementation of encoder(batch_x)
    - _get_encoder_module, _prepare_inputs, _postprocess deleted from class
  </behavior>
  <action>
  <!-- planner-discipline-allow: _get_encoder_module -->
  <!-- planner-discipline-allow: _postprocess -->
  Rewrite BasicEncodingMixin in src/chronocratic/models/_mixin/encoding.py (per D-02, D-03):

  1. Add "from contextlib import nullcontext" import
  2. Change _get_encoder signature from -> Callable[..., Any] to -> nn.Module (update docstring)
  3. Delete _get_encoder_module() entirely (was: lines 62-78)
  4. Delete _prepare_inputs() entirely (was: lines 80-93)
  5. Replace _postprocess() with _encode_batch(encoder: nn.Module, batch_x: torch.Tensor) -> torch.Tensor with default body: return encoder(batch_x)
  6. Rewrite encode():
     - Remove @torch.inference_mode() decorator
     - Add keyword-only *, gradient_enabled: bool = False parameter
     - Use encoder = self._get_encoder() directly as toggle target (no more _get_encoder_module)
     - grad_ctx = nullcontext() if gradient_enabled else torch.inference_mode()
     - DataLoader with pin_memory=not gradient_enabled
     - Replace loop body (was: args = _prepare_inputs; output = encoder(*args); _postprocess(output)) with single call: self._encode_batch(encoder, batch_x.to(self.device)).to(data.device)
     - Keep try/finally for encoder.train(was_training) restoration
  7. Update module docstring and class docstring to reflect 2-hook contract
  8. Remove TYPE_CHECKING import of Callable if no longer needed

  Verify structurally: grep confirms _encode_batch present, deleted hooks absent. Then run RecurrentAutoEncoder tests — the model still uses the old hook names, but the mixin encode() pipeline exercises _get_encoder() and the data loading, which validates the structural change did not break the callable path.
  </action>
  <verify>
    <automated>grep -c '_encode_batch' src/chronocratic/models/_mixin/encoding.py > 0 && (grep -c '_get_encoder_module' src/chronocratic/models/_mixin/encoding.py == 0) && uv run pytest tests/unit/test_recurrent_autoencoder.py -q --tb=short</automated>
  </verify>
  <done>BasicEncodingMixin has _get_encoder -> nn.Module and _encode_batch(encoder, batch_x); _get_encoder_module/_prepare_inputs/_postprocess deleted; encode() has gradient_enabled kwarg with nullcontext/inference_mode branching; pin_memory=not gradient_enabled; RecurrentAutoEncoder tests still pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add gradient_enabled to dilated BaseEncodingMixin</name>
  <files>src/chronocratic/models/convolutional/dilated/_mixin/encoding.py</files>
  <read_first>
    - src/chronocratic/models/convolutional/dilated/_mixin/encoding.py — encode() method (lines 183-274), PoolingEncodingMixin._evaluate_with_pooling (lines 293-326), DecompositionEncodingMixin._evaluate_with_feature_concatenation (lines 347-379)
  </read_first>
  <behavior>
    - dilated encode() accepts gradient_enabled=False (default) — same behavior as before
    - gradient_enabled=True keeps graph alive through encoder
    - "gradient_enabled" appears in BaseEncodingMixin.encode signature
    - nullcontext used (not enable_grad) in dilated encode body
    - pin_memory=not gradient_enabled in DataLoader
  </behavior>
  <action>
  Update dilated mixin in src/chronocratic/models/convolutional/dilated/_mixin/encoding.py (per D-03):

  1. Add "from contextlib import nullcontext" import
  2. In BaseEncodingMixin.encode(), add keyword-only *, gradient_enabled: bool = False parameter
  3. Replace "with torch.inference_mode():" (line 240) with conditional context manager:
     - grad_ctx = nullcontext() if gradient_enabled else torch.inference_mode()
     - with grad_ctx: (wrap existing body)
  4. Set pin_memory=not gradient_enabled in DataLoader (line 237)
  5. Do NOT touch hook surface — _get_encoder, _get_eval_method, _get_slice stay unchanged
  6. Verify: the .cpu() calls at lines 269, 326, 379 are safe — .cpu() is differentiable, preserves autograd graph

  The dilated loop uses extract_features_from_batch(batch) instead of (batch_x,) unpacking — leave that pattern. The key change is the context manager and pin_memory.
  </action>
  <verify>
    <automated>grep -c 'gradient_enabled' src/chronocratic/models/convolutional/dilated/_mixin/encoding.py > 0 && grep -c 'nullcontext' src/chronocratic/models/convolutional/dilated/_mixin/encoding.py > 0 && uv run pytest tests/ -k "encode" -q --tb=short</automated>
  </verify>
  <done>Dilated BaseEncodingMixin.encode() has gradient_enabled kwarg, uses nullcontext when True, pin_memory=not gradient_enabled; hook surface unchanged; encode-related tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Refactor 5 simple BasicEncodingMixin models to 2-hook contract</name>
  <files>
    src/chronocratic/models/generative/timevae/model.py
    src/chronocratic/models/recurrent/timenet/model.py
    src/chronocratic/models/recurrent/recurrentae/model.py
    src/chronocratic/models/convolutional/standard/mcl/model.py
    src/chronocratic/models/convolutional/standard/tstcc/model.py
    src/chronocratic/models/supervised/_adapters.py
  </files>
  <read_first>
    - src/chronocratic/models/generative/timevae/model.py — _get_encoder (line 198), _postprocess (line 202)
    - src/chronocratic/models/recurrent/timenet/model.py — _get_encoder (line 98), _postprocess (line 102)
    - src/chronocratic/models/recurrent/recurrentae/model.py — _get_encoder (line 102), _postprocess (line 105)
    - src/chronocratic/models/convolutional/standard/mcl/model.py — _get_encoder (line 63), _postprocess (line 67)
    - src/chronocratic/models/convolutional/standard/tstcc/model.py — _get_encoder (line 193), _prepare_inputs (line 202), _postprocess (line 212)
    - src/chronocratic/models/supervised/_adapters.py — any references to deleted hooks
  </read_first>
  <behavior>
    - All 5 models have _get_encoder -> nn.Module and _encode_batch(encoder, batch_x) -> Tensor
    - No model has _get_encoder_module, _prepare_inputs, or _postprocess
    - encode() output shape unchanged for each model
  </behavior>
  <action>
  <!-- planner-discipline-allow: _postprocess -->
  Replace _postprocess (and _get_encoder_module, _prepare_inputs where present) with _encode_batch in the 5 simple models:

  1. TimeVAE (timevae/model.py):
     - def _encode_batch(self, encoder: nn.Module, batch_x: torch.Tensor) -> torch.Tensor: return encoder(batch_x)[0]

  2. TimeNet (timenet/model.py):
     - def _encode_batch(self, encoder: nn.Module, batch_x: torch.Tensor) -> torch.Tensor: return encoder(batch_x)[:, -1, :]

  3. RecurrentAutoEncoder (recurrentae/model.py):
     - def _encode_batch(self, encoder: nn.Module, batch_x: torch.Tensor) -> torch.Tensor: return encoder(batch_x)[:, -1, :]

  4. MCL/FCN (mcl/model.py):
     - def _encode_batch(self, encoder: nn.Module, batch_x: torch.Tensor) -> torch.Tensor: return encoder(batch_x).unsqueeze(1)

  5. TSTCC (tstcc/model.py):
     - Delete _prepare_inputs (batch_x.float()) — fold float cast into _encode_batch
     - def _encode_batch(self, encoder: nn.Module, batch_x: torch.Tensor) -> torch.Tensor: return pool_feature_map(encoder(batch_x.float()))
     - MUST import pool_feature_map from chronocratic.models.utils if not already imported

  For all 5: annotate _get_encoder with -> nn.Module return type. Delete _postprocess, _get_encoder_module, _prepare_inputs.

  Then clean supervised/_adapters.py: if any docstring references deleted hooks (_get_encoder_module, _prepare_inputs, _postprocess), update to mention _encode_batch instead. Do not change code logic.

  Run targeted tests covering model-specific behavior:
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_recurrent_autoencoder.py tests/unit/test_mcl_config_and_encoder.py tests/unit/test_timenet_param_rename.py tests/unit/test_tstcc_supervised.py -q --tb=short</automated>
  </verify>
  <done>5 simple models use _encode_batch; old hooks deleted; _adapters.py docstring cleaned; all targeted tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| encode() gradient toggle | gradient_enabled controls whether autograd graph is preserved — security relevant for adversarial attack pipelines |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-09-02-01 | Information Disclosure | BasicEncodingMixin.encode() | mitigate | gradient_enabled defaults to False — no gradient leakage by default |
| T-09-02-02 | Spoofing | _encode_batch contract | mitigate | nn.Module return type on _get_encoder prevents bound method confusion |
</threat_model>

<verification>
1. uv run pytest tests/unit/test_recurrent_autoencoder.py tests/unit/test_mcl_config_and_encoder.py tests/unit/test_timenet_param_rename.py tests/unit/test_tstcc_supervised.py -q --tb=short — targeted tests pass
2. grep -rn "_get_encoder_module\|_prepare_inputs\|_postprocess" src/chronocratic/models/_mixin/encoding.py src/chronocratic/models/generative/timevae/model.py src/chronocratic/models/recurrent/timenet/model.py src/chronocratic/models/recurrent/recurrentae/model.py src/chronocratic/models/convolutional/standard/mcl/model.py src/chronocratic/models/convolutional/standard/tstcc/model.py --include="*.py" | grep -v "#" | grep -v "docstring" | wc -l == 0
3. grep -c "_encode_batch" src/chronocratic/models/_mixin/encoding.py > 0
4. grep -c "gradient_enabled" src/chronocratic/models/_mixin/encoding.py > 0
5. grep -c "gradient_enabled" src/chronocratic/models/convolutional/dilated/_mixin/encoding.py > 0
</verification>

<success_criteria>
- BasicEncodingMixin uses exactly 2 hooks: _get_encoder -> nn.Module, _encode_batch(encoder, batch_x) -> Tensor
- _get_encoder_module, _prepare_inputs, _postprocess removed from mixin and all 5 simple model overrides
- All 5 models produce encode() output identical in shape to pre-refactor
- gradient_enabled=True preserves autograd graph (nullcontext), encoder stays in eval()
- gradient_enabled=False (default) uses inference_mode, severs graph
- pin_memory=not gradient_enabled in both mixin families
- Dilated BaseEncodingMixin.encode() has gradient_enabled kwarg
- Targeted tests pass for all 5 models
</success_criteria>

<output>
Create `.planning/phases/09-fixes-and-updates/09-02-SUMMARY.md` when done
</output>

## Artifacts This Phase Produces

### Symbols Created
- `_encode_batch(self, encoder: nn.Module, batch_x: torch.Tensor) -> torch.Tensor` — new hook in BasicEncodingMixin
- `gradient_enabled: bool = False` — new kwarg on encode() in both mixin families

### Symbols Deleted
- `BasicEncodingMixin._get_encoder_module()` — removed (encoder IS _get_encoder return value)
- `BasicEncodingMixin._prepare_inputs()` — removed (logic folded into _encode_batch)
- `BasicEncodingMixin._postprocess()` — removed (logic folded into _encode_batch)
- `TimeVAE._postprocess()` — removed, logic in _encode_batch
- `TimeNet._postprocess()` — removed, logic in _encode_batch
- `RecurrentAutoEncoder._postprocess()` — removed, logic in _encode_batch
- `FCN._postprocess()` — removed, logic in _encode_batch
- `TSTCC._postprocess()` — removed, logic in _encode_batch
- `TSTCC._prepare_inputs()` — removed (float cast folded into _encode_batch)

### Symbols Modified
- `BasicEncodingMixin._get_encoder()` — return type changed from Callable[..., Any] to nn.Module
- `BasicEncodingMixin.encode()` — removed @inference_mode decorator, added gradient_enabled kwarg
- `BaseEncodingMixin.encode()` — added gradient_enabled kwarg, nullcontext branching
- `TimeVAE._encode_batch()` — replaces _postprocess, extracts z_mean [0]
- `TimeNet._encode_batch()` — replaces _postprocess, takes [:, -1, :]
- `RecurrentAutoEncoder._encode_batch()` — replaces _postprocess, takes [:, -1, :]
- `FCN._encode_batch()` — replaces _postprocess, unsqueezes to (B, 1, D)
- `TSTCC._encode_batch()` — replaces _postprocess + _prepare_inputs, pools encoder output

### Files Modified
- `src/chronocratic/models/_mixin/encoding.py`
- `src/chronocratic/models/convolutional/dilated/_mixin/encoding.py`
- `src/chronocratic/models/generative/timevae/model.py`
- `src/chronocratic/models/recurrent/timenet/model.py`
- `src/chronocratic/models/recurrent/recurrentae/model.py`
- `src/chronocratic/models/convolutional/standard/mcl/model.py`
- `src/chronocratic/models/convolutional/standard/tstcc/model.py`
- `src/chronocratic/models/supervised/_adapters.py` (docstring only)
