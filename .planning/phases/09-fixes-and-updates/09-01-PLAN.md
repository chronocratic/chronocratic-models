---
phase: 09-fixes-and-updates
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/chronocratic/models/convolutional/standard/mcl/encoder.py
  - src/chronocratic/models/convolutional/standard/tstcc/encoder.py
  - src/chronocratic/models/convolutional/standard/tstcc/augmentations.py
  - src/chronocratic/models/augmentation/primitives.py
  - docs/contributing.md
  - .planning/ROADMAP.md
  - tests/unit/test_mcl_config_and_encoder.py
  - tests/unit/test_tstcc_supervised.py
  - tests/test_tstcc_producer.py
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "FCNEncoder.forward() accepts (B, T, C) input with T != C and returns (B, output_dims)"
    - "TCCEncoder.forward() accepts (B, T, C) input with T != C and returns (B, output_dims, L')"
    - "TSTCC augmentation axes use channel_dim=-1 and time_dim=1 for (B, T, C) layout"
    - "contributing.md documents encoder-owns-the-transpose convention"
  artifacts:
    - path: "src/chronocratic/models/convolutional/standard/mcl/encoder.py"
      provides: "FCNEncoder with transpose(1,2) at start of forward()"
      contains: "transpose(1, 2)"
    - path: "src/chronocratic/models/convolutional/standard/tstcc/encoder.py"
      provides: "TCCEncoder with transpose(1,2) at start of forward()"
      contains: "transpose(1, 2)"
    - path: "src/chronocratic/models/convolutional/standard/tstcc/augmentations.py"
      provides: "TSTCC augmentation pair with (B,T,C) axes"
      contains: "channel_dim=-1"
    - path: "src/chronocratic/models/augmentation/primitives.py"
      provides: "Layout-neutral docstrings for ScalingParameters, PermutationParameters"
      min_lines: 1
    - path: "docs/contributing.md"
      provides: "Encoder-owns-the-transpose convention section"
      min_lines: 10
    - path: "tests/unit/test_mcl_config_and_encoder.py"
      provides: "Transpose-sensitive test with asymmetric shape"
      min_lines: 1
    - path: "tests/unit/test_tstcc_supervised.py"
      provides: "Transpose-sensitive test with asymmetric shape"
      min_lines: 1
  key_links:
    - from: "tests/unit/test_mcl_config_and_encoder.py"
      to: "src/chronocratic/models/convolutional/standard/mcl/encoder.py"
      via: "FCNEncoder forward with asymmetric (B,T,C) shape"
      pattern: "transpose.*1.*2"
    - from: "tests/unit/test_tstcc_supervised.py"
      to: "src/chronocratic/models/convolutional/standard/tstcc/encoder.py"
      via: "TCCEncoder forward with asymmetric (B,T,C) shape"
      pattern: "transpose.*1.*2"
    - from: "src/chronocratic/models/convolutional/standard/tstcc/augmentations.py"
      to: "src/chronocratic/models/augmentation/primitives.py"
      via: "channel_dim=-1, time_dim=1 for (B,T,C) layout"
      pattern: "channel_dim=-1"
---

<objective>
Fix Conv1D encoder shape crashes in MCL/FCN and TSTCC by adopting "encoder owns the transpose" (D-01), repoint TSTCC augmentation axes, and document the library-wide (B,T,C) convention.

Purpose: Two encoders (FCNEncoder, TCCEncoder) feed (B,T,C) tensors to nn.Conv1d, which requires (B,C,T). Fix: one-line transpose(1,2) as first op in each encoder's forward(). Also fix TSTCC augmentation axis bug (channel_dim=1 -> -1, time_dim=-1 -> 1) and document convention.
Output: Fixed encoders, correct augmentation axes, updated contributing.md, transpose-sensitive regression tests.
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsmodels/.claude/gsd-core/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/tsmodels/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/skaf/VSCodeProjects/tsmodels/.planning/PROJECT.md
@/Users/skaf/VSCodeProjects/tsmodels/.planning/ROADMAP.md
@/Users/skaf/VSCodeProjects/tsmodels/.planning/STATE.md
@/Users/skaf/VSCodeProjects/tsmodels/.planning/phases/09-fixes-and-updates/09-CONTEXT.md
@/Users/skaf/VSCodeProjects/tsmodels/.planning/phases/09-fixes-and-updates/09-RESEARCH.md
@/Users/skaf/VSCodeProjects/tsmodels/.planning/todos/specs/conv1d-shape-transpose-fix-spec.md

# Source files to modify
@src/chronocratic/models/convolutional/standard/mcl/encoder.py
@src/chronocratic/models/convolutional/standard/tstcc/encoder.py
@src/chronocratic/models/convolutional/standard/tstcc/augmentations.py
@src/chronocratic/models/augmentation/primitives.py
@docs/contributing.md
@.planning/ROADMAP.md

# Reference implementations (encoders that handle transpose correctly)
@src/chronocratic/models/generative/timevae/model.py
@src/chronocratic/models/convolutional/standard/series2vec/network.py
@src/chronocratic/models/convolutional/dilated/encoders/encoders.py

# Test files
@tests/unit/test_mcl_config_and_encoder.py
@tests/unit/test_tstcc_supervised.py
@tests/test_tstcc_producer.py
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Add transpose-sensitive tests for FCNEncoder and TCCEncoder</name>
  <files>
    tests/unit/test_mcl_config_and_encoder.py
    tests/unit/test_tstcc_supervised.py
  </files>
  <behavior>
    - Test 1: FCNEncoder(input_dims=3) forward with (4, 50, 3) produces (4, 320) — asymmetric T!=C catches missing transpose
    - Test 2: TCCEncoder(input_dims=3, conv_kernel_size=8, stride=1, output_dims=128) forward with (4, 50, 3) produces (4, 128, L') — asymmetric T!=C catches missing transpose
    - Both tests MUST fail before Task 2 (RED phase) because encoders lack transpose
  </behavior>
  <read_first>
    - tests/unit/test_mcl_config_and_encoder.py — existing FCNEncoder tests use (2,1,50) which is (B,C,T) with C=1; need asymmetric (B,T,C) test
    - tests/unit/test_tstcc_supervised.py — existing TSTCC tests; need encoder-specific asymmetric test
    - src/chronocratic/models/convolutional/standard/mcl/encoder.py — current FCNEncoder.forward (no transpose)
    - src/chronocratic/models/convolutional/standard/tstcc/encoder.py — current TCCEncoder.forward (no transpose)
  </read_first>
  <action>
    Add test_fcn_encoder_accepts_btc_with_asymmetric_shape to tests/unit/test_mcl_config_and_encoder.py: FCNEncoder(input_dims=3), input (4, 50, 3), assert output (4, 320). This test MUST fail initially because Conv1d sees 50 channels instead of 3.

    Add test_tcc_encoder_accepts_btc_with_asymmetric_shape to tests/unit/test_tstcc_supervised.py: TCCEncoder(input_dims=3, conv_kernel_size=8, stride=1, output_dims=128), input (4, 50, 3), assert output shape[0]==4 and shape[1]==128. This test MUST fail initially.

    Run both tests to confirm they fail (RED). Do NOT fix the encoders yet.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_mcl_config_and_encoder.py::TestFCNEncoder::test_fcn_encoder_accepts_btc_with_asymmetric_shape tests/unit/test_tstcc_supervised.py::test_tcc_encoder_accepts_btc_with_asymmetric_shape -x --tb=short 2>&1 | grep -c "FAILED\|RuntimeError" </automated>
  </verify>
  <done>
    Both tests exist and fail with RuntimeError about channel mismatch (proving the bug is real).
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix encoders, augmentation axes, docs, and reconcile old tests</name>
  <files>
    src/chronocratic/models/convolutional/standard/mcl/encoder.py
    src/chronocratic/models/convolutional/standard/tstcc/encoder.py
    src/chronocratic/models/convolutional/standard/tstcc/augmentations.py
    src/chronocratic/models/augmentation/primitives.py
    docs/contributing.md
    .planning/ROADMAP.md
    tests/unit/test_mcl_config_and_encoder.py
    tests/unit/test_tstcc_supervised.py
    tests/test_tstcc_producer.py
  </files>
  <read_first>
    - src/chronocratic/models/convolutional/standard/mcl/encoder.py — FCNEncoder.forward (add transpose(1,2))
    - src/chronocratic/models/convolutional/standard/tstcc/encoder.py — TCCEncoder.forward (add transpose(1,2))
    - src/chronocratic/models/convolutional/standard/tstcc/augmentations.py — _default_tstcc_pair axes, module docstring
    - src/chronocratic/models/augmentation/primitives.py — ScalingParameters.channel_dim docstring (line ~107), PermutationParameters.time_dim docstring (line ~164)
    - docs/contributing.md — append encoder-owns-the-transpose convention section
    - .planning/ROADMAP.md — Phase 9 wording (update from Option B to Option D per D-01)
    - tests/unit/test_mcl_config_and_encoder.py — line 109 test_encoder_shape uses (4,1,100) which is (B,C,T) with C=1; line 136 uses (2,1,50); line 149 uses (2,1,50); line 162 uses (2,3,100) — all need review for (B,T,C) compatibility
    - tests/test_tstcc_producer.py — line 29 uses layout="NCL" which is (B,C,T) — needs update to "NTC"
    - src/chronocratic/models/convolutional/standard/mcl/model.py — line 61 forward() passes (B,T,C) to encoder; verify no wrapper-level transpose needed
    - .planning/todos/specs/conv1d-shape-transpose-fix-spec.md — full spec for exact code changes
  </read_first>
  <action>
    Per D-01, apply four buckets from the spec:

    Bucket 1 — FCNEncoder.forward(): Add x = x.transpose(1,2) as first line with comment "# (B, T, C) -> (B, C, T) for Conv1d". Update docstring to document (batch, seq_len, input_dims) input.

    Bucket 2 — TCCEncoder.forward(): Add x = x.transpose(1,2) as first line with same comment. Update docstring Args from (batch, input_dims, seq_len) to (batch, seq_len, input_dims).

    Bucket 3 — TSTCC augmentation axes in _default_tstcc_pair(): Change ScalingParameters channel_dim=1 to channel_dim=-1. Change PermutationParameters time_dim=-1 to time_dim=1. Update module docstring from "(batch, channels, time)" to "(batch, time, channels)". Soften primitives.py docstrings: ScalingParameters.channel_dim note should say "Defaults to 1 (B,C,T layout); pass -1 for (B,T,C) layout." PermutationParameters.time_dim note should say "Defaults to -1 (B,C,T layout); pass 1 for (B,T,C) layout." Do NOT change primitive default values.

    Bucket 4 — contributing.md: Add "Tensor Shape Convention" subsection under Parameters Consistency with the encoder-owns-the-transpose convention text from the spec. Update ROADMAP.md Phase 9 wording to reflect Option D (transpose in encoder, not wrapper).

    Reconcile old tests: test_encoder_shape at line 109 feeds (4,1,100) to encoder — with input_dims=1, T==C==1 is degenerate. Update to (4, 50, 1) so T!=C and transpose matters. Same for lines 136, 149, 162. Fix tests/test_tstcc_producer.py layout="NCL" to "NTC" since data is (B,T,C).

    Run asymmetric tests from Task 1 to confirm they now pass (GREEN). Run full MCL+TSTCC test suite. Run ruff check and ruff format --check.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_mcl_config_and_encoder.py tests/unit/test_tstcc_supervised.py tests/test_tstcc_producer.py -q</automated>
  </verify>
  <done>
    Asymmetric tests pass, old tests reconciled, contributing.md updated, ROADMAP.md Phase 9 updated. ruff clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Encoder input | Untrusted tensor shapes from DataLoader — asymmetric tests catch silent wrong results |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|-------------|
| T-09-01-01 | Spoofing | FCNEncoder, TCCEncoder input shape | mitigate | Asymmetric-shape tests (T!=C) crash loudly if transpose is dropped |
| T-09-01-02 | Spoofing | TSTCC augmentation axes | mitigate | Augmentation producer tests verify shape preservation with (B,T,C) layout |
| T-09-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages; uses existing torch only |
</threat_model>

<verification>
- uv run pytest tests/unit/test_mcl_config_and_encoder.py tests/unit/test_tstcc_supervised.py tests/test_tstcc_producer.py -q
- uv run ruff check src/chronocratic/models/convolutional/standard/mcl/encoder.py src/chronocratic/models/convolutional/standard/tstcc/ src/chronocratic/models/augmentation/primitives.py
- uv run ruff format --check .
</verification>

<success_criteria>
- FCNEncoder.forward() has transpose(1,2) as first op with explanatory comment
- TCCEncoder.forward() has transpose(1,2) as first op with explanatory comment
- No transpose() call in mcl/model.py or tstcc/model.py wrappers
- _default_tstcc_pair uses channel_dim=-1 and time_dim=1
- Primitive default values unchanged (channel_dim=1, time_dim=-1); only docstrings softened
- contributing.md documents encoder-owns-the-transpose convention
- Two new asymmetric-shape tests pass
- All existing MCL/TSTCC tests reconciled and passing
</success_criteria>

<output>
Create `.planning/phases/09-fixes-and-updates/09-01-SUMMARY.md` when done
</output>

### Artifacts this phase produces

**Symbols created/modified:**
- `FCNEncoder.forward` — added `x = x.transpose(1, 2)` as first operation
- `TCCEncoder.forward` — added `x = x.transpose(1, 2)` as first operation
- `_default_tstcc_pair()` — `channel_dim=-1` (was `1`), `time_dim=1` (was `-1`)
- `test_fcn_encoder_accepts_btc_with_asymmetric_shape` — new test function
- `test_tcc_encoder_accepts_btc_with_asymmetric_shape` — new test function

**Files created:** none (all modifications to existing files)
**Files modified:** encoder.py (mcl, tstcc), augmentations.py (tstcc), primitives.py, contributing.md, ROADMAP.md, test_mcl_config_and_encoder.py, test_tstcc_supervised.py, test_tstcc_producer.py
