---
phase: 09-fixes-and-updates
plan: 03
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified:
  - src/chronocratic/models/convolutional/standard/series2vec/filters.py
  - src/chronocratic/models/convolutional/dilated/autotcl/losses.py
  - src/chronocratic/models/losses/contrastive.py
  - src/chronocratic/models/convolutional/standard/series2vec/model.py
  - docs/contributing.md
  - scripts/check_device.sh
  - tests/test_device_compat.py
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "series2vec filters run lfilter on data.cpu().numpy() and restore device via torch.as_tensor(result, dtype=torch.float32, device=data.device)"
    - "local_info_nce_loss builds all labels/zeros/eye on z1.device (no CPU tensors used for indexing)"
    - "l1_out_loss builds labels and mask on z1.device (no CPU tensors used for indexing)"
    - "contrastive loss indexing_factor stays as torch.Tensor on device (no .cpu().numpy() round-trip)"
    - "SoftDTW call in series2vec/model.py documents CPU fallback on MPS with comment"
    - "contributing.md has device compatibility convention section"
    - "check_device.sh lint guard passes with # device-ok on legitimate exceptions"
    - "test_device_compat.py smoke tests pass on all available backends"
  artifacts:
    - path: "src/chronocratic/models/convolutional/standard/series2vec/filters.py"
      provides: "Device-aware IIR filter via _filter_on_device helper"
      contains: "_filter_on_device"
    - path: "src/chronocratic/models/convolutional/dilated/autotcl/losses.py"
      provides: "Device-aware label construction in local_info_nce and l1_out"
      contains: "device=z1.device"
    - path: "src/chronocratic/models/losses/contrastive.py"
      provides: "Index kept as torch.Tensor (no numpy round-trip)"
      contains: "torch.Tensor"
    - path: "scripts/check_device.sh"
      provides: "Advisory lint guard for tensor constructors without device="
      contains: "device="
    - path: "tests/test_device_compat.py"
      provides: "Parametrized device smoke tests for fixed losses and filters"
      contains: "parametrize"
    - path: "docs/contributing.md"
      provides: "Device compatibility convention section"
      min_lines: 20
  key_links:
    - from: "src/chronocratic/models/convolutional/standard/series2vec/filters.py"
      to: "src/chronocratic/models/convolutional/standard/series2vec/model.py"
      via: "filter_frequencies calls lowpass/highpass which use _filter_on_device"
      pattern: "_filter_on_device"
    - from: "tests/test_device_compat.py"
      to: "src/chronocratic/models/convolutional/dilated/autotcl/losses.py"
      via: "parametrized tests call local_info_nce_loss, l1_out_loss on each device"
      pattern: "local_info_nce_loss"
    - from: "scripts/check_device.sh"
      to: "src/chronocratic/models/"
      via: "regex scan for torch.(eye|zeros|ones|arange|full|tensor|randn|empty) without device="
      pattern: "torch\\.(eye|zeros|ones|arange|full|tensor|randn|empty)"
---

<objective>
Fix blocking GPU/MPS crash sites, add device convention documentation, lint guard, and smoke tests (D-04).

Purpose: Four crash sites on non-CPU backends: scipy lfilter on MPS tensors (filters.py), CPU labels indexing device tensors (autotcl/losses.py x2), numpy index on device tensors (contrastive.py). Fix each with the single rule: tensors created inside forward/loss/mask must inherit device from input tensors.
Output: Fixed crash sites, contributing.md device section, lint guard script, device smoke tests.
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsmodels/.claude/gsd-core/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/tsmodels/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/skaf/VSCodeProjects/tsmodels/.planning/PROJECT.md
@/Users/skaf/VSCodeProjects/tsmodels/.planning/ROADMAP.md
@/Users/skaf/VSCodeProjects/tsmodels/.planning/STATE.md
@/Users/skaf/VSCodeProjects/tsmodels/.planning/phases/09-fixes-and-updates/09-CONTEXT.md
@/Users/skaf/VSCodeProjects/tsmodels/.planning/phases/09-fixes-and-updates/09-RESEARCH.md
@/Users/skaf/VSCodeProjects/tsmodels/.planning/todos/specs/gpu-mps-device-hardening.md

# Source files to modify
@src/chronocratic/models/convolutional/standard/series2vec/filters.py
@src/chronocratic/models/convolutional/dilated/autotcl/losses.py
@src/chronocratic/models/losses/contrastive.py
@src/chronocratic/models/convolutional/standard/series2vec/model.py
@docs/contributing.md

# New files
@scripts/check_device.sh
@tests/test_device_compat.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix crash sites in filters.py, autotcl/losses.py, contrastive.py, series2vec/model.py</name>
  <files>
    src/chronocratic/models/convolutional/standard/series2vec/filters.py
    src/chronocratic/models/convolutional/dilated/autotcl/losses.py
    src/chronocratic/models/losses/contrastive.py
    src/chronocratic/models/convolutional/standard/series2vec/model.py
  </files>
  <behavior>
    - Test 1: lowpass_filter on MPS tensor returns tensor on same device (not CPU)
    - Test 2: highpass_filter on MPS tensor returns tensor on same device
    - Test 3: local_info_nce_loss with GPU/MPS tensors does not crash (labels built on device)
    - Test 4: l1_out_loss with GPU/MPS tensors does not crash (labels/mask on device)
    - Test 5: temporal_contrastive_loss with GPU/MPS tensors does not crash (indexing_factor on device)
    - Test 6: instance_contrastive_loss with GPU/MPS tensors does not crash
  </behavior>
  <read_first>
    - src/chronocratic/models/convolutional/standard/series2vec/filters.py — crash site: lfilter(b, a, data) passes torch tensor to scipy which calls np.asarray -> .numpy() -> crash on MPS; torch.tensor(filtered_data) defaults to CPU, not data.device
    - src/chronocratic/models/convolutional/dilated/autotcl/losses.py lines 70-79 — crash site: labels built on CPU (torch.eye, torch.zeros without device=), pos_labels gets .to(z1.device) but labels does not; line 77 adds CPU labels to device torch.eye -> crash
    - src/chronocratic/models/convolutional/dilated/autotcl/losses.py lines 130, 135 — crash site: labels built on CPU (torch.arange without device=), mask built on CPU (torch.eye without device=); line 139 indexes similarity_matrix[labels.bool()] on device with CPU mask -> crash
    - src/chronocratic/models/losses/contrastive.py lines 68, 98 — crash site: indexing_factor = torch.arange(...).cpu().numpy() then used for advanced indexing of device tensors
    - src/chronocratic/models/losses/contrastive.py lines 12-13, 24-25 — _compute_contrastive_loss_logits signature takes np.ndarray, docstring says np.array
    - src/chronocratic/models/convolutional/standard/series2vec/model.py line 107 — SoftDTW use_cuda=x.is_cuda and torch.cuda.is_available() is correct but undocumented
    - .planning/todos/specs/gpu-mps-device-hardening.md — full spec for Tasks 1-5
    - src/chronocratic/models/convolutional/standard/tstcc/losses.py — reference implementation: NTXentLoss._correlated_mask takes device explicitly
  </read_first>
  <action>
    Per D-04, fix all four crash sites plus document SoftDTW fallback:

    Task 1 — filters.py: Add _filter_on_device(b, a, data) helper after imports (before filter_frequencies) that does lfilter(b, a, data.cpu().numpy()) then torch.as_tensor(result, dtype=torch.float32, device=data.device). Add import numpy as np. Add ponytail comment about complex->float32 drop. Rewrite lowpass_filter and highpass_filter to call _filter_on_device instead of lfilter + torch.tensor.

    Task 2 — autotcl/losses.py local_info_nce_loss (lines 70-77): Build labels on z1.device from start: torch.eye(k-1, dtype=torch.float32, device=z1.device), torch.zeros(1, k-1, device=z1.device), torch.zeros(k, 1, device=z1.device). Delete now-redundant .to(z1.device) from pos_labels = labels.clone().

    Task 3 — autotcl/losses.py l1_out_loss (lines 130, 135): labels = torch.cat([torch.arange(batch_size, device=z1.device) for _ in range(2)], dim=0). mask = torch.eye(labels.shape[0], dtype=torch.bool, device=z1.device).

    Task 4 — contrastive.py: Remove .cpu().numpy() from indexing_factor in temporal_contrastive_loss (line 68) and instance_contrastive_loss (line 98). Update _compute_contrastive_loss_logits signature from np.ndarray to torch.Tensor and docstring from np.array to torch.Tensor. Remove import numpy as np if ruff reports it unused.

    Task 5 — series2vec/model.py line 107: Add comment above SoftDTW call documenting that CUDA kernel has no MPS equivalent and CPU fallback is correct. Do not change logic.
  </action>
  <verify>
    <automated>uv run ruff check src/chronocratic/models/convolutional/standard/series2vec/filters.py src/chronocratic/models/convolutional/dilated/autotcl/losses.py src/chronocratic/models/losses/contrastive.py src/chronocratic/models/convolutional/standard/series2vec/model.py --no-fix</automated>
  </verify>
  <done>
    All four crash sites fixed: filters.py uses _filter_on_device, local_info_nce labels on z1.device, l1_out labels/mask on z1.device, contrastive indexing_factor stays torch.Tensor. SoftDTW documented. ruff clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Device convention docs, lint guard, smoke tests</name>
  <files>
    docs/contributing.md
    scripts/check_device.sh
    tests/test_device_compat.py
  </files>
  <read_first>
    - docs/contributing.md — current file to append device compatibility section
    - .planning/todos/specs/gpu-mps-device-hardening.md Task 6 — exact contributing.md text to append
    - .planning/todos/specs/gpu-mps-device-hardening.md Task 7 — lint guard script content
    - .planning/todos/specs/gpu-mps-device-hardening.md Task 8 — smoke test content
    - src/chronocratic/models/ — for annotating legitimate # device-ok exceptions in lint guard
  </read_first>
  <action>
    Per D-04, create three artifacts:

    Task 6 — contributing.md: Append "Device Compatibility (CPU / CUDA / MPS)" section with the convention text from the spec. Place near existing "Parameters Consistency" section.

    Task 7 — scripts/check_device.sh: Create scripts/ directory and write check_device.sh that rg-scans for torch.(eye|zeros|ones|arange|full|tensor|randn|empty) without device= in src/chronocratic/models/, excluding **/test* and lines with # device-ok. Run it and annotate legitimate exceptions with # device-ok: utils.py torch.full(...np.nan) then .to(device), ts_transformer.py positional encodings, cost/model.py register_buffer, layers/general.py register_buffer. Make it executable (chmod +x).

    Task 8 — tests/test_device_compat.py: Create device smoke tests parametrized over available backends. Test: autotcl_local_info_nce_runs, autotcl_l1_out_runs, contrastive_losses_run, series2vec_filters_preserve_device. Use _available_devices() helper to detect cpu, cuda, mps.
  </action>
  <verify>
    <automated>uv run pytest tests/test_device_compat.py -v</automated>
  </verify>
  <done>
    contributing.md has device section. check_device.sh runs clean with # device-ok on all legitimate exceptions. test_device_compat.py passes on all available devices.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tensor device placement | Tensors on mismatched devices crash on MPS/CUDA — loss functions must inherit device from inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|-------------|
| T-09-03-01 | Denial of Service | filters.py lfilter on MPS | mitigate | .cpu().numpy() round-trip restores device |
| T-09-03-02 | Denial of Service | autotcl losses CPU labels indexing device tensors | mitigate | device= kwarg on all constructors |
| T-09-03-03 | Denial of Service | contrastive.py numpy index on device tensors | mitigate | Keep indexing_factor as torch.Tensor |
| T-09-03-04 | Spoofing | Future device regression | mitigate | check_device.sh lint guard catches new bare constructors |
| T-09-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages; uses existing torch, scipy, numpy only |
</threat_model>

<verification>
- uv run pytest tests/test_device_compat.py -v (smoke tests on all available backends)
- bash scripts/check_device.sh (lint guard passes with # device-ok annotations)
- uv run pytest -q (full suite does not regress)
- uv run ruff check . && uv run ruff format --check .
</verification>

<success_criteria>
- filters.py uses _filter_on_device for lowpass and highpass (no direct lfilter on device tensors)
- local_info_nce_loss builds all labels on z1.device
- l1_out_loss builds labels and mask on z1.device
- contrastive.py indexing_factor is torch.Tensor, not np.ndarray
- SoftDTW has MPS fallback comment
- contributing.md has device compatibility section
- check_device.sh passes with # device-ok on legitimate exceptions
- test_device_compat.py passes on all available backends
</success_criteria>

<output>
Create `.planning/phases/09-fixes-and-updates/09-03-SUMMARY.md` when done
</output>

### Artifacts this phase produces

**Symbols created/modified:**
- `_filter_on_device(b, a, data)` — new helper in filters.py for scipy IIR round-trip
- `lowpass_filter` — rewritten to use `_filter_on_device` (was `lfilter` + `torch.tensor`)
- `highpass_filter` — rewritten to use `_filter_on_device` (was `lfilter` + `torch.tensor`)
- `local_info_nce_loss` — labels built on `z1.device` (was CPU)
- `l1_out_loss` — labels and mask built on `z1.device` (was CPU)
- `temporal_contrastive_loss` — indexing_factor stays `torch.Tensor` (was `np.ndarray`)
- `instance_contrastive_loss` — indexing_factor stays `torch.Tensor` (was `np.ndarray`)
- `_compute_contrastive_loss_logits` — signature changed `indexing_factor: np.ndarray` to `indexing_factor: torch.Tensor`
- `check_device.sh` — new lint guard script
- `test_device_compat.py` — new test file with parametrized device tests

**Files created:** scripts/check_device.sh, tests/test_device_compat.py
**Files modified:** filters.py, autotcl/losses.py, contrastive.py, series2vec/model.py, contributing.md
