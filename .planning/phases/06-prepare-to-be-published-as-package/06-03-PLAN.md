---
wave: 2
depends_on: [06-01-PLAN]
objective: "Update pyproject.toml name and ruff.toml exclude path per D-01, D-10"
files_modified:
  - "pyproject.toml"
  - "ruff.toml"
requirements: []
autonomous: true
type: execute
must_haves:
  truths:
    - "pyproject.toml project name is 'chronocratic-models'"
    - "ruff.toml exclude path references src/chronocratic/"
    - "No remaining references to 'tscollection' in config files"
  artifacts:
    - path: "pyproject.toml"
      provides: "Package metadata"
      contains: "name = \"chronocratic-models\""
    - path: "ruff.toml"
      provides: "Linter config"
      contains: "src/chronocratic/models/distances/soft_dtw/soft_dtw_cuda.py"
---

## Objective

Update the two configuration files that reference the old `tscollection` namespace: `pyproject.toml` (package name) and `ruff.toml` (exclude path). This plan depends on 06-01 (source rename) because the directory must exist before config files point to it.

## Tasks

### Task 1: Update pyproject.toml package name

<action>
1. In `pyproject.toml`, change `name = "tscollection-models"` to `name = "chronocratic-models"` (line 5).

2. At this stage, do NOT modify dependencies, metadata, or add setuptools_scm — that is handled by 06-04. Only rename the package.
</action>

<read_first>
- pyproject.toml
</read_first>

<acceptance_criteria>
- `pyproject.toml` line 5 reads `name = "chronocratic-models"`
- All other pyproject.toml content remains unchanged (dependencies, dev groups, etc.)
</acceptance_criteria>

### Task 2: Update ruff.toml exclude path

<action>
1. In `ruff.toml`, line 8, change:
   `"src/tscollection/models/distances/soft_dtw/soft_dtw_cuda.py"` → `"src/chronocratic/models/distances/soft_dtw/soft_dtw_cuda.py"`

2. Verify no other references to `tscollection` exist in `ruff.toml`.
</action>

<read_first>
- ruff.toml
</read_first>

<acceptance_criteria>
- `ruff.toml` line 8 reads `src/chronocratic/models/distances/soft_dtw/soft_dtw_cuda.py`
- `grep 'tscollection' ruff.toml` returns no results
</acceptance_criteria>

<verify>
- `grep 'tscollection' pyproject.toml | wc -l` returns `0`
- `grep 'tscollection' ruff.toml | wc -l` returns `0`
- `python -c "import tomllib; tomllib.load(open('pyproject.toml','rb'))"` exits 0
</verify>

## Artifacts This Plan Produces
- `pyproject.toml` with updated package name
- `ruff.toml` with updated exclude path
