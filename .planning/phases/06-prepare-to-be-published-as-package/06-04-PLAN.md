---
wave: 2
depends_on: [06-01-PLAN, 06-03-PLAN]
objective: "Overhaul pyproject.toml with setuptools_scm, towncrier, pruned deps, and complete metadata per D-03, D-04, D-07, D-09, D-10"
files_modified:
  - "pyproject.toml"
  - "src/chronocratic/models/__init__.py"
requirements: []
autonomous: true
type: execute
must_haves:
  truths:
    - "Build system requires setuptools-scm>=8"
    - "Version is dynamic (setuptools_scm)"
    - "Runtime dependencies pruned to 7 packages (numpy, scipy, lightning, torch, einops, numba, tqdm)"
    - "7 unused dependencies removed (torchvision, torchaudio, joblib, openpyxl, h5py, pandas, scikit-learn)"
    - "tqdm>=4.66.0 added (was imported but undeclared — bug fix per D-10)"
    - "towncrier>=24.8 added to dev dependencies"
    - "[project.optional-dependencies] docs defined with sphinx, pydata-sphinx-theme, myst-parser"
    - "[tool.setuptools_scm] configured with version_file = src/chronocratic/models/_version.py"
    - "[tool.towncrier] configured with package = chronocratic.models"
    - "Complete metadata: description, readme, license, keywords, authors, classifiers, project.urls"
    - "requires-python relaxed to >=3.12 (removed upper bound <3.13)"
    - "__version__ exported from chronocratic.models/__init__.py"
  artifacts:
    - path: "pyproject.toml"
      provides: "Complete package configuration"
      contains: "setuptools-scm>=8"
    - path: "src/chronocratic/models/__init__.py"
      provides: "Version export"
      contains: "from ._version import __version__"
---

## Objective

Transform `pyproject.toml` from a minimal config into a complete, publishable package definition. This includes dynamic versioning via setuptools_scm, towncrier changelog configuration, full metadata (authors, classifiers, URLs, keywords), dependency pruning (remove 7 unused deps, add missing tqdm), and docs optional-dependencies. Also add `__version__` export to `models/__init__.py` so Sphinx conf.py can import it.

## Tasks

### Task 1: Overhaul pyproject.toml

<action>
1. Replace the current `pyproject.toml` content with the full target state. Key changes (per D-03, D-04, D-07, D-09, D-10):

   **Build system:** Add `setuptools-scm>=8` to requires, add explicit `build-backend = "setuptools.build_meta"`.

   **Project metadata:**
   - `name = "chronocratic-models"` (already done in 06-03)
   - `dynamic = ["version"]` (replace `version = "0.1.0"`)
   - `description = "Self-supervised time-series representation learning models"`
   - `readme = "README.md"`
   - `requires-python = ">=3.12"` (remove `<3.13` upper bound)
   - `license = "BSD-3-Clause"`
   - `license-files = ["LICENSE"]`
   - `keywords` list: time-series, pytorch, lightning, machine-learning, self-supervised-learning, representation-learning, forecasting, classification, regression, anomaly-detection
   - `authors = [{name = "The Chronocratic Developers", email = "github@users.noreply.github.com"}]`
   - `classifiers`: Development Status :: 3 - Alpha, Intended Audience :: Developers, Intended Audience :: Science/Research, Python 3.12, Python 3.13, CPython, AI/Math/Libraries topics

   **Dependencies (per D-10):**
   - REMOVE: `torchvision>=0.19.0`, `torchaudio>=2.4.0`, `joblib>=1.4.0`, `openpyxl~=3.1.5`, `h5py~=3.16.0`, `pandas>=2.2.0`, `scikit-learn>=1.6,<2.0.0`
   - ADD: `tqdm>=4.66.0` (bug fix — imported in `dilated/_mixin/encoding.py` line 11 but not declared)
   - KEEP: `numpy>=2.1,<3.0.0`, `scipy>=1.13.0`, `lightning>=2.5,<3.0`, `torch>=2.4,<3.0`, `einops>=0.8.2`, `numba>=0.65.1`

   **Optional dependencies (per D-04):**
   - `docs = ["sphinx>=7.0", "pydata-sphinx-theme>=0.15", "myst-parser>=3.0"]`

   **Project URLs (per D-09):**
   - Homepage, Documentation, Repository, Issues (all chronocratic-models)

   **Dev dependencies (per D-07):**
   - Add `towncrier>=24.8` to `[dependency-groups] dev`

   **setuptools_scm config (per D-03):**
   - `version_file = "src/chronocratic/models/_version.py"`
   - `local_scheme = "no-local-version"`
   - `fallback_version = "0.1.0a1"`

   **towncrier config (per D-07):**
   - `package = "chronocratic.models"`
   - `package_dir = "src"`
   - `directory = "changelog.d"`
   - `filename = "CHANGELOG.md"`
   - 6 type directories: added, changed, deprecated, removed, fixed, security

2. Preserve existing sections: `[tool.uv]`, `[tool.setuptools]`, `[tool.setuptools.packages.find]`, `[tool.pytest.ini_options]`.
</action>

<read_first>
- pyproject.toml
- .planning/phases/06-prepare-to-be-published-as-package/06-RESEARCH.md  # Target state section
</read_first>

<acceptance_criteria>
- `pyproject.toml` contains `setuptools-scm>=8` in build-system requires
- `pyproject.toml` has `dynamic = ["version"]`
- `pyproject.toml` has exactly 7 runtime dependencies (no torchvision, torchaudio, joblib, openpyxl, h5py, pandas, scikit-learn)
- `pyproject.toml` has `tqdm>=4.66.0` in dependencies
- `pyproject.toml` has `[project.optional-dependencies] docs` with sphinx stack
- `pyproject.toml` has `[tool.setuptools_scm]` with version_file
- `pyproject.toml` has `[tool.towncrier]` with all 6 type directories
- `pyproject.toml` has complete metadata (description, readme, license, keywords, authors, classifiers, urls)
- `pyproject.toml` has `requires-python = ">=3.12"` (no upper bound)
</acceptance_criteria>

<verify>
- `python -c "import tomllib; tomllib.load(open('pyproject.toml','rb'))"` exits 0
- `grep 'tqdm' pyproject.toml` exits 0 (tqdm in dependencies)
- `grep 'torchvision' pyproject.toml | wc -l` returns `0` (removed)
- `grep 'dynamic' pyproject.toml` exits 0 (setuptools_scm)
</verify>

### Task 2: Add __version__ export to models/__init__.py

<action>
1. At the top of `src/chronocratic/models/__init__.py`, add:
   ```python
   try:
       from ._version import __version__
   except ImportError:
       __version__ = "0.0.0.dev0"  # ponytail: placeholder until setuptools_scm generates _version.py
   ```

2. Add `"__version__"` as the first entry in `__all__`.

3. The `try/except ImportError` guard ensures `__version__` is always available, even before the package is installed in editable mode and `_version.py` is generated by setuptools_scm.
</action>

<read_first>
- src/tscollection/models/__init__.py  # Will be src/chronocratic/models/__init__.py after 06-01
</read_first>

<acceptance_criteria>
- `src/chronocratic/models/__init__.py` imports `__version__` from `._version`
- `"__version__"` is the first entry in `__all__`
</acceptance_criteria>

<verify>
- `grep '__version__' src/chronocratic/models/__init__.py` exits 0
- `grep 'ImportError' src/chronocratic/models/__init__.py` exits 0 (guard present)
</verify>

## Artifacts This Plan Produces
- Fully configured `pyproject.toml` ready for publishing
- `__version__` export in `chronocratic.models`
