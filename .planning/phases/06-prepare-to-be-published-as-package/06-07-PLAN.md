---
wave: 3
depends_on: [06-06-PLAN]
objective: "Create .readthedocs.yaml build configuration per D-04"
files_modified:
  - ".readthedocs.yaml"
requirements: []
autonomous: true
type: execute
must_haves:
  truths:
    - ".readthedocs.yaml version 2"
    - "Build OS: ubuntu-24.04"
    - "Python: 3.12"
    - "Sphinx configuration: docs/conf.py"
    - "Install method: uv sync with docs extra"
  artifacts:
    - path: ".readthedocs.yaml"
      provides: "ReadTheDocs build config"
      contains: "ubuntu-24.04"
---

## Objective

Create `.readthedocs.yaml` to configure ReadTheDocs hosting for the documentation. Matches reference repo exactly per D-04. This plan depends on 06-06 because the docs/ directory and conf.py must exist before RTD can reference them.

## Tasks

### Task 1: Create .readthedocs.yaml

<action>
1. Create `.readthedocs.yaml` with:

```yaml
version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"

sphinx:
  configuration: docs/conf.py

python:
  install:
    - method: uv
      command: sync
      extras:
        - docs
```

2. This configuration tells RTD to:
   - Use ubuntu-24.04 as the build OS
   - Use Python 3.12
   - Find Sphinx config at docs/conf.py
   - Install the package with `uv sync` including the `docs` extra

No additional configuration needed. The reference uses uv; we match that.
</action>

<read_first>
- .planning/phases/06-prepare-to-be-published-as-package/06-RESEARCH.md  # .readthedocs.yaml section
</read_first>

<acceptance_criteria>
- `.readthedocs.yaml` exists at repo root
- Version 2 format
- Build OS is ubuntu-24.04
- Python 3.12
- Sphinx config points to docs/conf.py
- Install method is uv sync with docs extra
</acceptance_criteria>

<verify>
- `test -f .readthedocs.yaml` exits 0
- `grep 'ubuntu-24.04' .readthedocs.yaml` exits 0
</verify>

## Artifacts This Plan Produces
- `.readthedocs.yaml` — ReadTheDocs build configuration
