---
wave: 5
depends_on: [06-01-PLAN, 06-02-PLAN, 06-03-PLAN, 06-04-PLAN, 06-05-PLAN, 06-06-PLAN, 06-07-PLAN, 06-08-PLAN]
objective: "Run smoke tests: build, twine check, pytest, sphinx-build, lint to verify all changes per D-01 through D-10"
files_modified: []
requirements: []
autonomous: true
type: execute
must_haves:
  truths:
    - "python -m build completes without errors"
    - "twine check dist/* passes with no errors"
    - "pytest tests/ passes with no import errors"
    - "sphinx-build -b html docs/ docs/_build/ completes"
    - "ruff check src/ passes"
    - "No references to 'tscollection' remain in src/, tests/, or config files"
  artifacts:
    - path: "dist/"
      provides: "Built wheel and sdist"
---

## Objective

Final verification that all phase changes work together. Run the full smoke test suite: package build, twine validation, test suite, docs build, and lint check. This plan depends on ALL prior plans completing successfully.

## Tasks

### Task 1: Full smoke test battery

<action>
1. **Verify no dangling references:**
   - `grep -rl 'tscollection' src/ tests/ --include='*.py'` — must return zero files
   - `grep -rl 'tscollection' pyproject.toml ruff.toml` — must return zero results

2. **Verify namespace package structure:**
   - `test ! -f src/chronocratic/__init__.py` — must pass (PEP 420)
   - `test -f src/chronocratic/models/__init__.py` — must pass (models is a real package)

3. **Install package in editable mode:**
   - `uv sync` — installs package with all dependencies (including the new tqdm)

4. **Run test suite:**
   - `uv run pytest tests/ -x --tb=short` — all tests pass, no import errors

5. **Build package:**
   - `uv run python -m build` — produces wheel and sdist in dist/

6. **Validate with twine:**
   - `uv run twine check dist/*` — no errors or warnings

7. **Build docs:**
   - `uv sync --extra docs`
   - `uv run sphinx-build -b html docs/ docs/_build/` — completes without errors

8. **Lint check:**
   - `uv run ruff check src/` — passes
   - `uv run ruff check tests/` — passes
</action>

<read_first>
- pyproject.toml
- .github/workflows/build-and-test.yml
</read_first>

<acceptance_criteria>
- `grep -rl 'tscollection' src/ tests/ --include='*.py'` returns zero files
- `src/chronocratic/__init__.py` does NOT exist
- `uv sync` completes without errors
- `uv run pytest tests/ -x` passes all tests
- `uv run python -m build` produces wheel and sdist
- `uv run twine check dist/*` passes
- `uv run sphinx-build -b html docs/ docs/_build/` completes
- `uv run ruff check src/` passes
</acceptance_criteria>

<verify>
- `grep -rl 'tscollection' src/ tests/ --include='*.py' | wc -l` returns `0`
- `test ! -f src/chronocratic/__init__.py` exits 0
- `uv sync` exits 0
- `uv run pytest tests/ -x --tb=short` exits 0
- `uv run python -m build` exits 0 and produces `dist/*.whl`
- `uv run twine check dist/*` exits 0
- `uv run sphinx-build -b html docs/ docs/_build/` exits 0
- `uv run ruff check src/` exits 0
</verify>

## Artifacts This Plan Produces
- Verified package build (wheel + sdist in dist/)
- Passing test suite
- Built documentation
- Clean lint output
