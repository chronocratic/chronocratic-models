---
wave: 4
depends_on: [06-01-PLAN, 06-03-PLAN, 06-04-PLAN]
objective: "Fix build-and-test.yml coverage path and docs job per D-08"
files_modified:
  - ".github/workflows/build-and-test.yml"
requirements: []
autonomous: true
type: execute
must_haves:
  truths:
    - "Coverage path changed from src/chronocratic/datasets to src/chronocratic/models"
    - "Docs job uses uv sync --extra docs instead of pip install -e .[docs]"
    - "No references to tscollection remain in CI workflows"
  artifacts:
    - path: ".github/workflows/build-and-test.yml"
      provides: "Fixed CI workflow"
      contains: "--cov=src/chronocratic/models"
---

## Objective

Fix the CI workflow that references incorrect paths. The `build-and-test.yml` was cherry-picked from a setup commit and has `--cov=src/chronocratic/datasets` (wrong package). Per D-08, change to `src/chronocratic/models` and ensure docs job uses uv.

## Tasks

### Task 1: Fix build-and-test.yml coverage and docs job

<action>
1. In `.github/workflows/build-and-test.yml`:

   **Coverage path:** Change:
   - FROM: `uv run pytest tests/ --cov=src/chronocratic/datasets --cov-report=xml`
   - TO: `uv run pytest tests/ --cov=src/chronocratic/models --cov-report=xml`

   **Docs job:** If it uses `pip install -e ".[docs]"`, replace with `uv sync --extra docs`.

2. Verify no references to `tscollection` or `chronocratic/datasets` exist in any workflow file.

3. Check `pypi-publish.yml` — it already references `chronocratic-models`, no changes needed.
</action>

<read_first>
- .github/workflows/build-and-test.yml
- .github/workflows/pypi-publish.yml
</read_first>

<acceptance_criteria>
- `build-and-test.yml` coverage uses `--cov=src/chronocratic/models`
- No references to `tscollection` or `chronocratic/datasets` remain in CI workflows
- `pypi-publish.yml` is unchanged (already correct)
</acceptance_criteria>

<verify>
- `grep 'src/chronocratic/models' .github/workflows/build-and-test.yml` exits 0
- `grep 'tscollection' .github/workflows/build-and-test.yml | wc -l` returns `0`
- `grep 'chronocratic/datasets' .github/workflows/build-and-test.yml | wc -l` returns `0`
</verify>

## Artifacts This Plan Produces
- Fixed `.github/workflows/build-and-test.yml` with correct coverage path
