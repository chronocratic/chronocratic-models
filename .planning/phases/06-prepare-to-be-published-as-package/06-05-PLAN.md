---
wave: 2
depends_on: [06-01-PLAN]
objective: "Create LICENSE, README.md, CHANGELOG.md, and changelog.d/ infrastructure per D-05, D-06, D-07"
files_modified:
  - "LICENSE"
  - "README.md"
  - "CHANGELOG.md"
  - "changelog.d/README.md"
  - "changelog.d/added/.gitkeep"
  - "changelog.d/changed/.gitkeep"
  - "changelog.d/deprecated/.gitkeep"
  - "changelog.d/removed/.gitkeep"
  - "changelog.d/fixed/.gitkeep"
  - "changelog.d/security/.gitkeep"
requirements: []
autonomous: true
type: execute
must_haves:
  truths:
    - "LICENSE file exists with BSD-3-Clause text, copyright The Chronocratic Developers 2026-Present"
    - "README.md exists with badges, install section, quick start, model catalog, features, docs link"
    - "CHANGELOG.md exists with towncrier start string"
    - "changelog.d/ directory exists with 6 type subdirs and README.md"
  artifacts:
    - path: "LICENSE"
      provides: "BSD-3-Clause license"
      contains: "The Chronocratic Developers"
    - path: "README.md"
      provides: "Package landing page"
      min_lines: 40
    - path: "CHANGELOG.md"
      provides: "Changelog entry point"
      contains: "<!-- towncrier release notes start -->"
    - path: "changelog.d/README.md"
      provides: "Fragment instructions"
      min_lines: 10
---

## Objective

Create the static package metadata files: LICENSE (BSD-3-Clause per D-06), README.md with full structure per D-05, CHANGELOG.md stub, and changelog.d/ directory tree with type subdirectories per D-07.

## Tasks

### Task 1: Create LICENSE

<action>
1. Create `LICENSE` with standard BSD-3-Clause text.
2. Copyright holder: "The Chronocratic Developers"
3. Year: "2026"

The standard BSD-3-Clause boilerplate text (3 clauses: redistribution, modified form, no endorsement).
</action>

<read_first>
- .planning/phases/06-prepare-to-be-published-as-package/06-RESEARCH.md  # D-06 section
</read_first>

<acceptance_criteria>
- `LICENSE` file exists with BSD-3-Clause text
- Contains "The Chronocratic Developers" as copyright holder
- Contains year 2026
</acceptance_criteria>

<verify>
- `test -f LICENSE` exits 0
- `grep 'BSD 3-Clause' LICENSE` exits 0
</verify>

### Task 2: Create README.md

<action>
1. Create `README.md` matching the reference repo structure (per D-05):

   **Badges row:** License (BSD-3-Clause), PyPI version, Python versions (3.12+), downloads, build status (GitHub Actions), documentation (ReadTheDocs), code style (ruff), GitHub stars.

   **Installation section:** `pip install chronocratic-models` with note that PyPI name uses hyphen but import uses namespace: "The PyPI package name uses a hyphen (`chronocratic-models`), but the import uses the `chronocratic.models` namespace."

   **Quick start section:** TS2Vec encoding example with synthetic data:
   - Import `from chronocratic.models import TS2Vec, TS2VecModelParameters`
   - Create model with parameters
   - Call `encode()` on a synthetic tensor

   **Models section:** Catalog of all 10 models grouped by family:
   - Convolutional/dilated: TS2Vec, CoST, AutoTCL
   - Convolutional/standard: Series2Vec, TSTCC, FCN
   - Transformer: TST
   - Recurrent: TimeNet
   - Generative: TimeVAE

   **Features section:** Library highlights:
   - Polymorphic augmentation producer contract
   - Lightning integration for training
   - Self-supervised representation learning
   - Pre-configured model parameters
   - NumPy and PyTorch tensor support

   **Documentation section:** Link to `https://chronocratic-models.readthedocs.io/`

   **License section:** BSD 3-Clause
</action>

<read_first>
- .planning/phases/06-prepare-to-be-published-as-package/06-CONTEXT.md  # D-05, D-06 details
- src/tscollection/models/__init__.py  # Model list for catalog
</read_first>

<acceptance_criteria>
- `README.md` exists with all 5 sections: badges, install, quick start, model catalog, features
- All 10 models listed in the catalog
- Badge for ruff, PyPI, ReadTheDocs, BSD-3-Clause
- Quick start shows TS2Vec encode() example
- Contains namespace note about hyphen vs dot
</acceptance_criteria>

<verify>
- `test -f README.md` exits 0
- `grep 'chronocratic-models' README.md` exits 0
- `grep 'pip install' README.md` exits 0
</verify>

### Task 3: Create CHANGELOG.md and changelog.d/

<action>
1. Create `CHANGELOG.md` with:
   - Title: `# Changelog`
   - Brief intro: "All notable changes to this project will be documented in this file."
   - Towncrier start string: `<!-- towncrier release notes start -->`
   - Note about towncrier fragments

2. Create `changelog.d/` directory structure:
   - `changelog.d/README.md` — instructions on how to add fragments
   - `changelog.d/added/` (empty, add .gitkeep)
   - `changelog.d/changed/` (empty, add .gitkeep)
   - `changelog.d/deprecated/` (empty, add .gitkeep)
   - `changelog.d/removed/` (empty, add .gitkeep)
   - `changelog.d/fixed/` (empty, add .gitkeep)
   - `changelog.d/security/` (empty, add .gitkeep)
</action>

<read_first>
- .planning/phases/06-prepare-to-be-published-as-package/06-RESEARCH.md  # D-07, towncrier config
</read_first>

<acceptance_criteria>
- `CHANGELOG.md` exists with towncrier start string
- `changelog.d/` exists with 6 subdirectories
- `changelog.d/README.md` exists with fragment instructions
- Each subdirectory has .gitkeep (so git tracks them)
</acceptance_criteria>

<verify>
- `test -d changelog.d` exits 0
- `test -f CHANGELOG.md` exits 0
</verify>

## Artifacts This Plan Produces
- `LICENSE` — BSD-3-Clause text
- `README.md` — Full package landing page
- `CHANGELOG.md` — Towncrier entry point
- `changelog.d/` — Fragment directory tree (6 types)
