---
phase: 01-foundation
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/tscollection/models/_abstract/encoding_functionality_mixin.py
  - src/tscollection/models/_abstract/__init__.py
  - tests/test_mixin.py
autonomous: true
requirements: [MIX-01, MIX-02, MIX-03, MIX-04]
user_setup: []

must_haves:
  truths:
    - "BaseEncodingMixin(ABC) cannot be instantiated directly (enforces abstract _get_eval_method)"
    - "PoolingEncodingMixin._get_eval_method() returns _evaluate_with_pooling bound method"
    - "DecompositionEncodingMixin._get_eval_method() returns _evaluate_with_feature_concatenation bound method"
    - "PoolingEncodingMixin._get_slice() returns a real slice object (not None)"
    - "DecompositionEncodingMixin._get_slice() returns None (inherits base default)"
    - "encode() uses _get_encoder() and _get_eval_method() polymorphically (no hasattr branching)"
    - "encode() does not mutate instance state (no self._encoder or self._eval_method assignment)"
    - "DataLoader uses persistent_workers=num_workers > 0 (not hardcoded True)"
    - "Sliding window full_series uses .transpose(1, 2).contiguous() pooling (not raw .squeeze(1))"
  artifacts:
    - path: "src/tscollection/models/_abstract/encoding_functionality_mixin.py"
      provides: "BaseEncodingMixin, PoolingEncodingMixin, DecompositionEncodingMixin"
      contains: "class BaseEncodingMixin"
    - path: "src/tscollection/models/_abstract/__init__.py"
      provides: "Barrel exports for 3 mixin classes"
      contains: "encoding_functionality_mixin"
    - path: "tests/test_mixin.py"
      provides: "Unit tests for mixin hierarchy, importability, encode behavior"
      min_lines: 80
  key_links:
    - from: "BaseEncodingMixin.encode()"
      to: "_get_encoder(), _get_eval_method()"
      via: "Polymorphic dispatch (no instance state mutation)"
      pattern: "encoder = self._get_encoder"
    - from: "PoolingEncodingMixin"
      to: "BaseEncodingMixin"
      via: "_get_eval_method -> _evaluate_with_pooling"
      pattern: "def _get_eval_method"
    - from: "DecompositionEncodingMixin"
      to: "BaseEncodingMixin"
      via: "_get_eval_method -> _evaluate_with_feature_concatenation"
      pattern: "def _get_eval_method"
---

<objective>
Replace single EncodingFunctionalityMixin with 3-class hierarchy: BaseEncodingMixin(ABC), PoolingEncodingMixin, DecompositionEncodingMixin.
Purpose: Eliminate fragile hasattr() branching in favor of compile-time polymorphism via subclass overrides. New models inherit the appropriate mixin subclass. Implements MIX-01, MIX-02, MIX-03 per D-05, D-06. Also fixes two runtime bugs: persistent_workers crash with num_workers=0 and sliding-window full_series shape regression.
Output: src/tscollection/models/_abstract/encoding_functionality_mixin.py with 3 classes, updated __init__.py, tests/test_mixin.py.
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsmodels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/tsmodels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# User decisions (locked)
@.planning/phases/01-foundation/01-CONTEXT.md

# Research and patterns
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md

# Current code (to replace)
@src/tscollection/models/_abstract/encoding_functionality_mixin.py
@src/tscollection/models/_abstract/__init__.py

# Rbspaper reference (port with D-05 adaptations)
@_sources/rbspaper/src/rbspaper/models/abstract/encoding_functionality_mixin.py

# Utility functions used by mixin
@src/tscollection/models/utils.py

# Conventions
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="tdd">
  <name>Mixin hierarchy with polymorphic dispatch and tests</name>
  <files>src/tscollection/models/_abstract/encoding_functionality_mixin.py, src/tscollection/models/_abstract/__init__.py, tests/test_mixin.py, tests/conftest.py</files>

  <read_first>
  - src/tscollection/models/_abstract/encoding_functionality_mixin.py (full file: current single-class mixin with hasattr branching)
  - _sources/rbspaper/src/rbspaper/models/abstract/encoding_functionality_mixin.py (full file: 3-class hierarchy reference to port)
  - src/tscollection/models/utils.py (full file: utility functions -- apply_slicing, full_series_pooling, integer_pooling, multiscale_pooling, concat_last_step_features, extract_features_from_batch, process_sliding_window)
  - src/tscollection/models/_abstract/__init__.py (current barrel export)
  - .planning/phases/01-foundation/01-CONTEXT.md (D-05, D-06 decisions)
  - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 1: Mixin Hierarchy section; Pitfalls 1-3)
  - .planning/phases/01-foundation/01-PATTERNS.md (mixin pattern section, shared patterns)
  - .planning/codebase/CONVENTIONS.md (import order, error handling, logging)
  - tests/conftest.py (created by plan 01; read for existing fixtures)
  </read_first>

  <behavior>
  - Test: from tscollection.models._abstract import PoolingEncodingMixin succeeds (MIX-01)
  - Test: from tscollection.models._abstract import DecompositionEncodingMixin succeeds (MIX-02)
  - Test: from tscollection.models._abstract import BaseEncodingMixin succeeds (MIX-03)
  - Test: PoolingEncodingMixin is subclass of BaseEncodingMixin
  - Test: DecompositionEncodingMixin is subclass of BaseEncodingMixin
  - Test: BaseEncodingMixin is ABC (issubclass check with abc.ABC)
  - Test: Concrete PoolingEncodingMixin subclass._get_eval_method() returns _evaluate_with_pooling
  - Test: Concrete DecompositionEncodingMixin subclass._get_eval_method() returns _evaluate_with_feature_concatenation
  - Test: Concrete PoolingEncodingMixin subclass._get_slice(10, 20) returns slice(10, 30)
  - Test: Concrete DecompositionEncodingMixin subclass._get_slice(10, 20) returns None
  - Test: Concrete PoolingEncodingMixin subclass._get_encoder() returns self._averaged_encoder (default)
  - Test: encode() on Pooling mixin subclass does not set self._encoder or self._eval_method instance attributes (no state mutation)
  - Test: encode() DataLoader uses persistent_workers=num_workers > 0 (verify via source: grep persistent_workers in mixin file)
  - Test: encode() calls self._get_encoder() and self._get_eval_method() each invocation (polymorphic dispatch, verified by source inspection)
  - Test: _evaluate_with_feature_concatenation raises ValueError for encoding_window not in (None, 'full_series')
  - Test: Sliding window full_series path uses .transpose(1, 2).contiguous() followed by pooling (verified by source: grep transpose in _compute_sliding_representations)
  </behavior>

  <action>
  Implement the 3-class mixin hierarchy using TDD cycle. Port from rbspaper reference with D-05 adaptations.

  **RED**: Create tests/test_mixin.py with all behavior tests from the block above. Append to tests/conftest.py if it exists (from plan 01) or create it. Tests import from tscollection.models._abstract. Run uv run pytest tests/test_mixin.py -x (expect failure -- new classes do not exist yet).

  **GREEN**: Rewrite src/tscollection/models/_abstract/encoding_functionality_mixin.py as a 3-class hierarchy. Three classes in one file:

  1. BaseEncodingMixin(ABC) -- shared encode() entry point, _compute_sliding_representations(), abstract _get_eval_method(), default _get_encoder() returning self._averaged_encoder, default _get_slice() returning None.
  2. PoolingEncodingMixin(BaseEncodingMixin) -- _get_eval_method() returns self._evaluate_with_pooling, _get_slice() returns slice(sliding_padding, sliding_padding + sliding_length), _evaluate_with_pooling() uses self._get_encoder() (not self._encoder directly).
  3. DecompositionEncodingMixin(BaseEncodingMixin) -- _get_encoder() returns self.query_encoder, _get_eval_method() returns self._evaluate_with_feature_concatenation, _evaluate_with_feature_concatenation() validates encoding_window and uses self._get_encoder().

  D-05 adaptations (must apply):
  - Drop encoder is None guard from encode() -- encoders always set by constructors. Do NOT include `if encoder is None: raise RuntimeError(...)` in the port.
  - Use polymorphic dispatch: encode() calls self._get_encoder() and self._get_eval_method() as local variables (not instance attributes). _compute_sliding_representations uses self._get_slice() and self._get_eval_method() as local variables.
  - Use persistent_workers=num_workers > 0 in DataLoader (not hardcoded True).
  - Use .transpose(1, 2).contiguous() for sliding window full_series pooling (not raw .squeeze(1)).
  - Rename logger to _logger (module-level private convention).
  - Extract _EXPECTED_INPUT_DIMS = 3 constant.
  - Make causal, sliding_length, sliding_padding keyword-only after * in encode() signature.
  - Use @override decorator on all method overrides in PoolingEncodingMixin and DecompositionEncodingMixin (from typing import override).
  - Use TYPE_CHECKING guard for MaskMode import to avoid circular imports.
  - Call _logger.info() with %s formatting (not f-strings + logging.info).

  Update __all__ to: ['BaseEncodingMixin', 'DecompositionEncodingMixin', 'PoolingEncodingMixin'] (alphabetical).
  Update src/tscollection/models/_abstract/__init__.py -- star import from encoding_functionality_mixin is sufficient since __all__ controls exports.

  **REFACTOR**: Ensure Google-style docstrings on all public methods. Verify uv run ty check src/tscollection/models/_abstract/encoding_functionality_mixin.py passes.
  </action>

  <verify>
    <automated>uv run pytest tests/test_mixin.py -x -q</automated>
  </verify>

  <acceptance_criteria>
  - pytest tests/test_mixin.py passes with all 16+ behavior tests green
  - uv run ty check src/tscollection/models/_abstract/encoding_functionality_mixin.py exits 0
  - __all__ in encoding_functionality_mixin.py contains exactly: BaseEncodingMixin, DecompositionEncodingMixin, PoolingEncodingMixin (no EncodingFunctionalityMixin)
  - No hasattr() calls remain in the mixin file: `grep -c 'hasattr' src/tscollection/models/_abstract/encoding_functionality_mixin.py` returns 0
  - No self._encoder = assignments in encode() or _compute_sliding_representations (no instance state mutation)
  - persistent_workers=num_workers > 0 present: `grep 'persistent_workers=num_workers > 0' src/tscollection/models/_abstract/encoding_functionality_mixin.py` returns match
  - .transpose(1, 2) present in _compute_sliding_representations full_series path: `grep 'transpose(1, 2)' src/tscollection/models/_abstract/encoding_functionality_mixin.py` returns match
  - @override decorator used on all method overrides in PoolingEncodingMixin and DecompositionEncodingMixin
  - encoder is None guard NOT present: `grep -c 'encoder is None' src/tscollection/models/_abstract/encoding_functionality_mixin.py` returns 0
  - _logger used (not logger): `grep '_logger = logging' src/tscollection/models/_abstract/encoding_functionality_mixin.py` returns match
  - _EXPECTED_INPUT_DIMS = 3 constant present
  - MaskMode imported via TYPE_CHECKING guard
  - _get_encoder() in DecompositionEncodingMixin returns self.query_encoder
  - _evaluate_with_feature_concatenation raises ValueError for invalid encoding_window
  - From _abstract import: PoolingEncodingMixin, DecompositionEncodingMixin, BaseEncodingMixin all resolvable
  </acceptance_criteria>

  <done>All mixin tests pass, ty check clean, 3-class hierarchy implemented with D-05 adaptations</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan refactors internal mixin classes. No new external I/O or trust boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-SC | Tampering | pip/uv installs | mitigate | No new packages; stdlib abc, typing + existing torch/tqdm |

</threat_model>

<verification>
- uv run pytest tests/test_mixin.py -x -q passes all tests
- uv run ty check src/tscollection/models/_abstract/ encoding_functionality_mixin.py exits 0
- grep -c 'hasattr' src/tscollection/models/_abstract/encoding_functionality_mixin.py returns 0
- grep -c 'encoder is None' src/tscollection/models/_abstract/encoding_functionality_mixin.py returns 0
</verification>

<success_criteria>
- 3 classes defined: BaseEncodingMixin(ABC), PoolingEncodingMixin, DecompositionEncodingMixin
- No hasattr() branching (replaced by polymorphic _get_encoder/_get_eval_method/_get_slice)
- No instance state mutation in encode()
- persistent_workers=num_workers > 0 (fixes crash with num_workers=0)
- Sliding window full_series uses .transpose(1, 2) pooling (fixes shape regression)
- encoder is None guard dropped (D-05)
- All mixin tests pass
- MIX-01, MIX-02, MIX-03, MIX-04 addressed
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done
</output>
