---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: [01-01, 01-02]
files_modified:
  - src/tscollection/models/ts2vec/model.py
  - src/tscollection/models/cost/model.py
  - src/tscollection/models/autotcl/model.py
  - tests/test_from_config.py
autonomous: true
requirements: [MIX-04, CFG-01, CFG-03]
user_setup: []

must_haves:
  truths:
    - "TS2Vec inherits from PoolingEncodingMixin (not EncodingFunctionalityMixin)"
    - "CoST inherits from DecompositionEncodingMixin (not EncodingFunctionalityMixin)"
    - "AutoTCL inherits from PoolingEncodingMixin (not EncodingFunctionalityMixin)"
    - "TS2Vec.from_config(config) returns a TS2Vec instance with correct attributes"
    - "CoST.from_config(config) returns a CoST instance with correct attributes"
    - "AutoTCL.from_config(config) returns an AutoTCL instance with correct attributes"
    - "encode() behavior unchanged for all 3 models after mixin swap"
  artifacts:
    - path: "src/tscollection/models/ts2vec/model.py"
      provides: "TS2Vec with PoolingEncodingMixin and from_config()"
      contains: "PoolingEncodingMixin"
    - path: "src/tscollection/models/cost/model.py"
      provides: "CoST with DecompositionEncodingMixin and from_config()"
      contains: "DecompositionEncodingMixin"
    - path: "src/tscollection/models/autotcl/model.py"
      provides: "AutoTCL with PoolingEncodingMixin and from_config()"
      contains: "PoolingEncodingMixin"
    - path: "tests/test_from_config.py"
      provides: "Integration tests for from_config() round-trip on all 3 models"
      min_lines: 60
  key_links:
    - from: "src/tscollection/models/ts2vec/model.py"
      to: "src/tscollection/models/_abstract/encoding_functionality_mixin.py"
      via: "PoolingEncodingMixin import"
      pattern: "from tscollection.models._abstract import PoolingEncodingMixin"
    - from: "src/tscollection/models/cost/model.py"
      to: "src/tscollection/models/_abstract/encoding_functionality_mixin.py"
      via: "DecompositionEncodingMixin import"
      pattern: "from tscollection.models._abstract import DecompositionEncodingMixin"
    - from: "src/tscollection/models/autotcl/model.py"
      to: "src/tscollection/models/_abstract/encoding_functionality_mixin.py"
      via: "PoolingEncodingMixin import"
      pattern: "from tscollection.models._abstract import PoolingEncodingMixin"
    - from: "TS2Vec.from_config()"
      to: "TS2VecModelParameters"
      via: "vars(config) unpack + **additional_kwargs"
      pattern: "def from_config.*TS2VecModelParameters"
---

<objective>
Update all 3 models to inherit new mixin classes and add from_config() classmethod.
Purpose: Wire the config dataclasses (plan 01) and mixin hierarchy (plan 02) into each model. TS2Vec and AutoTCL use PoolingEncodingMixin. CoST uses DecompositionEncodingMixin. Each model gains a from_config() factory method. Implements MIX-04, CFG-01, CFG-03 per D-04, D-06.
Output: 3 updated model files, from_config() integration tests.
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsmodels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/tsmodels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# User decisions (locked)
@.planning/phases/01-foundation/01-CONTEXT.md

# Research
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md

# Current model files (to modify)
@src/tscollection/models/ts2vec/model.py
@src/tscollection/models/cost/model.py
@src/tscollection/models/autotcl/model.py

# Prior plan outputs (dependencies)
@src/tscollection/models/config.py
@src/tscollection/models/_abstract/encoding_functionality_mixin.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Update model inheritance and add from_config() for all 3 models</name>
  <files>src/tscollection/models/ts2vec/model.py, src/tscollection/models/cost/model.py, src/tscollection/models/autotcl/model.py, tests/test_from_config.py</files>

  <read_first>
  - src/tscollection/models/ts2vec/model.py (full file: current TS2Vec with EncodingFunctionalityMixin)
  - src/tscollection/models/cost/model.py (full file: current CoST with EncodingFunctionalityMixin)
  - src/tscollection/models/autotcl/model.py (full file: current AutoTCL with EncodingFunctionalityMixin)
  - src/tscollection/models/config.py (created by plan 01: TS2VecModelParameters, CoSTModelParameters, AutoTCLModelParameters)
  - src/tscollection/models/_abstract/encoding_functionality_mixin.py (created by plan 02: PoolingEncodingMixin, DecompositionEncodingMixin)
  - .planning/phases/01-foundation/01-CONTEXT.md (D-04: from_config pattern; D-06: mixin assignments)
  - .planning/phases/01-foundation/01-PATTERNS.md (model inheritance pattern section, from_config section)
  - .planning/codebase/CONVENTIONS.md (import order, __all__ pattern)
  </read_first>

  <behavior>
  - Test: TS2Vec.from_config(TS2VecModelParameters(input_dims=1)) returns TS2Vec instance
  - Test: CoST.from_config(CoSTModelParameters(input_dims=1, sequence_length=100)) returns CoST instance
  - Test: AutoTCL.from_config(AutoTCLModelParameters(input_dims=1)) returns AutoTCL instance
  - Test: from_config() instance has correct hidden_dims, output_dims, depth, dropout_rate from config
  - Test: from_config() passes additional kwargs (augmentation_mode, augmentation_method_params) to __init__
  - Test: TS2Vec class inherits PoolingEncodingMixin: issubclass(TS2Vec, PoolingEncodingMixin)
  - Test: CoST class inherits DecompositionEncodingMixin: issubclass(CoST, DecompositionEncodingMixin)
  - Test: AutoTCL class inherits PoolingEncodingMixin: issubclass(AutoTCL, PoolingEncodingMixin)
  </behavior>

  <action>
  Update each model file with new mixin import and from_config() classmethod. Each model is a focused change (2 lines import + 1 line class + ~5 lines from_config).

  For each model:
  1. Replace import: `from tscollection.models._abstract import PoolingEncodingMixin` (TS2Vec, AutoTCL) or `from tscollection.models._abstract import DecompositionEncodingMixin` (CoST). Old import `EncodingFunctionalityMixin` must be removed.
  2. Replace class declaration: `class TS2Vec(pl.LightningModule, PoolingEncodingMixin):` etc.
  3. Add config import: `from tscollection.models.config import TS2VecModelParameters` (or CoSTModelParameters / AutoTCLModelParameters).
  4. Add from_config() classmethod after __init__ (before or after configure_optimizers). Pattern per D-04:

  ```
  @classmethod
  def from_config(cls, config, **additional_kwargs):
      return cls(**vars(config), **additional_kwargs)
  ```

  TS2Vec specifics:
  - Import: `from tscollection.models._abstract import PoolingEncodingMixin`
  - Import: `from tscollection.models.config import TS2VecModelParameters`
  - Class: `class TS2Vec(pl.LightningModule, PoolingEncodingMixin):`
  - from_config takes config: TS2VecModelParameters
  - TS2Vec has _averaged_encoder attribute; PoolingEncodingMixin._get_encoder() default returns self._averaged_encoder (correct)

  CoST specifics:
  - Import: `from tscollection.models._abstract import DecompositionEncodingMixin`
  - Import: `from tscollection.models.config import CoSTModelParameters`
  - Class: `class CoST(pl.LightningModule, DecompositionEncodingMixin):`
  - from_config takes config: CoSTModelParameters
  - CoST has query_encoder attribute; DecompositionEncodingMixin._get_encoder() returns self.query_encoder (correct)

  AutoTCL specifics:
  - Import: `from tscollection.models._abstract import PoolingEncodingMixin`
  - Import: `from tscollection.models.config import AutoTCLModelParameters`
  - Class: `class AutoTCL(pl.LightningModule, PoolingEncodingMixin):`
  - from_config takes config: AutoTCLModelParameters
  - AutoTCL has _averaged_encoder attribute; PoolingEncodingMixin._get_encoder() default returns self._averaged_encoder (correct)

  CRITICAL: Do not modify __init__, training_step, or validation_step. Only change import, class declaration, and add from_config(). Preserve all existing noqa comments and docstrings.

  Write tests/test_from_config.py with behavior tests. Tests require augmentation_mode and augmentation_method_params as additional_kwargs for from_config() (since config excludes augmentation fields per D-01). Import augmentation enums and factories to construct minimal valid kwargs.
  </action>

  <verify>
    <automated>uv run pytest tests/test_from_config.py -x -q</automated>
  </verify>

  <acceptance_criteria>
  - pytest tests/test_from_config.py passes with all 8 behavior tests green
  - TS2Vec inherits PoolingEncodingMixin: `grep 'class TS2Vec.*PoolingEncodingMixin' src/tscollection/models/ts2vec/model.py` returns match
  - CoST inherits DecompositionEncodingMixin: `grep 'class CoST.*DecompositionEncodingMixin' src/tscollection/models/cost/model.py` returns match
  - AutoTCL inherits PoolingEncodingMixin: `grep 'class AutoTCL.*PoolingEncodingMixin' src/tscollection/models/autotcl/model.py` returns match
  - No references to EncodingFunctionalityMixin remain in model files: `grep -c 'EncodingFunctionalityMixin' src/tscollection/models/ts2vec/model.py src/tscollection/models/cost/model.py src/tscollection/models/autotcl/model.py` returns 0 for each
  - from_config() method present in all 3 model files: `grep -c 'def from_config' src/tscollection/models/ts2vec/model.py src/tscollection/models/cost/model.py src/tscollection/models/autotcl/model.py` returns 1 for each
  - vars(config) + additional_kwargs pattern used: `grep '\*\*vars(config)' src/tscollection/models/ts2vec/model.py` returns match
  - TS2Vec __init__, training_step, validation_step unchanged (line count same as before)
  - CoST __init__, training_step, validation_step unchanged
  - AutoTCL __init__, training_step, validation_step unchanged
  - All imports follow CONVENTIONS.md order (__all__ first, stdlib, third-party, local)
  - __all__ in each model file unchanged (still exports only the model class)
  </acceptance_criteria>

  <done>All 3 models inherit correct mixins, from_config() works with augmentation kwargs, tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan modifies existing model classes. No new external I/O or trust boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-SC | Tampering | pip/uv installs | mitigate | No new packages; existing imports only |

</threat_model>

<verification>
- uv run pytest tests/test_from_config.py -x -q passes
- uv run ty check src/tscollection/models/ts2vec/model.py src/tscollection/models/cost/model.py src/tscollection/models/autotcl/model.py exits 0
- grep -c 'EncodingFunctionalityMixin' on all 3 model files returns 0
</verification>

<success_criteria>
- TS2Vec inherits PoolingEncodingMixin, has from_config(TS2VecModelParameters)
- CoST inherits DecompositionEncodingMixin, has from_config(CoSTModelParameters)
- AutoTCL inherits PoolingEncodingMixin, has from_config(AutoTCLModelParameters)
- from_config() passes vars(config) + additional_kwargs to __init__
- __init__, training_step, validation_step unchanged for all 3 models
- MIX-04 satisfied: encode() behavior preserved (mixin swap uses polymorphic dispatch equivalent to old hasattr branching)
- CFG-01/CFG-03 satisfied: from_config integrates config dataclasses into model instantiation
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done
</output>
