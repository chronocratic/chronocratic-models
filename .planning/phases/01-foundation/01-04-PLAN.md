---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - src/tscollection/models/config.py
  - src/tscollection/models/_abstract/encoding_functionality_mixin.py
  - src/tscollection/models/ts2vec/model.py
  - src/tscollection/models/cost/model.py
  - src/tscollection/models/autotcl/model.py
autonomous: true
requirements: [CFG-03, MIX-04]
user_setup: []

must_haves:
  truths:
    - "uv run ty check src/ exits 0 with zero type errors across all modified files"
    - "uv run ruff check src/ exits 0 with no lint errors on modified files"
    - "All tests (test_config.py, test_mixin.py, test_from_config.py) pass together"
    - "No stale references to EncodingFunctionalityMixin in any source file under src/"
  artifacts:
    - path: "src/tscollection/models/config.py"
      provides: "Type-clean config dataclasses"
      min_lines: 40
    - path: "src/tscollection/models/_abstract/encoding_functionality_mixin.py"
      provides: "Type-clean 3-class mixin hierarchy"
      min_lines: 200
  key_links:
    - from: "ty check"
      to: "All modified files"
      via: "Static type analysis across config.py, mixin, 3 models"
      pattern: "ty check src/"
---

<objective>
Run full type checking and test suite across all modified files, fix any issues.
Purpose: Verify the entire phase is coherent. Type check all new code (config dataclasses, mixin hierarchy, model inheritance, from_config) with ty. Run full test suite (config, mixin, from_config). Addresses CFG-03 (type checking) and MIX-04 (behavioral preservation).
Output: All checks passing, any issues fixed.
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsmodels/.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

# All files modified by prior plans
@src/tscollection/models/config.py
@src/tscollection/models/_abstract/encoding_functionality_mixin.py
@src/tscollection/models/ts2vec/model.py
@src/tscollection/models/cost/model.py
@src/tscollection/models/autotcl/model.py
</context>

<tasks>

<task type="auto">
  <name>Type check, lint, and full test verification</name>
  <files>src/tscollection/models/config.py, src/tscollection/models/_abstract/encoding_functionality_mixin.py, src/tscollection/models/ts2vec/model.py, src/tscollection/models/cost/model.py, src/tscollection/models/autotcl/model.py</files>

  <read_first>
  - src/tscollection/models/config.py (output of plan 01)
  - src/tscollection/models/_abstract/encoding_functionality_mixin.py (output of plan 02)
  - src/tscollection/models/ts2vec/model.py (output of plan 03)
  - src/tscollection/models/cost/model.py (output of plan 03)
  - src/tscollection/models/autotcl/model.py (output of plan 03)
  </read_first>

  <action>
  Run verification commands and fix any issues:

  1. Run `uv run ty check src/` -- if errors, read the failing file and fix type annotations, imports, or signatures. Common issues: missing type hints on from_config return type, incorrect MaskMode import path, unannotated variables.
  2. Run `uv run ruff check src/` -- if errors, fix lint issues. Common issues: unused imports (old EncodingFunctionalityMixin import if not fully removed), import sort order, missing docstrings on from_config.
  3. Run `uv run pytest tests/ -x -q` -- if failures, read the error output and fix the failing code. Do NOT modify tests to pass; fix production code.
  4. Run grep to confirm no stale references: `grep -r 'EncodingFunctionalityMixin' src/` should return 0 matches.
  </action>

  <verify>
    <automated>uv run ty check src/ && uv run ruff check src/ && uv run pytest tests/ -x -q</automated>
  </verify>

  <acceptance_criteria>
  - `uv run ty check src/` exits 0 with no errors
  - `uv run ruff check src/` exits 0 with no lint errors
  - `uv run pytest tests/ -x -q` passes all tests (test_config.py + test_mixin.py + test_from_config.py)
  - `grep -r 'EncodingFunctionalityMixin' src/` returns 0 matches (no stale references)
  - No new noqa suppressions added beyond what was necessary to fix legitimate issues
  </acceptance_criteria>

  <done>All type checks, lint, and tests pass across the entire modified surface</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Verification-only plan. No code changes beyond fixing type/lint issues. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-SC | Tampering | pip/uv installs | mitigate | No new packages; verification only |

</threat_model>

<verification>
- ty check src/ exits 0
- ruff check src/ exits 0
- pytest tests/ -x -q passes
</verification>

<success_criteria>
- Full type check passes on src/ with zero errors (CFG-03)
- Full test suite passes (config + mixin + from_config tests)
- No stale references to old mixin name (MIX-04)
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-04-SUMMARY.md` when done
</output>
