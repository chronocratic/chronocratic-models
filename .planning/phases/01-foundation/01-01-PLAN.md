---
phase: 01-foundation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/tscollection/models/config.py
  - tests/test_config.py
  - tests/conftest.py
autonomous: true
requirements: [CFG-01, CFG-03]
user_setup: []

must_haves:
  truths:
    - "TS2VecModelParameters instantiates with only input_dims required; all other fields have defaults"
    - "CoSTModelParameters instantiates with input_dims and sequence_length required; all other fields have defaults"
    - "AutoTCLModelParameters instantiates with only input_dims required; all other fields have defaults"
    - "TS2VecModelParameters and AutoTCLModelParameters inherit from DilatedCNNModelParameters"
    - "CoSTModelParameters inherits directly from ModelParameters"
    - "All dataclass fields are explicitly typed for IDE autocompletion and type checking"
    - "vars() on any config dataclass produces a dict matching the model __init__ parameter names"
  artifacts:
    - path: "src/tscollection/models/config.py"
      provides: "ModelParameters, DilatedCNNModelParameters, TS2VecModelParameters, CoSTModelParameters, AutoTCLModelParameters"
      contains: "class ModelParameters"
    - path: "tests/test_config.py"
      provides: "Unit tests for config dataclass instantiation, inheritance, field defaults, vars() unpacking"
      min_lines: 50
    - path: "tests/conftest.py"
      provides: "Shared pytest fixtures (minimal for config tests)"
      min_lines: 10
  key_links:
    - from: "src/tscollection/models/config.py"
      to: "src/tscollection/models/encoders/masking.py"
      via: "MaskMode enum import"
      pattern: "from tscollection.models.encoders.masking import MaskMode"
    - from: "src/tscollection/models/config.py"
      to: "src/tscollection/models/ts2vec/model.py"
      via: "Field names must match TS2Vec __init__ params"
      pattern: "input_dims, hidden_dims, output_dims, depth, dropout_rate, conv_kernel_size, mask_mode, learning_rate, max_train_length, temporal_unit, sync_dist"
    - from: "src/tscollection/models/config.py"
      to: "src/tscollection/models/cost/model.py"
      via: "Field names must match CoST __init__ params"
      pattern: "input_dims, sequence_length, kernel_sizes, max_train_length, hidden_dims, output_dims, depth, dropout_rate, mask_mode, learning_rate, seasonal_loss_weight, queue_size, momentum, temperature, sync_dist"
    - from: "src/tscollection/models/config.py"
      to: "src/tscollection/models/autotcl/model.py"
      via: "Field names must match AutoTCL __init__ params"
      pattern: "input_dims, kernel_sizes, hidden_dims, output_dims, depth, dropout_rate, conv_kernel_size, mask_mode, learning_rate, max_train_length, sync_dist"
---

<objective>
Create typed config dataclasses for TS2Vec, CoST, and AutoTCL model parameters.
Purpose: Replace untyped dict-based model configuration with dataclasses that provide IDE autocompletion, static type checking, and a clear parameter surface for each model. This implements CFG-01 and CFG-03 per D-01, D-02, D-03, D-07.
Output: src/tscollection/models/config.py with 5 dataclass definitions, tests/test_config.py, tests/conftest.py.
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsmodels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/tsmodels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# User decisions (locked)
@.planning/phases/01-foundation/01-CONTEXT.md

# Research and patterns
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md

# Rbspaper reference (adapt, do not copy verbatim)
@_sources/rbspaper/src/rbspaper/models/config.py

# Model __init__ signatures (field derivation source)
@src/tscollection/models/ts2vec/model.py
@src/tscollection/models/cost/model.py
@src/tscollection/models/autotcl/model.py

# Conventions for module structure and import order
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="tdd">
  <name>Config dataclass hierarchy with tests</name>
  <files>src/tscollection/models/config.py, tests/test_config.py, tests/conftest.py</files>

  <read_first>
  - src/tscollection/models/ts2vec/model.py (lines 20-36: TS2Vec __init__ signature)
  - src/tscollection/models/cost/model.py (lines 25-44: CoST __init__ signature)
  - src/tscollection/models/autotcl/model.py (lines 38-54: AutoTCL __init__ signature)
  - src/tscollection/models/encoders/masking.py (MaskMode enum definition)
  - src/tscollection/models/encoders/masking.py (module structure pattern: __all__, import order)
  - _sources/rbspaper/src/rbspaper/models/config.py (reference hierarchy shape, adapted per D-02)
  - .planning/phases/01-foundation/01-CONTEXT.md (D-01 through D-07 decisions)
  - .planning/phases/01-foundation/01-PATTERNS.md (config.py pattern section)
  </read_first>

  <behavior>
  - Test: ModelParameters is an ABC with no fields; cannot be instantiated directly
  - Test: DilatedCNNModelParameters requires input_dims; hidden_dims=64, output_dims=320, depth=10, dropout_rate=0.1, conv_kernel_size=3
  - Test: TS2VecModelParameters requires input_dims; all other fields have defaults matching TS2Vec __init__; inherits from DilatedCNNModelParameters
  - Test: CoSTModelParameters requires input_dims and sequence_length; all other fields have defaults matching CoST __init__; inherits from ModelParameters directly (NOT DilatedCNNModelParameters)
  - Test: AutoTCLModelParameters requires input_dims; all other fields have defaults matching AutoTCL __init__; inherits from DilatedCNNModelParameters
  - Test: vars(TS2VecModelParameters(input_dims=1)) produces dict with keys: input_dims, hidden_dims, output_dims, depth, dropout_rate, conv_kernel_size, mask_mode, learning_rate, max_train_length, temporal_unit, sync_dist
  - Test: vars(CoSTModelParameters(input_dims=1, sequence_length=100)) produces dict with all CoST __init__ keys (minus augmentation fields)
  - Test: vars(AutoTCLModelParameters(input_dims=1)) produces dict with all AutoTCL __init__ keys (minus augmentation fields)
  - Test: TS2VecModelParameters.mask_mode defaults to MaskMode.BINOMIAL
  - Test: CoSTModelParameters.kernel_sizes defaults to empty list (field(default_factory=list))
  - Test: AutoTCLModelParameters.kernel_sizes defaults to empty list (field(default_factory=list))
  - Test: issubclass(DilatedCNNModelParameters, ModelParameters) is True
  - Test: issubclass(TS2VecModelParameters, DilatedCNNModelParameters) is True
  - Test: issubclass(AutoTCLModelParameters, DilatedCNNModelParameters) is True
  - Test: issubclass(CoSTModelParameters, ModelParameters) is True
  - Test: CoSTModelParameters is NOT a subclass of DilatedCNNModelParameters
  </behavior>

  <action>
  Create tests directory infrastructure (tests/conftest.py) then implement config dataclasses using TDD cycle.

  **RED**: Create tests/test_config.py with all behavior tests from the block above. Tests should import from tscollection.models.config. Run uv run pytest tests/test_config.py -x (expect failure -- module does not exist).

  **GREEN**: Create src/tscollection/models/config.py following the hierarchy per D-03: ModelParameters (ABC, no fields) -> DilatedCNNModelParameters -> TS2VecModelParameters, AutoTCLModelParameters. CoSTModelParameters inherits directly from ModelParameters per D-03. All fields derived from model __init__ signatures. Augmentation fields (augmentation_mode, augmentation_method_params, augmentation_mode_params) EXCLUDED per D-01. Runner artifacts (model_name, set_input_dims, set_sequence_length) EXCLUDED per D-02. Use @dataclass decorator. Import MaskMode from tscollection.models.encoders.masking. Set __all__ at top of file with alphabetically sorted exports. Use field(default_factory=list) for kernel_sizes in both CoSTModelParameters and AutoTCLModelParameters. Follow CONVENTIONS.md for import order (__all__ first, stdlib, third-party, local) and error handling patterns.

  Key defaults from model __init__:
  - TS2VecModelParameters: mask_mode=MaskMode.BINOMIAL, learning_rate=1e-3, max_train_length=None, temporal_unit=0, sync_dist=False
  - CoSTModelParameters: kernel_sizes=field(default_factory=list), max_train_length=201, hidden_dims=64, output_dims=320, depth=10, dropout_rate=0.1, mask_mode=MaskMode.BINOMIAL, learning_rate=1e-3, seasonal_loss_weight=0.1, queue_size=65536, momentum=0.999, temperature=0.07, sync_dist=False
  - AutoTCLModelParameters: kernel_sizes=field(default_factory=list), mask_mode=MaskMode.BINOMIAL, learning_rate=1e-3, max_train_length=None, sync_dist=False

  **REFACTOR**: Ensure docstrings are Google-style. Verify all type hints are explicit (int, float, bool, MaskMode, list[int], int | None). Run uv run ty check src/tscollection/models/config.py to confirm static typing passes.
  </action>

  <verify>
    <automated>uv run pytest tests/test_config.py -x -q</automated>
  </verify>

  <acceptance_criteria>
  - pytest tests/test_config.py passes with all 16+ behavior tests green
  - uv run ty check src/tscollection/models/config.py exits 0 (no type errors)
  - src/tscollection/models/config.py declares __all__ with all 5 class names sorted alphabetically
  - ModelParameters is abc.ABC (cannot be instantiated)
  - DilatedCNNModelParameters has 6 fields: input_dims (required), hidden_dims, output_dims, depth, dropout_rate, conv_kernel_size (all with defaults)
  - TS2VecModelParameters adds 5 fields: mask_mode, learning_rate, max_train_length, temporal_unit, sync_dist
  - CoSTModelParameters adds 15 fields (inherits from ModelParameters, NOT DilatedCNNModelParameters): input_dims (required), sequence_length (required), kernel_sizes, max_train_length, hidden_dims, output_dims, depth, dropout_rate, mask_mode, learning_rate, seasonal_loss_weight, queue_size, momentum, temperature, sync_dist
  - AutoTCLModelParameters adds 5 fields: kernel_sizes, mask_mode, learning_rate, max_train_length, sync_dist
  - No augmentation fields present in any dataclass (D-01)
  - No runner artifacts (model_name, set_input_dims, set_sequence_length) present (D-02)
  - CoSTModelParameters.kernel_sizes and AutoTCLModelParameters.kernel_sizes use field(default_factory=list)
  - CoSTModelParameters.max_train_length defaults to 201 (matches CoST __init__)
  - CoSTModelParameters.queue_size defaults to 65536 (matches CoST __init__)
  - CoSTModelParameters.momentum defaults to 0.999 (matches CoST __init__)
  - CoSTModelParameters.temperature defaults to 0.07 (matches CoST __init__)
  </acceptance_criteria>

  <done>All config tests pass, ty check clean, 5 dataclasses defined with correct inheritance and defaults</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan introduces pure Python dataclasses with no external I/O. No new trust boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-SC | Tampering | pip/uv installs | mitigate | No new packages; stdlib dataclasses + abc only |

</threat_model>

<verification>
- uv run pytest tests/test_config.py -x -q passes all tests
- uv run ty check src/tscollection/models/config.py exits 0
- grep confirms no augmentation fields in config.py: `grep -c 'augmentation' src/tscollection/models/config.py` returns 0
</verification>

<success_criteria>
- 5 dataclasses defined: ModelParameters, DilatedCNNModelParameters, TS2VecModelParameters, CoSTModelParameters, AutoTCLModelParameters
- All tests pass (CFG-01: instantiation, inheritance, field defaults, vars() unpacking)
- Static type checking passes (CFG-03: ty check clean)
- No runner artifacts, no augmentation fields (D-01, D-02 honored)
- CoSTModelParameters inherits from ModelParameters directly, not DilatedCNNModelParameters (D-03 honored)
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done
</output>
