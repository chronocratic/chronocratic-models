---
phase: 01-augmentation-producer-contract
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tscollection/models/augmentation/primitives.py
  - tests/test_aug_primitives.py
autonomous: true
requirements: [G1]
user_setup: []

must_haves:
  truths:
    - "Jitter adds Gaussian noise, returns same shape Tensor"
    - "Scaling multiplies by per-channel factor, returns same shape Tensor"
    - "Permutation permutes time segments, returns same shape Tensor"
    - "ComposeAugmentation chains primitives sequentially"
    - "All primitives satisfy Augmentation Protocol structurally"
    - "Parameter dataclasses have correct defaults matching original tstcc/augmentations.py"
    - "ty check passes on primitives.py with zero errors"
  artifacts:
    - path: "src/tscollection/models/augmentation/primitives.py"
      provides: "Shared primitives: Jitter, Scaling, Permutation, ComposeAugmentation + parameter dataclasses"
      contains: "class Jitter"
    - path: "tests/test_aug_primitives.py"
      provides: "TDD tests for each primitive and parameter dataclass"
      min_lines: 100
  key_links:
    - from: "src/tscollection/models/augmentation/primitives.py"
      to: "src/tscollection/models/augmentation/base.py"
      via: "imports Augmentation Protocol from base.py (not primitives.py)"
      pattern: "from.*base import.*Augmentation"
    - from: "src/tscollection/models/augmentation/primitives.py"
      to: "tests/test_aug_primitives.py"
      via: "All primitives importable from primitives.py"
      pattern: "from tscollection.models.augmentation.primitives import"

---

<objective>
Create `augmentation/primitives.py` with shared, model-agnostic primitives: `Jitter`, `Scaling`, `Permutation`, `ComposeAugmentation` extracted from `tstcc/augmentations.py` (D-06). Reshape from `AugmentationMethod.augment() -> TrainingViews` to `Augmentation.__call__() -> Tensor`. Each class implements the `Augmentation` Protocol via `__call__`. This file imports nothing model-specific (SPEC §4.9).
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsmodels/.planning/phases/01-augmentation-producer-contract/SPEC.md
@/Users/skaf/VSCodeProjects/tsmodels/.planning/codebase/CONVENTIONS.md
@/Users/skaf/VSCodeProjects/tsmodels/.planning/codebase/TESTING.md
</execution_context>

<context>
@src/tscollection/models/augmentation/base.py
@src/tscollection/models/convolutional/standard/tstcc/augmentations.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for primitives, then implement them (per D-06)</name>
  <files>
    tests/test_aug_primitives.py
    src/tscollection/models/augmentation/primitives.py
  </files>
  <behavior>
    - Test: Jitter default params produce same-shape output, noise added
    - Test: Jitter with p=0 returns input unchanged
    - Test: Scaling default params produce same-shape output
    - Test: Scaling with per_sample=True draws independent factor per batch element
    - Test: Permutation produces same-shape output, segments reordered
    - Test: ComposeAugmentation chains two primitives (Jitter + Scaling)
    - Test: All primitives are callable (have __call__)
    - Test: Parameter dataclasses have correct defaults (JitterParameters.sigma=0.1, ScalingParameters.sigma=0.1, PermutationParameters.max_segments=5)
  </behavior>
  <action>
    **Step 1 - Create test file (RED):** Write `tests/test_aug_primitives.py` with all test behaviors above. Tests will fail because primitives.py does not exist yet.

    **Step 2 - Create primitives.py (GREEN):** Create `src/tscollection/models/augmentation/primitives.py`.

    Extract from `tstcc/augmentations.py` (lines 53-211) and reshape:

    1. **Helper functions:** Copy `_normalize_dim()` and `_should_apply()` from tstcc/augmentations.py.

    2. **Parameter dataclasses:** Copy `JitterParameters`, `ScalingParameters`, `PermutationParameters` exactly. These are frozen dataclasses with default values.

    3. **Jitter class:** Convert from `AugmentationMethod` subclass to `Augmentation` Protocol implementer. Key change: `augment(data) -> TrainingViews` becomes `__call__(self, x: torch.Tensor) -> torch.Tensor`. The `__call__` method does the noise addition (currently lines 72-81 in tstcc). Return bare tensor, NOT TrainingViews.

    4. **Scaling class:** Same conversion. `__call__` does the scaling (currently lines 117-131 in tstcc). Return bare tensor.

    5. **Permutation class:** Same conversion. `__call__` does the segment permutation (currently lines 161-184 in tstcc). Return bare tensor.

    6. **ComposeAugmentation class:** Same conversion. Stores `augmentations: list[Augmentation]` (NOT `list[AugmentationMethod]`). `__call__` chains: `current = aug(current)` for each in the list (currently lines 202-211 in tstcc). Return bare tensor.

    **Import pattern:** `from tscollection.models.augmentation.base import Augmentation` — imports from sibling module (the Augmentation Protocol is defined in base.py, NOT in primitives.py). Do NOT import TrainingViews or AugmentationMethod.

    **__all__:** Export all classes and parameter dataclasses.

    **Key difference from original:** No `TrainingViews` wrapping. The old code returned `TrainingViews(views=(result,), metadata={})`. New code returns `result` directly. This is the primitive-to-producer separation.
  </action>
  <verify>
    <automated>uv run pytest tests/test_aug_primitives.py -v --tb=short</automated>
  </verify>
  <done>
    - test_aug_primitives.py has 8+ passing tests
    - primitives.py imports only from `augmentation/base.py` and stdlib/torch
    - All primitives implement `__call__(x: Tensor) -> Tensor`
    - Parameter dataclasses have correct defaults matching original
    - `ty check src/tscollection/models/augmentation/primitives.py` passes
  </done>
</task>

</tasks>

<verification>
`uv run pytest tests/test_aug_primitives.py -v` passes. `ty check src/tscollection/models/augmentation/primitives.py` passes with zero errors.
</verification>

<success_criteria>
1. `src/tscollection/models/augmentation/primitives.py` exists with Jitter, Scaling, Permutation, ComposeAugmentation
2. All primitives satisfy `Augmentation` Protocol (callable, Tensor -> Tensor)
3. Parameter dataclasses have correct defaults
4. No model-specific imports in primitives.py
5. All tests pass, type check passes
</success_criteria>

<output>
Create `.planning/phases/01-augmentation-producer-contract/01-02-SUMMARY.md` when done
</output>
