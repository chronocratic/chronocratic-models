---
phase: 01-augmentation-producer-contract
plan: 10
type: execute
wave: 6
depends_on: [01-09]
files_modified:
  - tests/test_augmentation.py
  - tests/test_augmentation_base.py
  - tests/test_augmentation_per_model.py
  - tests/test_aug_config.py
  - tests/test_from_config.py
  - tests/test_smoke.py
autonomous: true
requirements: [G1, G6]
user_setup: []

must_haves:
  truths:
    - "All existing test files import new symbols and pass"
    - "test_smoke.py uses new producer types for model training"
    - "test_augmentation.py tests both old and new types (D-05 backward compat)"
    - "Old symbol tests still pass (D-05: backward compat)"
  artifacts:
    - path: "tests/test_smoke.py"
      provides: "Updated smoke tests using producer-based augmentations"
      min_lines: 60
    - path: "tests/test_augmentation.py"
      provides: "Updated augmentation tests with new + old types"
      min_lines: 80
  key_links:
    - from: "tests/test_smoke.py"
      to: "src/tscollection/models/augmentation/__init__.py"
      via: "imports new symbols from barrel"
      pattern: "from tscollection.models.augmentation import"
    - from: "tests/test_augmentation.py"
      to: "src/tscollection/models/augmentation/base.py"
      via: "imports new contract types"
      pattern: "from tscollection.models.augmentation.base import"

---

<objective>
Migrate existing test files to use new types while keeping old-symbol tests passing (D-05). Update `test_smoke.py` to use producer-based augmentations for all 4 models. Update augmentation-related tests to cover both old and new contract types. This is split from the barrel update (plan 09) to keep scope manageable — 2 tasks, 6 test files.
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsmodels/.planning/phases/01-augmentation-producer-contract/SPEC.md
@/Users/skaf/VSCodeProjects/tsmodels/.planning/codebase/CONVENTIONS.md
@/Users/skaf/VSCodeProjects/tsmodels/.planning/codebase/TESTING.md
</execution_context>

<context>
@src/tscollection/models/augmentation/__init__.py
@tests/test_smoke.py
@tests/test_augmentation.py
@tests/test_augmentation_base.py
@tests/test_augmentation_per_model.py
@tests/test_aug_config.py
@tests/test_from_config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update test_smoke.py to use producer-based augmentations (per D-03 Phase C prep)</name>
  <files>
    tests/test_smoke.py
  </files>
  <action>
    Update smoke tests to use new producer types:
    - `TS2Vec` tests: use `CropShiftProducer()` instead of `CropShiftAugmentation()`
    - `CoST` tests: use `IndependentPair(CosTRandomFunctionAugmentation())` instead of raw augment
    - `AutoTCL` tests: use `AutoTCLNeuralNetworkAugmentation()` (now inherits TrainableAugmentationProducer)
    - `TSTCC` tests: use `_default_tstcc_pair()` instead of `TSTCCDualAugmentation`
    - Extensibility test: use new `AugmentationProducer` + `SingleView` / `ViewPair` types
    - All models should train with finite loss using new contract
  </action>
  <verify>
    <automated>uv run pytest tests/test_smoke.py -v --tb=short</automated>
  </verify>
  <done>
    - test_smoke.py trains all 4 models with producer-based augmentations
    - All smoke tests pass with finite loss
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate remaining test files (augmentation, aug_base, aug_per_model, aug_config, from_config)</name>
  <files>
    tests/test_augmentation.py
    tests/test_augmentation_base.py
    tests/test_augmentation_per_model.py
    tests/test_aug_config.py
    tests/test_from_config.py
  </files>
  <action>
    Update each test file:

    **test_augmentation.py:**
    - `TestTrainingViews` -> keep for backward compat (old symbol still exists)
    - `TestAugmentationMethod` -> keep (old symbol)
    - Add new tests for `Augmentation`, `AugmentationProducer`, ViewSet types
    - `TestCropShiftAugmentation` -> add `CropShiftProducer` tests

    **test_augmentation_base.py:** Add tests for new contract types (SingleView, ViewPair, AlignedPair, TrainableAugmentationProducer)

    **test_augmentation_per_model.py:** Update imports to use new types

    **test_aug_config.py:** Add tests for primitive parameter dataclasses

    **test_from_config.py:** Update to use new producer types

    **Important:** Existing old-symbol tests MUST still pass (D-05: backward compat). New tests use new types. Both coexist until final delete.
  </action>
  <verify>
    <automated>uv run pytest tests/test_augmentation.py tests/test_augmentation_base.py tests/test_augmentation_per_model.py tests/test_aug_config.py tests/test_from_config.py -v --tb=short</automated>
  </verify>
  <done>
    - All 5 test files pass with both old and new types
    - Old symbol tests still work (D-05 backward compat)
    - New contract tests added
  </done>
</task>

</tasks>

<verification>
`uv run pytest tests/test_augmentation.py tests/test_augmentation_base.py tests/test_augmentation_per_model.py tests/test_aug_config.py tests/test_from_config.py tests/test_smoke.py -v` passes.
</verification>

<success_criteria>
1. `test_smoke.py` trains all models with producer-based augmentations
2. All 6 test files pass with both old and new types
3. Old-symbol tests still pass (D-05 backward compat)
4. No test file references removed symbols
</success_criteria>

<output>
Create `.planning/phases/01-augmentation-producer-contract/01-10-SUMMARY.md` when done
</output>
