---
phase: 01-augmentation-producer-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tscollection/models/augmentation/base.py
  - tests/test_aug_contract.py
autonomous: true
requirements: [G2, G6]
user_setup: []

must_haves:
  truths:
    - "Augmentation Protocol is structural (__call__: Tensor -> Tensor), not runtime-checkable"
    - "AugmentationProducer[V] is covariant in V, structural, not runtime-checkable"
    - "SingleView has a frozen 'view' field of type torch.Tensor"
    - "ViewPair has frozen 'first' and 'second' fields of type torch.Tensor"
    - "AlignedPair extends ViewPair with frozen 'overlap_length' field (int)"
    - "TrainableAugmentationProducer is a nominal ABC + nn.Module (not Protocol)"
    - "AugmentationTrainingStrategy is retained unchanged from existing base.py"
    - "ty check src/tscollection/models/augmentation/base.py passes with zero errors"
  artifacts:
    - path: "src/tscollection/models/augmentation/base.py"
      provides: "New contract types: Augmentation, AugmentationProducer, SingleView, ViewPair, AlignedPair, TrainableAugmentationProducer, AugmentationTrainingStrategy"
      contains: "class AugmentationProducer"
    - path: "tests/test_aug_contract.py"
      provides: "TDD tests for new contract types"
      min_lines: 80
  key_links:
    - from: "src/tscollection/models/augmentation/base.py"
      to: "torch.nn.Module"
      via: "TrainableAugmentationProducer inherits nn.Module"
      pattern: "TrainableAugmentationProducer.*nn\\.Module"
    - from: "src/tscollection/models/augmentation/base.py"
      to: "tests/test_aug_contract.py"
      via: "All new symbols importable from base.py"
      pattern: "from tscollection.models.augmentation.base import"

---

<objective>
Create the new augmentation contract types in `base.py`: `Augmentation` Protocol, typed ViewSet dataclasses (`SingleView`, `ViewPair`, `AlignedPair`), `AugmentationProducer[V]` covariant Protocol, `TrainableAugmentationProducer` nominal ABC, and retain `AugmentationTrainingStrategy` unchanged. Keep existing `TrainingViews`, `AugmentationMethod`, `TrainableAugmentation` alongside for backward compat (D-05).
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsmodels/.planning/PROJECT.md
@/Users/skaf/VSCodeProjects/tsmodels/.planning/ROADMAP.md
@/Users/skaf/VSCodeProjects/tsmodels/.planning/phases/01-augmentation-producer-contract/SPEC.md
@/Users/skaf/VSCodeProjects/tsmodels/.planning/phases/01-augmentation-producer-contract/01-CONTEXT.md
@/Users/skaf/VSCodeProjects/tsmodels/.planning/codebase/CONVENTIONS.md
@/Users/skaf/VSCodeProjects/tsmodels/.planning/codebase/TESTING.md
</execution_context>

<context>
@src/tscollection/models/augmentation/base.py
@src/tscollection/models/supervised/supervised.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for new contract types, then implement them in base.py (per D-01, D-02, D-07)</name>
  <files>
    tests/test_aug_contract.py
    src/tscollection/models/augmentation/base.py
  </files>
  <behavior>
    - Test: Augmentation Protocol is a Protocol with __call__(x: Tensor) -> Tensor
    - Test: SingleView is a frozen dataclass with `view: torch.Tensor` field
    - Test: ViewPair is a frozen dataclass with `first`, `second: torch.Tensor` fields
    - Test: AlignedPair extends ViewPair with `overlap_length: int` field
    - Test: AugmentationProducer[V] is covariant Protocol with produce(x: Tensor) -> V
    - Test: TrainableAugmentationProducer is a subclass of nn.Module (nominal ABC)
    - Test: TrainableAugmentationProducer has abstract produce() -> SingleView
    - Test: TrainableAugmentationProducer has abstract train_step()
    - Test: TrainableAugmentationProducer.configure_optimizer() returns AdamW
    - Test: TrainableAugmentationProducer.should_train_augmentation() delegates to strategy
    - Test: AugmentationTrainingStrategy is retained unchanged (still ABC, has compute_loss, should_train)
    - Test: AlignedPair is-a ViewPair (issubclass check)
    - Test: Existing TrainingViews, AugmentationMethod, TrainableAugmentation still importable
  </behavior>
  <action>
    **Step 1 - Create test file (RED):** Write `tests/test_aug_contract.py` with all 13 test behaviors above. The tests will fail because the new symbols do not exist yet.

    **Step 2 - Modify base.py (GREEN):** Add NEW symbols to the existing `base.py` (DO NOT delete existing ones — D-05 requires keeping old names until final delete). Append after existing code:

    1. `Augmentation` Protocol (SPEC §4.2): `@runtime_checkable` is NOT needed (structural, not runtime-checked). Signature: `def __call__(self, x: torch.Tensor) -> torch.Tensor: ...`

    2. `SingleView`, `ViewPair`, `AlignedPair` frozen dataclasses (SPEC §4.3):
       - `SingleView(view: torch.Tensor)`
       - `ViewPair(first: torch.Tensor, second: torch.Tensor)`
       - `AlignedPair` subclasses `ViewPair` — reuse `first`/`second` via `super().__init__()`, add `overlap_length: int`
       - Use `@dataclass(frozen=True)` for immutability

    3. `V = TypeVar("V", covariant=True)` then `AugmentationProducer(Protocol[V])` (SPEC §4.4):
       - `def produce(self, x: torch.Tensor) -> V: ...`
       - NOT `@runtime_checkable` — structural conformance only

    4. `TrainableAugmentationProducer(nn.Module, ABC)` (SPEC §4.5, D-02):
       - `__init__(self, training_strategy: AugmentationTrainingStrategy)` — stores `_training_strategy`, calls `super().__init__()`
       - `@abstractmethod produce(self, x: torch.Tensor) -> SingleView`
       - `@abstractmethod train_step(self, x: torch.Tensor, encoder: nn.Module, batch_idx: int) -> torch.Tensor | None`
       - `configure_optimizer(self, lr: float) -> AdamW` — returns `AdamW(self.parameters(), lr=lr)`
       - `should_train_augmentation(self, epoch: int, batch_idx: int) -> bool` — delegates to `self._training_strategy.should_train(epoch, batch_idx)`

    5. Retain `AugmentationTrainingStrategy` unchanged (D-07) — it already exists, do not modify.

    6. Retain `TrainingViews`, `AugmentationMethod`, `TrainableAugmentation` unchanged (D-05) — do not delete yet.

    7. Update `__all__` to include new symbols alongside existing ones.

    **Conventions:** Google-style docstrings, full type hints, keyword-only constructors (`*, input_dims: int`), `__all__` at top of file. Import `TypeVar` from `typing`, `Protocol` from `typing`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_aug_contract.py -v --tb=short</automated>
  </verify>
  <done>
    - test_aug_contract.py has 13+ passing tests
    - `ty check src/tscollection/models/augmentation/base.py` passes with zero errors
    - All existing symbols still importable from base.py
    - New symbols are importable: Augmentation, SingleView, ViewPair, AlignedPair, AugmentationProducer, TrainableAugmentationProducer
  </done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_aug_contract.py -v --tb=short` and `ty check src/tscollection/models/augmentation/base.py`. All tests pass, no type errors.
</verification>

<success_criteria>
1. `tests/test_aug_contract.py` exists with 13+ passing tests covering all new contract types
2. `base.py` exports all new symbols in `__all__` alongside retained old symbols
3. `AlignedPair` is-a `ViewPair` verified by `issubclass`
4. `TrainableAugmentationProducer` inherits `nn.Module` and is abstract
5. `ty check` passes with zero errors
</success_criteria>

<output>
Create `.planning/phases/01-augmentation-producer-contract/01-01-SUMMARY.md` when done
</output>
