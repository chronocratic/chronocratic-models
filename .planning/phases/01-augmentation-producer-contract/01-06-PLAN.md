---
phase: 01-augmentation-producer-contract
plan: 06
type: execute
wave: 3
depends_on: [01-01, 01-02, 01-03, 01-04, 01-05]
files_modified:
  - src/tscollection/models/convolutional/dilated/cost/augmentation.py
  - src/tscollection/models/convolutional/dilated/cost/model.py
  - tests/test_cost_producer.py
autonomous: true
requirements: [G3, G6]
user_setup: []

must_haves:
  truths:
    - "CosTRandomFunctionAugmentation satisfies Augmentation Protocol (__call__: Tensor -> Tensor)"
    - "CoST accepts AugmentationProducer[ViewPair] | None in constructor"
    - "CoST uses IndependentPair(CosTRandomFunctionAugmentation()) as default"
    - "CoST training/validation use .produce().first and .produce().second (not two .augment() calls)"
    - "CoST trains 5 steps with finite loss"
    - "CoST numerical output matches old CoST when seeded (SC-7)"
  artifacts:
    - path: "src/tscollection/models/convolutional/dilated/cost/augmentation.py"
      provides: "CosTRandomFunctionAugmentation reshaped to Augmentation Protocol"
      contains: "__call__"
    - path: "tests/test_cost_producer.py"
      provides: "TDD tests for CoST producer integration"
      min_lines: 50
  key_links:
    - from: "src/tscollection/models/convolutional/dilated/cost/model.py"
      to: "src/tscollection/models/augmentation/base.py"
      via: "imports AugmentationProducer, ViewPair"
      pattern: "from.*base import.*AugmentationProducer"

---

<objective>
Wire CoST to the new producer contract. Reshape `CosTRandomFunctionAugmentation` from `AugmentationMethod.augment() -> TrainingViews` to `Augmentation.__call__() -> Tensor` (SPEC §5, D-06: stays model-specific in `cost/augmentation.py`). Update `CoST` to accept `AugmentationProducer[ViewPair]` with `IndependentPair` as default, replacing the current pattern of two inline `.augment(x).views[0]` calls. Second model in the sequential D-03 Phase B chain (TS2Vec -> CoST).
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsmodels/.planning/phases/01-augmentation-producer-contract/SPEC.md
@/Users/skaf/VSCodeProjects/tsmodels/.planning/codebase/CONVENTIONS.md
@/Users/skaf/VSCodeProjects/tsmodels/.planning/codebase/TESTING.md
</execution_context>

<context>
@src/tscollection/models/augmentation/base.py
@src/tscollection/models/augmentation/producers.py
@src/tscollection/models/convolutional/dilated/cost/augmentation.py
@src/tscollection/models/convolutional/dilated/cost/model.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reshape CosTRandomFunctionAugmentation to Augmentation Protocol and wire CoST model (per D-03 Phase B, second in sequence)</name>
  <files>
    tests/test_cost_producer.py
    src/tscollection/models/convolutional/dilated/cost/augmentation.py
    src/tscollection/models/convolutional/dilated/cost/model.py
  </files>
  <behavior>
    - Test: CosTRandomFunctionAugmentation is callable (has __call__ -> Tensor)
    - Test: CosTRandomFunctionAugmentation with sigma=0.1 produces same-shape output
    - Test: CoST constructor accepts IndependentPair(CosTRandomFunctionAugmentation())
    - Test: CoST training uses .produce().first / .produce().second
    - Test: CoST trains 5 steps with finite loss
    - Test: Seeded CoST produces identical loss sequence to unseeded run (numerical equivalence, SC-7)
  </behavior>
  <action>
    **Step 1 - Create test file (RED):** Write `tests/test_cost_producer.py` with all behaviors above. Include a seeded comparison test: create two CoST models with same seed, run training steps, verify loss sequences are identical (SC-7).

    **Step 2 - Reshape cost/augmentation.py (GREEN):**

    `CosTRandomFunctionAugmentation` stays in `cost/augmentation.py` (D-06: model-specific). Reshape from `AugmentationMethod` subclass to `Augmentation` Protocol implementer.

    Key changes:
    - Remove `class CosTRandomFunctionAugmentation(AugmentationMethod)` inheritance
    - Rename `augment(data, **kwargs) -> TrainingViews` to `__call__(self, x: torch.Tensor) -> torch.Tensor`
    - The three internal methods (_jitter, _scale, _shift) stay the same
    - Return bare tensor (NOT TrainingViews): `return self._jitter(self._shift(self._scale(x)))`
    - Keep `CosTRandomFunctionAugmentationParameters` as-is
    - Keep backward-compat dict params constructor

    Import `Augmentation` from `augmentation/base.py` (for type annotation in `__all__` doc).

    **Step 3 - Wire CoST model (GREEN):**

    In `src/tscollection/models/convolutional/dilated/cost/model.py`:
    - Change import: `from tscollection.models.augmentation.base import AugmentationProducer, ViewPair`
    - Change constructor param: `augmentation: AugmentationProducer[ViewPair] | None = None`
    - Change default (lazy import):
      ```python
      from tscollection.models.augmentation.producers import IndependentPair
      from tscollection.models.convolutional.dilated.cost.augmentation import CosTRandomFunctionAugmentation
      self._augmentation: AugmentationProducer[ViewPair] = IndependentPair(aug=CosTRandomFunctionAugmentation())
      ```
    - Change `training_step`:
      ```python
      pair = self._augmentation.produce(x)
      query, key = pair.first, pair.second
      train_loss = self._compute_total_loss(query, key, update_key_encoder=True)
      ```
    - Change `validation_step` similarly

    **Old code:**
    ```python
    query = self._augmentation.augment(x).views[0]
    key = self._augmentation.augment(x).views[0]
    ```
    **New code:** Single `.produce()` call returning ViewPair with both views.

    **DO NOT delete old AugmentationMethod import yet** — D-05: keep until final delete commit. Old augmentation file still has backward-compat alias.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cost_producer.py -v --tb=short</automated>
  </verify>
  <done>
    - test_cost_producer.py has 6+ passing tests (including seeded comparison)
    - CosTRandomFunctionAugmentation is callable (Augmentation Protocol)
    - CoST uses IndependentPair as default
    - CoST uses .produce().first / .produce().second (single call, not two .augment())
    - `ty check src/tscollection/models/convolutional/dilated/cost/model.py` passes
  </done>
</task>

</tasks>

<verification>
`uv run pytest tests/test_cost_producer.py -v` passes. `ty check src/tscollection/models/convolutional/dilated/cost/model.py src/tscollection/models/convolutional/dilated/cost/augmentation.py` passes with zero errors.
</verification>

<success_criteria>
1. `CosTRandomFunctionAugmentation` implements `Augmentation` Protocol (`__call__: Tensor -> Tensor`)
2. `CoST` accepts `AugmentationProducer[ViewPair] | None`
3. CoST uses `IndependentPair` as default, single `.produce()` call
4. No `.augment()` or `.views[0]` access in CoST model code
5. Seeded comparison test verifies numerical equivalence (SC-7)
6. All tests pass, type check passes
</success_criteria>

<output>
Create `.planning/phases/01-augmentation-producer-contract/01-06-SUMMARY.md` when done
</output>
