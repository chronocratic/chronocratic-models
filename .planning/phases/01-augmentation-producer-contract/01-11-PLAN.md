---
phase: 01-augmentation-producer-contract
plan: 11
type: execute
wave: 8
depends_on: [01-12]
files_modified:
  - src/tscollection/models/augmentation/base.py
  - src/tscollection/models/augmentation/dual.py
  - src/tscollection/models/augmentation/__init__.py
autonomous: true
requirements: [G1, G2, G3, G4, G5, G6]
user_setup: []

must_haves:
  truths:
    - "TrainingViews class is removed from base.py"
    - "AugmentationMethod ABC is removed from base.py"
    - "TrainableAugmentation ABC is removed from base.py"
    - "DualAugmentation is removed from dual.py (file deleted)"
    - "augmentation/__init__.py does not export TrainingViews, AugmentationMethod, TrainableAugmentation, DualAugmentation"
    - "New symbols (Augmentation, AugmentationProducer, ViewSets, etc.) remain in base.py"
    - "ty check src/tscollection/models/augmentation/ passes with zero errors"
  artifacts:
    - path: "src/tscollection/models/augmentation/base.py"
      provides: "Clean base.py without old ABCs"
      contains: "class AugmentationProducer"
    - path: "src/tscollection/models/augmentation/__init__.py"
      provides: "Clean barrel without old exports"
      contains: "AugmentationProducer"
  key_links:
    - from: "src/tscollection/models/augmentation/base.py"
      to: "tests/test_augmentation_base.py"
      via: "old tests removed, new tests remain"
      pattern: "TrainingViews"
    - from: "src/tscollection/models/augmentation/__init__.py"
      to: "src/tscollection/models/augmentation/base.py"
      via: "imports only new symbols"
      pattern: "from.*base import"

---

<objective>
Core deletion: Remove `TrainingViews`, `AugmentationMethod`, `TrainableAugmentation` from `base.py` and delete `augmentation/dual.py`. Clean `augmentation/__init__.py` of old exports. This plan handles only the core augmentation module — per-model cleanup (ts2vec, cost, autotcl, tstcc files + utils.py) is handled in plan 13. All 4 models must be fully wired to new contract first (D-03 Phase C).
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsmodels/.planning/phases/01-augmentation-producer-contract/SPEC.md
@/Users/skaf/VSCodeProjects/tsmodels/.planning/codebase/CONVENTIONS.md
@/Users/skaf/VSCodeProjects/tsmodels/.planning/codebase/TESTING.md
</execution_context>

<context>
@src/tscollection/models/augmentation/base.py
@src/tscollection/models/augmentation/dual.py
@src/tscollection/models/augmentation/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete old symbols from base.py and dual.py, clean barrel (per D-03 Phase C, SPEC §5)</name>
  <files>
    src/tscollection/models/augmentation/base.py
    src/tscollection/models/augmentation/dual.py
    src/tscollection/models/augmentation/__init__.py
  </files>
  <action>
    **Step 1 - Delete old symbols from base.py:**

    Remove these classes from `src/tscollection/models/augmentation/base.py`:
    - `TrainingViews` (dataclass)
    - `AugmentationMethod` (ABC)
    - `TrainableAugmentation` (ABC)

    Keep: `Augmentation` Protocol, `AugmentationProducer[V]` Protocol, `SingleView`, `ViewPair`, `AlignedPair`, `TrainableAugmentationProducer`, `AugmentationTrainingStrategy`, `TypeVar V`.

    Update `__all__`.

    **Step 2 - Delete dual.py:**

    Delete `src/tscollection/models/augmentation/dual.py` entirely. No file remains.

    **Step 3 - Clean augmentation/__init__.py:**

    Remove exports of: `TrainingViews`, `AugmentationMethod`, `TrainableAugmentation`, `DualAugmentation`. Keep all new exports (Augmentation, AugmentationProducer, ViewSets, producers, decorators, trainable_support).

    **Critical:** Do NOT touch per-model files here — plan 13 handles ts2vec, cost, autotcl, tstcc cleanup.
  </action>
  <verify>
    <automated>python -c "from tscollection.models.augmentation import AugmentationProducer, SingleView, ViewPair, AlignedPair, Seeded; print('new OK')" && ! grep -rn "TrainingViews\|AugmentationMethod\|TrainableAugmentation\|DualAugmentation" src/tscollection/models/augmentation/base.py src/tscollection/models/augmentation/__init__.py 2>/dev/null</automated>
  </verify>
  <done>
    - dual.py is deleted
    - TrainingViews, AugmentationMethod, TrainableAugmentation removed from base.py
    - augmentation/__init__.py does not export old deleted symbols
    - New symbols remain importable
    - `ty check src/tscollection/models/augmentation/` passes
  </done>
</task>

</tasks>

<verification>
`ty check src/tscollection/models/augmentation/` passes with zero errors. `ls src/tscollection/models/augmentation/dual.py` returns "No such file". Grep confirms no old symbols in base.py or __init__.py.
</verification>

<success_criteria>
1. `augmentation/dual.py` deleted
2. `TrainingViews`, `AugmentationMethod`, `TrainableAugmentation` removed from base.py
3. `augmentation/__init__.py` does not export old symbols
4. New symbols remain importable from barrel
5. Type check passes on augmentation/ module
</success_criteria>

<output>
Create `.planning/phases/01-augmentation-producer-contract/01-11-SUMMARY.md` when done
</output>
