---
phase: 01-augmentation-producer-contract
plan: 13
type: execute
wave: 7
depends_on: [01-10]
files_modified:
  - src/tscollection/models/convolutional/standard/tstcc/augmentations.py
  - src/tscollection/models/convolutional/dilated/ts2vec/augmentation.py
  - src/tscollection/models/convolutional/dilated/cost/augmentation.py
  - src/tscollection/models/convolutional/dilated/autotcl/augmentation/methods.py
  - src/tscollection/models/convolutional/dilated/autotcl/utils.py
  - src/tscollection/models/convolutional/dilated/autotcl/model.py
  - tests/test_augmentation.py
  - tests/test_augmentation_base.py
  - tests/test_augmentation_per_model.py
autonomous: true
requirements: [G1, G2, G3, G4, G5, G6]
user_setup: []

must_haves:
  truths:
    - "TSTCCDualAugmentation already removed in plan 08 (REVIEW MEDIUM fix)"
    - "DualAugmentation import removed from all files"
    - "CropShiftAugmentation alias removed from ts2vec/augmentation.py"
    - "No remaining references to TrainingViews, AugmentationMethod, TrainableAugmentation in per-model files"
    - "autotcl/utils.py no longer imports AugmentationMethod"
    - "autotcl/model.py no longer imports TrainableAugmentation"
    - "All tests pass after per-model cleanup"
    - "ty check src/ passes with zero errors"
  artifacts:
    - path: "src/tscollection/models/convolutional/standard/tstcc/augmentations.py"
      provides: "Clean tstcc augmentations without TSTCCDualAugmentation"
      contains: "def _default_tstcc_pair"
    - path: "src/tscollection/models/convolutional/dilated/autotcl/utils.py"
      provides: "Clean utils without AugmentationMethod import"
      contains: "def calculate_mutual_information"
  key_links:
    - from: "src/tscollection/models/convolutional/dilated/autotcl/utils.py"
      to: "src/tscollection/models/augmentation/base.py"
      via: "imports AugmentationProducer (not AugmentationMethod)"
      pattern: "from.*base import.*AugmentationProducer"
    - from: "src/tscollection/models/convolutional/standard/tstcc/augmentations.py"
      to: "src/tscollection/models/augmentation/primitives.py"
      via: "imports primitives (Jitter, Scaling, Permutation)"
      pattern: "from.*primitives import"

---

<objective>
Per-model cleanup: Remove old-symbol references (TrainingViews, AugmentationMethod, TrainableAugmentation, DualAugmentation, TSTCCDualAugmentation) from model-specific augmentation files, utils, and tests. This is the second half of D-03 Phase C (plan 11 handled core augmentation module deletion; plan 13 handles per-model files). After this plan, grep for any old symbol in src/ returns zero hits.
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsmodels/.planning/phases/01-augmentation-producer-contract/SPEC.md
@/Users/skaf/VSCodeProjects/tsmodels/.planning/codebase/CONVENTIONS.md
@/Users/skaf/VSCodeProjects/tsmodels/.planning/codebase/TESTING.md
</execution_context>

<context>
@src/tscollection/models/convolutional/standard/tstcc/augmentations.py
@src/tscollection/models/convolutional/dilated/ts2vec/augmentation.py
@src/tscollection/models/convolutional/dilated/cost/augmentation.py
@src/tscollection/models/convolutional/dilated/autotcl/augmentation/methods.py
@src/tscollection/models/convolutional/dilated/autotcl/utils.py
@src/tscollection/models/convolutional/dilated/autotcl/model.py
@tests/test_augmentation.py
@tests/test_augmentation_base.py
@tests/test_augmentation_per_model.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean per-model augmentation files and tests (D-03 Phase C)</name>
  <files>
    src/tscollection/models/convolutional/standard/tstcc/augmentations.py
    src/tscollection/models/convolutional/dilated/ts2vec/augmentation.py
    src/tscollection/models/convolutional/dilated/cost/augmentation.py
    src/tscollection/models/convolutional/dilated/autotcl/augmentation/methods.py
    src/tscollection/models/convolutional/dilated/autotcl/utils.py
    src/tscollection/models/convolutional/dilated/autotcl/model.py
    tests/test_augmentation.py
    tests/test_augmentation_base.py
    tests/test_augmentation_per_model.py
  </files>
  <action>
    **Step 1 - Clean tstcc/augmentations.py:**
    Remove `DualAugmentation` import from `augmentation/dual.py` (already deleted in plan 11). Remove `TrainingViews`, `AugmentationMethod` imports from `base.py`. TSTCCDualAugmentation already deleted in plan 08. File now imports only from `augmentation/primitives.py`, `augmentation/producers.py`, `augmentation/base.py` (for AugmentationProducer).

    **Step 2 - Clean ts2vec/augmentation.py:**
    Remove `CropShiftAugmentation` alias (backward-compat shim pointing to CropShiftProducer). Remove `TrainingViews`, `AugmentationMethod` imports.

    **Step 3 - Clean cost/augmentation.py:**
    Remove `TrainingViews`, `AugmentationMethod` imports. CosTRandomFunctionAugmentation is now a primitive (Augmentation Protocol) — imports from base.py should be `Augmentation`, `SingleView`, etc.

    **Step 4 - Clean autotcl/augmentation/methods.py:**
    Remove `TrainableAugmentation`, `TrainingViews` imports. Already uses `TrainableAugmentationProducer`, `SingleView` from plan 07.

    **Step 5 - Clean autotcl/utils.py:**
    Remove `AugmentationMethod` import. Update `calculate_mutual_information` to accept `AugmentationProducer[SingleView]` and call `.produce(x)` instead of `.augment(x)`, access `.view` instead of `.views[0]`.

    **Step 6 - Clean autotcl/model.py:**
    Remove `TrainableAugmentation` import. Already uses `TrainableAugmentationProducer` from plan 07. Verify no stale references.

    **Step 7 - Update tests:**
    Remove tests referencing deleted symbols (TrainingViews, AugmentationMethod, TrainableAugmentation, DualAugmentation, TSTCCDualAugmentation). Keep tests for retained symbols. Remove backward-compat barrel export tests.

    **Step 8 - Verify no remaining references:**
    Grep for `TrainingViews`, `AugmentationMethod`, `TrainableAugmentation`, `DualAugmentation`, `TSTCCDualAugmentation` across `src/` and `tests/`. All hits must be zero (or in comments documenting deletion).
  </action>
  <verify>
    <automated>uv run pytest tests/test_augmentation.py tests/test_augmentation_base.py tests/test_augmentation_per_model.py -v --tb=short && ty check src/</automated>
  </verify>
  <done>
    - TSTCCDualAugmentation already removed in plan 08 (REVIEW MEDIUM fix)
    - CropShiftAugmentation alias removed from ts2vec/augmentation.py
    - autotcl/utils.py no longer imports AugmentationMethod
    - autotcl/model.py no longer imports TrainableAugmentation
    - No references to old symbols in src/ or tests/ (grep confirms zero hits)
    - All tests pass
    - `ty check src/` passes with zero errors
  </done>
</task>

</tasks>

<verification>
`uv run pytest -v` passes. `ty check src/` passes with zero errors. `grep -rn "TrainingViews\|AugmentationMethod\|TrainableAugmentation\|DualAugmentation\|TSTCCDualAugmentation" src/ tests/` returns zero hits (excluding comments).
</verification>

<success_criteria>
1. TSTCCDualAugmentation already removed in plan 08
2. CropShiftAugmentation alias removed
3. autotcl/utils.py no longer imports AugmentationMethod
4. No remaining references to old symbols in src/ or tests/
5. All tests pass, ty check passes
</success_criteria>

<output>
Create `.planning/phases/01-augmentation-producer-contract/01-13-SUMMARY.md` when done
</output>
