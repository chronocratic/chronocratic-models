---
phase: 01-augmentation-producer-contract
plan: 07
type: execute
wave: 3
depends_on: [01-01, 01-02, 01-03, 01-04, 01-06]
files_modified:
  - src/tscollection/models/convolutional/dilated/autotcl/augmentation/methods.py
  - src/tscollection/models/convolutional/dilated/autotcl/model.py
  - src/tscollection/models/convolutional/dilated/autotcl/utils.py
  - tests/test_autotcl_producer.py
autonomous: true
requirements: [G3, G6]
user_setup: []

must_haves:
  truths:
    - "AutoTCLNeuralNetworkAugmentation inherits TrainableAugmentationProducer (nominal ABC + nn.Module)"
    - "AutoTCLNeuralNetworkAugmentation.produce() returns SingleView"
    - "AutoTCL accepts AugmentationProducer[SingleView] | None in constructor"
    - "AutoTCL.configure_optimizers uses maybe_configure_augmentation_optimizer helper"
    - "AutoTCL.training_step uses maybe_train_augmentation helper"
    - "AutoTCL has no isinstance(TrainableAugmentation) checks in model body"
    - "AutoTCL trains 5 steps with neural network augmentation (finite loss)"
    - "AutoTCL trains 5 steps with SingleViewProducer(Jitter(...)) (finite loss, static aug path)"
    - "AutoTCL numerical output matches old AutoTCL when seeded (SC-7)"
    - "autotcl/utils.py no longer imports AugmentationMethod; uses AugmentationProducer or new types"
  artifacts:
    - path: "src/tscollection/models/convolutional/dilated/autotcl/augmentation/methods.py"
      provides: "AutoTCLNeuralNetworkAugmentation reshaped to TrainableAugmentationProducer"
      contains: "class AutoTCLNeuralNetworkAugmentation"
    - path: "tests/test_autotcl_producer.py"
      provides: "TDD tests for AutoTCL producer integration"
      min_lines: 70
  key_links:
    - from: "src/tscollection/models/convolutional/dilated/autotcl/model.py"
      to: "src/tscollection/models/augmentation/trainable_support.py"
      via: "imports maybe_train_augmentation, maybe_configure_augmentation_optimizer"
      pattern: "from.*trainable_support import"
    - from: "src/tscollection/models/convolutional/dilated/autotcl/augmentation/methods.py"
      to: "src/tscollection/models/augmentation/base.py"
      via: "imports TrainableAugmentationProducer"
      pattern: "from.*base import.*TrainableAugmentationProducer"

---

<objective>
Wire AutoTCL to the new producer contract. Reshape `AutoTCLNeuralNetworkAugmentation` from `TrainableAugmentation` (old ABC) to `TrainableAugmentationProducer` (new ABC). Replace 5 `isinstance(TrainableAugmentation)` checks in the model body with the two centralized `maybe_*` helpers from `trainable_support.py` (D-02, SPEC §4.5.2). Update `autotcl/utils.py` to remove `AugmentationMethod` import (blocker #4: utils.py imports AugmentationMethod but was not addressed in prior plan). Third model in the sequential D-03 Phase B chain (TS2Vec -> CoST -> AutoTCL).
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsmodels/.planning/phases/01-augmentation-producer-contract/SPEC.md
@/Users/skaf/VSCodeProjects/tsmodels/.planning/codebase/CONVENTIONS.md
@/Users/skaf/VSCodeProjects/tsmodels/.planning/codebase/TESTING.md
</execution_context>

<context>
@src/tscollection/models/augmentation/base.py
@src/tscollection/models/augmentation/trainable_support.py
@src/tscollection/models/convolutional/dilated/autotcl/augmentation/methods.py
@src/tscollection/models/convolutional/dilated/autotcl/model.py
@src/tscollection/models/convolutional/dilated/autotcl/utils.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reshape AutoTCLNeuralNetworkAugmentation, wire AutoTCL model, update utils.py (per D-02, D-03 Phase B, third in sequence)</name>
  <files>
    tests/test_autotcl_producer.py
    src/tscollection/models/convolutional/dilated/autotcl/augmentation/methods.py
    src/tscollection/models/convolutional/dilated/autotcl/model.py
    src/tscollection/models/convolutional/dilated/autotcl/utils.py
  </files>
  <behavior>
    - Test: AutoTCLNeuralNetworkAugmentation is subclass of TrainableAugmentationProducer
    - Test: AutoTCLNeuralNetworkAugmentation is nn.Module
    - Test: AutoTCLNeuralNetworkAugmentation.produce() returns SingleView
    - Test: AutoTCLNeuralNetworkAugmentation.train_step() returns loss tensor
    - Test: AutoTCL constructor accepts AutoTCLNeuralNetworkAugmentation
    - Test: AutoTCL constructor accepts SingleViewProducer(Jitter(...))
    - Test: AutoTCL.configure_optimizers uses maybe_configure_augmentation_optimizer
    - Test: AutoTCL.training_step uses maybe_train_augmentation
    - Test: AutoTCL trains 5 steps with neural aug (finite loss)
    - Test: AutoTCL trains 5 steps with static aug (finite loss, no trainable path)
    - Test: Seeded AutoTCL produces identical loss sequence (numerical equivalence, SC-7)
  </behavior>
  <action>
    **Step 1 - Create test file (RED):** Write `tests/test_autotcl_producer.py` with all behaviors above. Include seeded comparison test for numerical equivalence (SC-7).

    **Step 2 - Reshape autotcl/augmentation/methods.py (GREEN):**

    `AutoTCLNeuralNetworkAugmentation` changes parent from `TrainableAugmentation` (old ABC) to `TrainableAugmentationProducer` (new ABC).

    Key changes:
    - Import `TrainableAugmentationProducer`, `AugmentationTrainingStrategy`, `SingleView` from `augmentation/base.py`
    - `class AutoTCLNeuralNetworkAugmentation(TrainableAugmentationProducer):` (new parent)
    - `__init__` signature stays the same (params, training_strategy)
    - Rename `augment(data) -> TrainingViews` to `produce(self, x: torch.Tensor) -> SingleView`
      - Return `SingleView(view=self.model.augment(x))` (NOT TrainingViews)
    - `train_step` stays the same — signature matches `TrainableAugmentationProducer.train_step`
    - `configure_optimizer` stays the same — signature matches parent
    - `_build_model`, `forward`, `get_model` stay the same
    - Keep `AutoTCLNeuralNetworkAugmentationParameters` as-is

    **Step 3 - Wire AutoTCL model (GREEN):**

    In `src/tscollection/models/convolutional/dilated/autotcl/model.py`:

    Import changes:
    - Add: `from tscollection.models.augmentation.base import AugmentationProducer, SingleView`
    - Add: `from tscollection.models.augmentation.trainable_support import maybe_train_augmentation, maybe_configure_augmentation_optimizer`
    - Remove `TrainableAugmentation` import (the 5 isinstance checks will be replaced)

    Constructor:
    - Change param: `augmentation: AugmentationProducer[SingleView] | None = None`
    - Change default: lazy import `AutoTCLNeuralNetworkAugmentation(...)`
    - Type annotation: `self._augmentation: AugmentationProducer[SingleView]`

    `configure_optimizers` (lines 89-95):
    ```python
    def configure_optimizers(self) -> AdamW | list[AdamW]:
        main_optimizer = AdamW(self._encoder.parameters(), lr=self._learning_rate)
        aug_opt = maybe_configure_augmentation_optimizer(
            self._augmentation, lr=self._meta_learning_rate
        )
        if aug_opt is not None:
            return [main_optimizer, aug_opt]
        return main_optimizer
    ```

    `training_step` (lines 108-177):
    ```python
    x = extract_features_from_batch(batch)
    x = process_sample_length(sample=x, max_sample_length=self._max_train_length)

    opts = self.optimizers()

    # Phase 1: Aug network self-training (centralized gate, not isinstance in model)
    aug_loss = maybe_train_augmentation(
        self._augmentation,
        x=x, encoder=self._encoder,
        epoch=self.current_epoch, batch_idx=batch_idx,
    )
    if aug_loss is not None:
        main_opt, meta_opt = cast('list[AdamW]', opts)
        # ... same optimizer step logic ...

    # Phase 2: Uniform encoder training — no isinstance check
    self._encoder.train()
    view = self._augmentation.produce(x)
    aug_x = view.view  # Changed from views.views[0]

    # ... rest of encoder training stays the same ...
    ```

    `_eval_mutual_information` (lines 179-205):
    - Replace `isinstance(TrainableAugmentation)` checks with `isinstance(TrainableAugmentationProducer)`. This is acceptable per SPEC §4.5.1 exception: diagnostic/logging methods may branch directly on TrainableAugmentationProducer because the maybe_* helpers are designed for training_step and configure_optimizers, and _eval_mutual_information has a legitimate need for `.eval()` / `.train(prev_mode)` toggling that does not fit the helper signature. Add an inline comment: `# SPEC §4.5.1 exception: diagnostic method may branch on TrainableAugmentationProducer directly`.

    `validation_step` (lines 207-236):
    - Change `views = self._augmentation.augment(x)` to `view = self._augmentation.produce(x)`
    - Change `aug_x = views.views[0]` to `aug_x = view.view`

    **Step 4 - Update autotcl/utils.py (GREEN):**

    In `src/tscollection/models/convolutional/dilated/autotcl/utils.py`:
    - Remove `from tscollection.models.augmentation.base import AugmentationMethod`
    - Update `calculate_mutual_information` signature: change `augmentation_method: AugmentationMethod` to accept the new type. Since utils.py is used for diagnostic purposes, the cleanest fix is to change the parameter type to `AugmentationProducer[SingleView]` and update the call from `augmentation_method.augment(x)` to `augmentation_method.produce(x)` with `.view` access.
    - Keep backward compat: retain `AugmentationMethod` as a type alias or accept both during transition (D-05).

    **Key risk (from RESEARCH.md):** 5 isinstance checks at lines 91, 124, 145, 195, 203. Lines 91, 124, 145 go through maybe_* helpers. Lines 195, 203 are in `_eval_mutual_information` — use `isinstance(TrainableAugmentationProducer)` directly there with SPEC §4.5.1 comment.

    **DO NOT delete old TrainableAugmentation yet** — D-05: keep until final delete commit.
  </action>
  <verify>
    <automated>uv run pytest tests/test_autotcl_producer.py -v --tb=short</automated>
  </verify>
  <done>
    - test_autotcl_producer.py has 10+ passing tests (including seeded comparison)
    - AutoTCLNeuralNetworkAugmentation inherits TrainableAugmentationProducer
    - AutoTCL training_step uses maybe_train_augmentation (no isinstance on TrainableAugmentation)
    - AutoTCL configure_optimizers uses maybe_configure_augmentation_optimizer
    - AutoTCL validation_step uses .produce() and .view
    - autotcl/utils.py no longer imports AugmentationMethod
    - _eval_mutual_information has SPEC §4.5.1 comment for isinstance exception
    - Both trainable and static augmentation paths work
    - `ty check src/tscollection/models/convolutional/dilated/autotcl/model.py` passes
  </done>
</task>

</tasks>

<verification>
`uv run pytest tests/test_autotcl_producer.py -v` passes. `ty check src/tscollection/models/convolutional/dilated/autotcl/model.py src/tscollection/models/convolutional/dilated/autotcl/augmentation/methods.py src/tscollection/models/convolutional/dilated/autotcl/utils.py` passes with zero errors.
</verification>

<success_criteria>
1. `AutoTCLNeuralNetworkAugmentation` inherits `TrainableAugmentationProducer`
2. `AutoTCL` accepts `AugmentationProducer[SingleView] | None`
3. `AutoTCL.training_step` uses `maybe_train_augmentation` (not isinstance checks)
4. `AutoTCL.configure_optimizers` uses `maybe_configure_augmentation_optimizer`
5. `autotcl/utils.py` no longer imports `AugmentationMethod`
6. `_eval_mutual_information` has SPEC §4.5.1 comment for isinstance exception
7. Both trainable and static augmentation paths train with finite loss
8. Seeded comparison test verifies numerical equivalence (SC-7)
9. All tests pass, type check passes
</success_criteria>

<output>
Create `.planning/phases/01-augmentation-producer-contract/01-07-SUMMARY.md` when done
</output>
