---
phase: 01-augmentation-producer-contract
plan: 05
type: execute
wave: 3
depends_on: [01-01, 01-02, 01-03, 01-04]
files_modified:
  - src/tscollection/models/convolutional/dilated/ts2vec/augmentation.py
  - src/tscollection/models/convolutional/dilated/ts2vec/model.py
  - tests/test_ts2vec_producer.py
autonomous: true
requirements: [G3, G6]
user_setup: []

must_haves:
  truths:
    - "CropShiftProducer returns AlignedPair with 0 < overlap_length <= T"
    - "TS2Vec accepts AugmentationProducer[AlignedPair] | None in constructor"
    - "TS2Vec._encode_augmented_views uses .produce() and reads .first, .second, .overlap_length"
    - "TS2Vec no longer accesses views.metadata['crop_length']"
    - "TS2Vec trains 5 steps with CropShiftProducer with finite loss"
    - "TS2Vec produces deterministic output when seeded (SC-7, determinism only per REVIEW)"
    - "FullOverlapPair(Jitter(...)) injected into TS2Vec trains with finite loss"
  artifacts:
    - path: "src/tscollection/models/convolutional/dilated/ts2vec/augmentation.py"
      provides: "CropShiftProducer (reshaped from CropShiftAugmentation)"
      contains: "class CropShiftProducer"
    - path: "tests/test_ts2vec_producer.py"
      provides: "TDD tests for TS2Vec producer integration"
      min_lines: 60
  key_links:
    - from: "src/tscollection/models/convolutional/dilated/ts2vec/model.py"
      to: "src/tscollection/models/augmentation/base.py"
      via: "imports AugmentationProducer, AlignedPair"
      pattern: "from.*base import.*AugmentationProducer"
    - from: "src/tscollection/models/convolutional/dilated/ts2vec/augmentation.py"
      to: "src/tscollection/models/augmentation/base.py"
      via: "imports AugmentationProducer, AlignedPair"
      pattern: "from.*base import"

---

<objective>
Wire TS2Vec to the new producer contract. Reshape `CropShiftAugmentation` into `CropShiftProducer` (SPEC §4.7, §5). Update `TS2Vec` to accept `AugmentationProducer[AlignedPair]` and use `.produce()` / `.first` / `.second` / `.overlap_length` instead of `.augment()` / `.views[i]` / `.metadata['crop_length']`. This eliminates the metadata dict dependency (SPEC §2.3 root cause #1). First model in the sequential D-03 Phase B chain (TS2Vec -> CoST -> AutoTCL -> TS-TCC).
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsmodels/.planning/phases/01-augmentation-producer-contract/SPEC.md
@/Users/skaf/VSCodeProjects/tsmodels/.planning/codebase/CONVENTIONS.md
@/Users/skaf/VSCodeProjects/tsmodels/.planning/codebase/TESTING.md
</execution_context>

<context>
@src/tscollection/models/augmentation/base.py
@src/tscollection/models/convolutional/dilated/ts2vec/augmentation.py
@src/tscollection/models/convolutional/dilated/ts2vec/model.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reshape CropShiftAugmentation to CropShiftProducer and wire TS2Vec model (per D-03 Phase B, first in sequence)</name>
  <files>
    tests/test_ts2vec_producer.py
    src/tscollection/models/convolutional/dilated/ts2vec/augmentation.py
    src/tscollection/models/convolutional/dilated/ts2vec/model.py
  </files>
  <behavior>
    - Test: CropShiftProducer produces AlignedPair with 0 < overlap_length <= T
    - Test: CropShiftProducer preserves per-sample crop offsets
    - Test: TS2Vec constructor accepts CropShiftProducer
    - Test: TS2Vec constructor accepts FullOverlapPair(Jitter(...))
    - Test: TS2Vec trains 5 steps with CropShiftProducer (finite loss)
    - Test: TS2Vec trains 5 steps with FullOverlapPair(Jitter(...)) (finite loss)
    - Test: Seeded TS2Vec with CropShiftProducer produces identical embeddings across two runs (determinism, SC-7 per REVIEW HIGH-3)
  </behavior>
  <action>
    **Step 1 - Create test file (RED):** Write `tests/test_ts2vec_producer.py` with all behaviors above. Include a seeded comparison test: create two TS2Vec models with same seed, run forward pass, verify embeddings are identical (SC-7: determinism only — REVIEW HIGH-3 notes equivalence fixtures out of scope).

    **Step 2 - Reshape ts2vec/augmentation.py (GREEN):**

    Keep the file but reshape `CropShiftAugmentation` into `CropShiftProducer`. Rename the class (D-05: keep old name as alias until final delete).

    Key changes to `augment()` method:
    - Rename to `produce(self, x: torch.Tensor) -> AlignedPair` (REVIEW HIGH-2: NO temporal_unit kwarg — `produce(x)` only)
    - Bake `temporal_unit` into `CropShiftProducer.__init__` via `CropShiftAugmentationParameters` (not passed per-call)
    - Return `AlignedPair(first=..., second=..., overlap_length=crop_length)` instead of `TrainingViews(views=(...), metadata={'crop_length': ...})`
    - The crop logic (extract_subsequences_per_row, offsets, etc.) stays the same
    - Import `AlignedPair` from `augmentation/base.py` (not from barrel to avoid circular imports)

    Keep `CropShiftAugmentationParameters` as-is.

    Add `CropShiftAugmentation = CropShiftProducer` alias (D-05: backward compat until delete).

    Update `__all__` to include `CropShiftProducer`.

    **Step 3 - Wire TS2Vec model (GREEN):**

    In `src/tscollection/models/convolutional/dilated/ts2vec/model.py`:
    - Change import: `from tscollection.models.augmentation.base import AugmentationProducer, AlignedPair`
    - Change constructor param: `augmentation: AugmentationProducer[AlignedPair] | None = None`
    - Change default: lazy import `CropShiftProducer()` instead of `CropShiftAugmentation()`
    - Change `_encode_augmented_views`:
      ```python
      pair = self._augmentation.produce(x)  # REVIEW HIGH-2: no temporal_unit kwarg
      encoder = self._encoder if self.training else self._averaged_encoder
      emb_1 = encoder(pair.first)[:, -pair.overlap_length:]
      emb_2 = encoder(pair.second)[:, :pair.overlap_length]
      ```
    - Remove `views.metadata['crop_length']` access
    - Remove `views.views[0]` / `views.views[1]` access

    **DO NOT delete old CropShiftAugmentation class yet** — D-05: keep until final delete commit.
  </action>
  <verify>
    <automated>uv run pytest tests/test_ts2vec_producer.py -v --tb=short</automated>
  </verify>
  <done>
    - test_ts2vec_producer.py has 7+ passing tests (including seeded comparison)
    - CropShiftProducer returns AlignedPair (not TrainingViews)
    - TS2Vec uses .produce() and .first/.second/.overlap_length
    - No metadata dict access in TS2Vec model
    - Both CropShiftProducer and FullOverlapPair(Jitter) work with TS2Vec
    - `ty check src/tscollection/models/convolutional/dilated/ts2vec/model.py` passes
  </done>
</task>

</tasks>

<verification>
`uv run pytest tests/test_ts2vec_producer.py -v` passes. `ty check src/tscollection/models/convolutional/dilated/ts2vec/model.py src/tscollection/models/convolutional/dilated/ts2vec/augmentation.py` passes with zero errors.
</verification>

<success_criteria>
1. `CropShiftProducer` in `ts2vec/augmentation.py` returns `AlignedPair`
2. `TS2Vec` accepts `AugmentationProducer[AlignedPair] | None`
3. `TS2Vec._encode_augmented_views` uses `.produce()` and typed fields
4. No `.metadata` access in TS2Vec model code
5. Seeded test verifies determinism (SC-7 per REVIEW HIGH-3 — equivalence fixtures deferred)
6. All tests pass, type check passes
</success_criteria>

<output>
Create `.planning/phases/01-augmentation-producer-contract/01-05-SUMMARY.md` when done
</output>
