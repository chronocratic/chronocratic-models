---
phase: 12-standardize-representation-dim-across-all-models
plan: 9
type: execute
wave: 4
depends_on: [12-07]
files_modified:
  - src/chronocratic/models/convolutional/dilated/encoders/encoders.py
autonomous: true
requirements: [D-01, D-02, D-05]
must_haves:
  truths:
    - "All dilated encoder classes (TS2VecTimeSeriesEncoder, AutoTCLTimeSeriesEncoder, CoSTTimeSeriesEncoder) use singular params"
    - "CoST encoder even-validator message uses representation_dim"
    - "CoST component_dim is encoder-internal (not in any config)"
    - "encoder_dilations, encoder_channels, encoder_kernels preserved unchanged"
  artifacts:
    - path: "src/chronocratic/models/convolutional/dilated/encoders/encoders.py"
      provides: "All dilated encoder classes with singular dimension params"
  key_links:
    - from: "src/chronocratic/models/convolutional/dilated/encoders/encoders.py"
      to: "src/chronocratic/models/convolutional/dilated/ts2vec/model.py"
      via: "TS2VecTimeSeriesEncoder constructor must accept renamed kwargs"
      pattern: "representation_dim"
---

<objective>
Rename any remaining *_dims in encoders.py (helper functions, docstrings, comments) that were not covered by per-model encoder renames in plans 03, 06, 07. Update even-validator messages and component_dim references.

Purpose: Plans 03 (TS2Vec), 06 (AutoTCL), 07 (CoST) renamed their respective encoder __init__ params. This plan catches any remaining *_dims in shared helper functions, docstrings, and comments in encoders.py that belong to none of those classes specifically.

Decisions: D-01 (plural to singular), D-02 (output_dims -> representation_dim), D-05 (CoST component_dim internal)
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsmodels/.claude/gsd-core/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/tsmodels/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-standardize-representation-dim-across-all-models/12-CONTEXT.md
@.planning/phases/12-standardize-representation-dim-across-all-models/12-RESEARCH.md
@.planning/todos/specs/dimension-param-naming-unification-spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep remaining *_dims in encoders.py</name>
  <files>
    src/chronocratic/models/convolutional/dilated/encoders/encoders.py
  </files>
  <behavior>
    - Zero *_dims identifiers remain in encoders.py (excluding encoder_dilations/channels/kernels)
    - All docstrings and comments use singular dimension names
  </behavior>
  <action>
    1. Grep encoders.py for remaining *_dims identifiers: `grep -n "_dims" encoders.py | grep -v "encoder_dilations\|encoder_channels\|encoder_kernels"`. Expected: zero hits after plans 03, 06, 07.

    2. Fix any survivors found (docstrings, comments, helper functions that are not part of encoder __init__ methods).

    3. Anti-pattern: do NOT rename encoder_dilations, encoder_channels, encoder_kernels (Pitfall 8 - they are not *_dims). Verify with grep after.
  </action>
  <verify>
    <automated>grep -rn "_dims" src/chronocratic/models/convolutional/dilated/encoders/encoders.py --include="*.py" | grep -v "encoder_dilations\|encoder_channels\|encoder_kernels" | wc -l</automated>
  </verify>
  <done>
    - Zero *_dims in encoders.py (excluding encoder_dilations/channels/kernels)
    - All docstrings and comments use singular dimension names
  </done>
</task>

</tasks>

<verification>
Grep: `grep -rn "_dims" src/chronocratic/models/convolutional/dilated/encoders/encoders.py --include="*.py" | grep -v "encoder_dilations\|encoder_channels\|encoder_kernels"` returns 0.
</verification>

<success_criteria>
Zero *_dims identifiers remain in encoders.py (excluding encoder_dilations/channels/kernels). encoder_dilations/channels/kernels preserved unchanged.
</success_criteria>

<output>
Create `.planning/phases/12-standardize-representation-dim-across-all-models/12-09-SUMMARY.md` when done
</output>
