---
phase: 12-standardize-representation-dim-across-all-models
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - src/chronocratic/models/convolutional/standard/tstcc/config.py
  - src/chronocratic/models/convolutional/standard/tstcc/model.py
  - src/chronocratic/models/convolutional/standard/tstcc/encoder.py
autonomous: true
requirements: [D-01, D-02, D-03]
must_haves:
  truths:
    - "TSTCC config uses singular dimension names throughout"
    - "TSTCC model exposes representation_dim property"
    - "TSTCC encoder constructor accepts representation_dim"
  artifacts:
    - path: "src/chronocratic/models/convolutional/standard/tstcc/config.py"
      provides: "TSTCCModelParameters with singular dimension fields"
    - path: "src/chronocratic/models/convolutional/standard/tstcc/model.py"
      provides: "TSTCC model with updated representation_dim property"
  key_links:
    - from: "src/chronocratic/models/convolutional/standard/tstcc/config.py"
      to: "src/chronocratic/models/convolutional/standard/tstcc/model.py"
      via: "config field names must match model __init__ params exactly"
      pattern: "representation_dim"
---

<objective>
Rename TSTCC config fields, model params, encoder wiring, and update existing representation_dim property.

Decisions: D-01 (plural to singular), D-02 (unify to representation_dim), D-03 (property returns encode output)
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsmodels/.claude/gsd-core/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/tsmodels/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-standardize-representation-dim-across-all-models/12-CONTEXT.md
@.planning/todos/specs/dimension-param-naming-unification-spec.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TSTCC config + model + encoder rename + property update</name>
  <files>
    src/chronocratic/models/convolutional/standard/tstcc/config.py
    src/chronocratic/models/convolutional/standard/tstcc/model.py
    src/chronocratic/models/convolutional/standard/tstcc/encoder.py
  </files>
  <behavior>
    - Config: input_dim, representation_dim (was output_dims)
    - Model __init__: singular params, private attrs
    - Model: existing representation_dim property updated to return self._encoder.representation_dim
    - Encoder: constructor accepts representation_dim, all internal references updated
    - TSTCC instantiation with new params succeeds
  </behavior>
  <action>
    1. In tstcc/config.py: rename input_dims to input_dim, output_dims to representation_dim. Update validators (message text), docstrings. On the representation_dim config dataclass field, add rename-breadcrumb comment per spec §7.0: `# migration(a13): renamed from output_dims — width of the vector encode() returns. See CHANGELOG v0.1.0a13.` Also expand the reason in the Google-style docstring Args entry for representation_dim.

    2. In tstcc/model.py: rename __init__ params. Update private attrs. Update encoder constructor call. Update existing representation_dim property to return self._encoder.representation_dim. Update _encode_batch docstrings.

    3. In tstcc/encoder.py: rename constructor param from old output name to representation_dim. Update internal references. Update docstrings.

    4. Grep tstcc/ directory for remaining plural dimension identifiers; rename any survivors.

    Anti-pattern: keep temporal_contrast_hidden_dim unchanged (D-04) — it is already singular. Breadcrumb goes on config field only, not encoder occurrences.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_tstcc_encode_output.py tests/unit/test_tstcc_supervised.py -x -q</automated>
  </verify>
  <done>
    - Zero plural dimension identifiers remain in tstcc/ source files
    - TSTCC instantiates with singular params
    - TSTCC.representation_dim returns correct encode output width
    - TSTCC(**vars(TSTCCModelParameters(...))) succeeds
    - All TSTCC-related tests pass
  </done>
</task>

</tasks>

<verification>
Run model-specific tests: `uv run pytest tests/unit/test_tstcc_encode_output.py tests/unit/test_tstcc_supervised.py tests/unit/test_tstcc_normalization_layer_type.py -x -q`
</verification>

<success_criteria>
TSTCC config, model, encoder all use singular dimension names. representation_dim property returns encode output width. temporal_contrast_hidden_dim unchanged. Tests pass.
</success_criteria>

<output>
Create `.planning/phases/12-standardize-representation-dim-across-all-models/12-02-SUMMARY.md` when done
</output>
