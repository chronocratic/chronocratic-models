---
phase: 12-standardize-representation-dim-across-all-models
plan: 3
type: execute
wave: 1
depends_on: []
files_modified:
  - src/chronocratic/models/convolutional/dilated/ts2vec/config.py
  - src/chronocratic/models/convolutional/dilated/ts2vec/model.py
  - src/chronocratic/models/convolutional/dilated/encoders/encoders.py
  - tests/unit/test_ts2vec_augmentation.py
autonomous: true
requirements: [D-01, D-02, D-03]
must_haves:
  truths:
    - "TS2Vec config uses singular dimension names: input_dim, hidden_dim, representation_dim"
    - "TS2Vec model __init__ uses singular dimension names"
    - "TS2Vec model exposes representation_dim property returning encode() output feature dim"
    - "TS2VecTimeSeriesEncoder in encoders.py renamed to match"
  artifacts:
    - path: "src/chronocratic/models/convolutional/dilated/ts2vec/config.py"
      provides: "TS2VecModelParameters with renamed fields"
    - path: "src/chronocratic/models/convolutional/dilated/ts2vec/model.py"
      provides: "TS2Vec with representation_dim property"
  key_links:
    - from: "src/chronocratic/models/convolutional/dilated/ts2vec/model.py"
      to: "src/chronocratic/models/convolutional/dilated/encoders/encoders.py"
      via: "TS2VecTimeSeriesEncoder renamed in same plan to avoid intermediate breakage"
      pattern: "representation_dim"
---

<objective>
Rename TS2Vec config, model, and TS2VecTimeSeriesEncoder in shared encoders.py. Add representation_dim property. Update TS2Vec-specific test. Per D-01, D-02, D-03.

Purpose: TS2Vec uses output_dims, input_dims, hidden_dims in config/model. Rename to representation_dim, input_dim, hidden_dim. Also rename TS2VecTimeSeriesEncoder in encoders.py to avoid intermediate broken state.
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsmodels/.claude/gsd-core/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/tsmodels/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-standardize-representation-dim-across-all-models/12-CONTEXT.md
@.planning/phases/12-standardize-representation-dim-across-all-models/12-RESEARCH.md
@.planning/todos/specs/dimension-param-naming-unification-spec.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename TS2Vec config + model + add representation_dim property</name>
  <files>
    src/chronocratic/models/convolutional/dilated/ts2vec/config.py
    src/chronocratic/models/convolutional/dilated/ts2vec/model.py
  </files>
  <behavior>
    - Config fields: input_dims->input_dim, hidden_dims->hidden_dim, output_dims->representation_dim
    - Model __init__: same renames, private attrs updated
    - New property: representation_dim returns self._representation_dim
    - Encoder kwargs in model __init__ use new names
  </behavior>
  <action>
    In config.py: rename input_dims->input_dim, hidden_dims->hidden_dim, output_dims->representation_dim per D-01. On the representation_dim config dataclass field, add rename-breadcrumb per spec §7.0: `# migration(a13): renamed from output_dims — width of the vector encode() returns. See CHANGELOG v0.1.0a13.` Expand reason in Google-style docstring Args entry for representation_dim. In model.py: rename __init__ params, private attrs, docstrings, and encoder kwargs. Add representation_dim @property returning self._representation_dim per D-03. After rename, grep ts2vec/ for remaining _dims occurrences; fix any that are not encoder_dilations/channels/kernels (Pitfall 8). Breadcrumb goes on config field only.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_ts2vec_augmentation.py -x -q</automated>
  </verify>
  <done>
    - config.py has zero *_dims fields (verified by grep)
    - model.py __init__ uses singular params
    - model.py exposes representation_dim property
    - Encoder kwargs updated to new names
    - TS2Vec-specific tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rename TS2VecTimeSeriesEncoder in encoders.py</name>
  <files>
    src/chronocratic/models/convolutional/dilated/encoders/encoders.py
    tests/unit/test_ts2vec_augmentation.py
  </files>
  <behavior>
    - TS2VecTimeSeriesEncoder __init__ params: input_dim, hidden_dim, representation_dim (singular)
    - Internal references updated
    - TS2Vec model and encoder kwargs match (no intermediate breakage)
  </behavior>
  <action>
    In encoders.py: rename TS2VecTimeSeriesEncoder __init__ params from plural to singular (input_dims->input_dim, hidden_dims->hidden_dim, output_dims->representation_dim). Update all internal references within the TS2VecTimeSeriesEncoder class only. Do NOT rename AutoTCLTimeSeriesEncoder or CoSTTimeSeriesEncoder here — they are handled in their own plans (06, 07). Anti-pattern: do not rename encoder_dilations/channels/kernels (Pitfall 8).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_ts2vec_augmentation.py -x -q</automated>
  </verify>
  <done>
    - TS2VecTimeSeriesEncoder uses singular params
    - TS2Vec model and encoder kwargs match
    - TS2Vec-specific tests pass
    - AutoTCL/CoST encoder classes unchanged
  </done>
</task>

</tasks>

<verification>
Run: `uv run pytest tests/unit/test_ts2vec_augmentation.py -x -q`. Grep: `grep -rn "_dims" src/chronocratic/models/convolutional/dilated/ts2vec/ --include="*.py"` returns 0.
</verification>

<success_criteria>
TS2Vec source and TS2VecTimeSeriesEncoder in encoders.py use singular dimension names. representation_dim property exists. Zero *_dims in ts2vec/ source. TS2Vec-specific tests pass.
</success_criteria>

<output>
Create `.planning/phases/12-standardize-representation-dim-across-all-models/12-03-SUMMARY.md` when done
</output>
