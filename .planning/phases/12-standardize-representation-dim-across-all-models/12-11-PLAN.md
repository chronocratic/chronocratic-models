---
phase: 12-standardize-representation-dim-across-all-models
plan: 11
type: execute
wave: 4
depends_on: [12-10]
files_modified:
  - src/chronocratic/models/transformer/tst/config.py
  - src/chronocratic/models/transformer/tst/model.py
  - src/chronocratic/models/supervised/factory.py
  - src/chronocratic/models/supervised/_adapters.py
  - tests/unit/test_tst_supervised.py
  - tests/unit/test_tst_encode_output.py
autonomous: true
requirements: [D-01, D-03, D-06, D-08]
must_haves:
  truths:
    - "TST config uses singular dimension names: input_dim, hidden_dim, feedforward_dim"
    - "TST representation_dim property returns hidden_dim (not hidden_dim * sequence_length)"
    - "make_tst_supervised computes head in_features = representation_dim * sequence_length"
    - "TST supervised forward pass output shapes unchanged"
  artifacts:
    - path: "src/chronocratic/models/transformer/tst/config.py"
      provides: "TSTModelParameters with renamed fields"
    - path: "src/chronocratic/models/transformer/tst/model.py"
      provides: "TST with fixed representation_dim property (returns hidden_dim)"
    - path: "src/chronocratic/models/supervised/factory.py"
      provides: "make_tst_supervised with inline flatten computation"
  key_links:
    - from: "src/chronocratic/models/transformer/tst/model.py"
      to: "src/chronocratic/models/supervised/factory.py"
      via: "representation_dim=hidden_dim; factory multiplies by sequence_length"
      pattern: "representation_dim"
---

<objective>
Rename TST config, model, and supervised factory. Fix representation_dim property to return hidden_dim (not hidden_dim * seq_len). Update make_tst_supervised to compute flatten inline. Update tests. Per D-01, D-03, D-06, D-08.

Purpose: TST's representation_dim property currently returns hidden_dims * sequence_length (a supervised-head artifact). Fix to return hidden_dim alone per D-03. The supervised factory must compensate by computing representation_dim * sequence_length inline per D-06. This is the CRUX of the phase (Pitfall 4).
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsmodels/.claude/gsd-core/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/tsmodels/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-standardize-representation-dim-across-all-models/12-CONTEXT.md
@.planning/phases/12-standardize-representation-dim-across-all-models/12-RESEARCH.md
@.planning/todos/specs/dimension-param-naming-unification-spec.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename TST config + model + fix representation_dim property</name>
  <files>
    src/chronocratic/models/transformer/tst/config.py
    src/chronocratic/models/transformer/tst/model.py
  </files>
  <behavior>
    - Config fields: input_dims->input_dim, hidden_dims->hidden_dim, feedforward_dims->feedforward_dim
    - Model __init__: same renames, private attrs updated
    - Property: representation_dim now returns self._encoder.hidden_dim (DROP * sequence_length)
    - Docstring: "Feature dim of the pooled encode() representation"
  </behavior>
  <action>
    In config.py: rename input_dims->input_dim, hidden_dims->hidden_dim, feedforward_dims->feedforward_dim per D-01. In model.py: rename __init__ params and private attrs. Update encoder kwargs. CRITICAL: fix representation_dim @property to return self._encoder.hidden_dim (not self._encoder.hidden_dims * self._encoder.sequence_length). Update property docstring to per D-03 definition. Per spec step 8.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_tst_encode_output.py -x -q</automated>
  </verify>
  <done>
    - config.py has zero *_dims fields
    - model.py __init__ uses singular params
    - representation_dim returns hidden_dim (not hidden_dim * sequence_length)
    - test_tst_encode_output passes
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix make_tst_supervised + _adapters docstring + update TST-specific tests</name>
  <files>
    src/chronocratic/models/supervised/factory.py
    src/chronocratic/models/supervised/_adapters.py
    tests/unit/test_tst_supervised.py
  </files>
  <behavior>
    - make_tst_supervised: head in_features = backbone.representation_dim * backbone.encoder.sequence_length
    - _adapters: docstring updated to reflect TST deliberate non-mirroring
    - test_tst_supervised: forward shapes unchanged, representation_dim assertion updated
  </behavior>
  <action>
    In factory.py make_tst_supervised: change head in_features from backbone.representation_dim to backbone.representation_dim * backbone.encoder.sequence_length per D-06. In _adapters.py: update TST-related docstring to note TST is the deliberate non-mirroring case. Update test_tst_supervised.py: rename kwargs, fix representation_dim assertions to expect hidden_dim (not hidden_dim * seq_len), verify supervised forward shapes unchanged. Per spec step 8. Shared test file updates (test_from_config.py, test_backbone_representation_dim.py, test_smoke.py, test_encoder_decoder_contract.py) are handled in plan 12.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_tst_supervised.py tests/unit/test_tst_encode_output.py -x -q -k tst</automated>
  </verify>
  <done>
    - make_tst_supervised computes in_features = representation_dim * sequence_length
    - _adapters docstring updated
    - TST-specific supervised tests pass
    - test_tst_supervised.py uses singular param names
  </done>
</task>

</tasks>

<verification>
Run: `uv run pytest tests/unit/ -x -q -k "tst or TST"` — all TST tests pass. Grep: `grep -rn "hidden_dims\|feedforward_dims\|input_dims" src/chronocratic/models/transformer/tst/ --include="*.py"` returns 0.
</verification>

<success_criteria>
TST source uses singular names. representation_dim returns hidden_dim. make_tst_supervised computes flatten inline. Supervised forward shapes unchanged. All TST tests pass.
</success_criteria>

<output>
Create `.planning/phases/12-standardize-representation-dim-across-all-models/12-11-SUMMARY.md` when done
</output>
