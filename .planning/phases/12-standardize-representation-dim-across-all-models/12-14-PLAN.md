---
phase: 12-standardize-representation-dim-across-all-models
plan: 14
type: execute
wave: 7
depends_on: [12-13]
files_modified: []
autonomous: true
requirements: [D-01, D-03, D-07, D-08, D-10]
must_haves:
  truths:
    - "Zero *_dims identifiers remain in src/ tree"
    - "Zero *_dims identifiers remain in tests/ tree"
    - "All 10 models expose correct representation_dim property"
    - "TST representation_dim returns hidden_dim (not hidden_dim * sequence_length)"
    - "make_tst_supervised head in_features = representation_dim * sequence_length"
    - "Full test suite passes with zero failures"
    - "ruff format + check clean"
    - "ty check src/ clean"
    - "graphify updated"
  artifacts:
    - path: "src/chronocratic/models/"
      provides: "Clean source tree with zero *_dims survivors"
  key_links:
    - from: "src/"
      to: "tests/"
      via: "All test kwargs match renamed source signatures"
      pattern: "input_dim"
---

<objective>
Final verification sweep: repo-wide *_dims grep, full test suite, ruff check, ty check, graphify update, ROADMAP update. Per D-01, D-03, D-07, D-08, D-10.

Purpose: Confirm zero *_dims survivors across entire repo. Run full test suite to ensure all renames and kw-only changes are coherent. Validate with linter and type checker. Update knowledge graph. Per spec step 13.
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsmodels/.claude/gsd-core/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/tsmodels/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-standardize-representation-dim-across-all-models/12-CONTEXT.md
@.planning/todos/specs/dimension-param-naming-unification-spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repo-wide *_dims grep sweep + fix survivors</name>
  <files>
  </files>
  <action>
    1. Grep src/ for all *_dims identifiers: `grep -rn "_dims" src/chronocratic/models/ --include="*.py" | grep -v "__pycache__"`. Expected: zero hits (encoder_dilations, encoder_channels, encoder_kernels are NOT _dims). 2. Grep tests/ for all *_dims identifiers: `grep -rn "_dims" tests/ --include="*.py" | grep -v "__pycache__"`. Expected: zero hits. 3. Fix any survivors found. 4. Verify no encoding_output_dim was introduced: `grep -rn "encoding_output_dim" src/ --include="*.py"` must return 0. 5. Verify latent_dim preserved in timevae: `grep -n "latent_dim" src/chronocratic/models/generative/timevae/` must show latent_dim exists. 6. Verify layers preserved in recurrentae: `grep -n "layers" src/chronocratic/models/recurrent/recurrentae/config.py` must show layers tuple. 7. Verify rename-breadcrumb comments per spec §7.0 exist on all 6 config dataclass fields renamed to representation_dim (per D-02): grep for `migration(a13)` in mcl/config.py, tstcc/config.py, ts2vec/config.py, autotcl/config.py, cost/config.py, series2vec/config.py. Each must have one breadcrumb with the correct old-name (output_dims for MCL/TSTCC/TS2Vec/AutoTCL/CoST, representation_dims for Series2Vec). 8. Add follow-up prune entry: in CHANGELOG.md or a TODO comment in each breadcrumb config file, note that breadcrumbs should be pruned a couple minor versions after a13.
  </action>
  <verify>
    <automated>
      SRC_DIMS=$(grep -rn "_dims" src/chronocratic/models/ --include="*.py" | grep -v "__pycache__" | grep -v "encoder_dilations\|encoder_channels\|encoder_kernels" | wc -l);
      TEST_DIMS=$(grep -rn "_dims" tests/ --include="*.py" | grep -v "__pycache__" | wc -l);
      BREADCRUMB_COUNT=$(grep -rl "migration(a13)" src/chronocratic/models/convolutional/standard/mcl/config.py src/chronocratic/models/convolutional/standard/tstcc/config.py src/chronocratic/models/convolutional/dilated/ts2vec/config.py src/chronocratic/models/convolutional/dilated/autotcl/config.py src/chronocratic/models/convolutional/dilated/cost/config.py src/chronocratic/models/convolutional/standard/series2vec/config.py 2>/dev/null | wc -l);
      echo "src/_dims=$SRC_DIMS tests/_dims=$TEST_DIMS breadcrumbs=$BREADCRUMB_COUNT"
    </automated>
  </verify>
  <done>
    - Zero *_dims in src/ (excluding encoder_dilations/channels/kernels)
    - Zero *_dims in tests/
    - No encoding_output_dim introduced
    - latent_dim and layers preserved
    - All 6 config fields renamed to representation_dim have migration(a13) breadcrumb comments
    - Follow-up prune entry added for breadcrumbs
  </done>
</task>

<task type="auto">
  <name>Task 2: Full test suite + lint + type check + graphify</name>
  <files>
  </files>
  <action>
    1. Run full test suite: `uv run pytest tests/ -x -q`. Must pass all tests. 2. Run ruff check: `uv run ruff check src/ tests/`. Must be clean. 3. Run ruff format check: `uv run ruff format --check src/ tests/`. Must be clean (auto-format if needed). 4. Run type check: `uv run ty check src/`. Must pass. 5. Update graphify: `graphify update .`. 6. Update ROADMAP.md Phase 12 plan list and status.
  </action>
  <verify>
    <automated>
      uv run pytest tests/ -x -q &&
      uv run ruff check src/ tests/ &&
      uv run ruff format --check src/ tests/ &&
      uv run ty check src/
    </automated>
  </verify>
  <done>
    - All tests pass
    - ruff check clean
    - ruff format clean
    - ty check clean
    - graphify updated
    - ROADMAP updated
  </done>
</task>

</tasks>

<verification>
Phase gate: `grep -rn "_dims" src/chronocratic/models/ --include="*.py" | grep -v "__pycache__" | grep -v "encoder_dilations\|encoder_channels\|encoder_kernels" | wc -l` returns 0. `uv run pytest tests/` passes. `uv run ruff check src/ tests/` clean. `uv run ty check src/` clean.
</verification>

<success_criteria>
Zero *_dims survivors in src/ and tests/. Full test suite passes. ruff clean. ty check clean. graphify updated. ROADMAP updated. Phase 12 complete.
</success_criteria>

<output>
Create `.planning/phases/12-standardize-representation-dim-across-all-models/12-14-SUMMARY.md` when done
</output>
