---
phase: 12-standardize-representation-dim-across-all-models
plan: 7
type: execute
wave: 2
depends_on: [12-06]
files_modified:
  - src/chronocratic/models/convolutional/dilated/cost/config.py
  - src/chronocratic/models/convolutional/dilated/cost/model.py
  - src/chronocratic/models/convolutional/dilated/encoders/encoders.py
  - tests/unit/test_cost_augmentation.py
  - tests/unit/test_cost_sequence_concat.py
autonomous: true
requirements: [D-01, D-02, D-03, D-05]
must_haves:
  truths:
    - "CoST config uses singular dimension names (input_dim, hidden_dim, representation_dim)"
    - "CoST model exposes representation_dim property returning full encode width"
    - "CoSTTimeSeriesEncoder in encoders.py renamed to match"
    - "CoST even-validator message uses representation_dim"
  artifacts:
    - path: "src/chronocratic/models/convolutional/dilated/cost/config.py"
      provides: "CoSTModelParameters with singular dimension fields"
    - path: "src/chronocratic/models/convolutional/dilated/cost/model.py"
      provides: "CoST model with representation_dim property"
  key_links:
    - from: "src/chronocratic/models/convolutional/dilated/cost/model.py"
      to: "src/chronocratic/models/convolutional/dilated/encoders/encoders.py"
      via: "CoSTTimeSeriesEncoder renamed in same plan to avoid intermediate breakage"
      pattern: "representation_dim"
---

<objective>
Rename CoST config, model, and CoSTTimeSeriesEncoder in shared encoders.py. Add representation_dim property returning full width (not 2x). Update even-validator message. Update CoST-specific tests. Per D-01, D-02, D-03, D-05.

Purpose: CoST uses output_dims, input_dims, hidden_dims in config/model. Rename to representation_dim, input_dim, hidden_dim. Also rename CoSTTimeSeriesEncoder in encoders.py to avoid intermediate broken state (model uses new kwargs, encoder still expects old).
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsmodels/.claude/gsd-core/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/tsmodels/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-standardize-representation-dim-across-all-models/12-CONTEXT.md
@.planning/phases/12-standardize-representation-dim-across-all-models/12-RESEARCH.md
@.planning/todos/specs/dimension-param-naming-unification-spec.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CoST config + model rename + representation_dim property</name>
  <files>
    src/chronocratic/models/convolutional/dilated/cost/config.py
    src/chronocratic/models/convolutional/dilated/cost/model.py
  </files>
  <behavior>
    - Config: input_dim, hidden_dim, representation_dim (all singular)
    - Model __init__: singular params, private attrs
    - Model: new representation_dim property returns self._representation_dim (full width, not component_dim)
    - CoST instantiation with new params succeeds
  </behavior>
  <action>
    1. In cost/config.py: rename input_dims -> input_dim, hidden_dims -> hidden_dim, output_dims -> representation_dim. Update docstrings and field descriptions. On the representation_dim config dataclass field, add rename-breadcrumb per spec §7.0: `# migration(a13): renamed from output_dims — width of the vector encode() returns. See CHANGELOG v0.1.0a13.` Expand reason in Google-style docstring Args entry for representation_dim.

    2. In cost/model.py: rename __init__ params to match config. Update private attrs. Update encoder constructor call-site kwargs to use new names. Rename component_dims -> component_dim in internal wiring (D-05: component_dim is encoder-internal, not a config field). Add representation_dim property returning self._representation_dim per D-03.

    3. Grep cost/ directory for remaining plural dimension identifiers; rename any survivors in docstrings and comments.

    Anti-pattern: component_dim must NOT be added to the config dataclass (Pitfall 3). It stays encoder-internal.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_cost_augmentation.py tests/unit/test_cost_sequence_concat.py -x -q</automated>
  </verify>
  <done>
    - Zero plural dimension identifiers remain in cost/ source files
    - CoST instantiates with singular params
    - CoST.representation_dim returns full encode width (not component_dim)
    - component_dim not in config
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CoSTTimeSeriesEncoder rename in encoders.py + CoST-specific tests</name>
  <files>
    src/chronocratic/models/convolutional/dilated/encoders/encoders.py
    tests/unit/test_cost_augmentation.py
    tests/unit/test_cost_sequence_concat.py
  </files>
  <behavior>
    - CoSTTimeSeriesEncoder __init__ params: input_dim, hidden_dim, representation_dim (singular)
    - component_dims -> component_dim (encoder-internal)
    - Even-validator message updated to use representation_dim
    - CoST model and encoder kwargs match (no intermediate breakage)
  </behavior>
  <action>
    1. In encoders.py: rename CoSTTimeSeriesEncoder __init__ params from plural to singular. Rename output_dims -> representation_dim, component_dims -> component_dim (D-05: encoder-internal only). Update even-validator message to reference representation_dim. Update all internal references within the CoSTTimeSeriesEncoder class only.

    Anti-pattern: do NOT rename TS2VecTimeSeriesEncoder or AutoTCLTimeSeriesEncoder here — they are handled in plans 03 and 06. Do NOT rename encoder_dilations/channels/kernels (Pitfall 8).

    2. Rename all CoST-related constructor kwargs from *_dims to *_dim in test_cost_augmentation.py and test_cost_sequence_concat.py. Shared test file updates are handled in plan 12.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_cost_augmentation.py tests/unit/test_cost_sequence_concat.py -x -q</automated>
  </verify>
  <done>
    - CoSTTimeSeriesEncoder uses singular params
    - CoST component_dim is encoder-internal
    - CoST even-validator uses representation_dim
    - CoST model and encoder kwargs match
    - CoST-specific tests pass
    - TS2Vec/AutoTCL encoder classes unchanged
  </done>
</task>

</tasks>

<verification>
Run CoST-specific tests: `uv run pytest tests/unit/test_cost_augmentation.py tests/unit/test_cost_sequence_concat.py -x -q`. Grep: `grep -rn "_dims" src/chronocratic/models/convolutional/dilated/cost/ --include="*.py"` returns 0.
</verification>

<success_criteria>
CoST config, model, and CoSTTimeSeriesEncoder in encoders.py all use singular dimension names. representation_dim returns full encode width. Even-validator updated. CoST-specific tests pass. Breadcrumb present on config field.
</success_criteria>

<output>
Create `.planning/phases/12-standardize-representation-dim-across-all-models/12-07-SUMMARY.md` when done
</output>
