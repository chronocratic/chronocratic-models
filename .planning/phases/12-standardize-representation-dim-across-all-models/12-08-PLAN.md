---
phase: 12-standardize-representation-dim-across-all-models
plan: 8
type: execute
wave: 1
depends_on: []
files_modified:
  - src/chronocratic/models/recurrent/recurrentae/config.py
  - src/chronocratic/models/recurrent/recurrentae/model.py
  - src/chronocratic/models/recurrent/recurrentae/layers.py
autonomous: true
requirements: [D-01, D-03, D-04]
must_haves:
  truths:
    - "RecurrentAE config uses input_dim (singular), layers tuple preserved unchanged"
    - "RecurrentAE model exposes representation_dim property returning layers[-1]"
    - "Layer builder functions use input_dim param"
  artifacts:
    - path: "src/chronocratic/models/recurrent/recurrentae/config.py"
      provides: "RecurrentAEModelParameters with input_dim, layers preserved"
    - path: "src/chronocratic/models/recurrent/recurrentae/model.py"
      provides: "RecurrentAE model with representation_dim property"
    - path: "src/chronocratic/models/recurrent/recurrentae/layers.py"
      provides: "Layer builder functions with input_dim"
  key_links:
    - from: "src/chronocratic/models/recurrent/recurrentae/config.py"
      to: "src/chronocratic/models/recurrent/recurrentae/model.py"
      via: "config field names must match model __init__ params exactly"
      pattern: "input_dim"
---

<objective>
Rename RecurrentAE config from input_dims to input_dim, update layer builder functions, add representation_dim property returning layers[-1]. Keep layers tuple UNCHANGED per D-04. Test updates handled in plan 12.

Decisions: D-01 (input_dims -> input_dim), D-03 (representation_dim returns layers[-1]), D-04 (layers stays)
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsmodels/.claude/gsd-core/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/tsmodels/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-standardize-representation-dim-across-all-models/12-CONTEXT.md
@.planning/todos/specs/dimension-param-naming-unification-spec.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RecurrentAE config + model + layers rename + representation_dim property</name>
  <files>
    src/chronocratic/models/recurrent/recurrentae/config.py
    src/chronocratic/models/recurrent/recurrentae/model.py
    src/chronocratic/models/recurrent/recurrentae/layers.py
  </files>
  <behavior>
    - Config: input_dim (was plural), layers tuple UNCHANGED
    - Model __init__: input_dim param, private attr _input_dim
    - Model: new representation_dim property returns self._layers[-1] (D-03)
    - Layer builder functions: input_dim param
    - RecurrentAE instantiation with new params succeeds
  </behavior>
  <action>
    1. In recurrentae/config.py: rename input_dims to input_dim. Keep layers tuple UNCHANGED per D-04. Update docstrings.

    2. In recurrentae/model.py: rename __init__ param input_dims to input_dim. Update private attr _input_dims to _input_dim. Update all internal references. Add representation_dim property returning self._layers[-1] per D-03.

    3. In recurrentae/layers.py: rename input_dims -> input_dim in layer builder function signatures and all internal references. Update docstrings.

    4. Grep recurrentae/ directory for remaining plural dimension identifiers; rename any survivors in docstrings and comments.

    Anti-pattern: do NOT rename layers to anything else. It is a genuine tuple per D-04.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_recurrent_autoencoder.py tests/unit/test_recurrent_encode_output.py -x -q</automated>
  </verify>
  <done>
    - Zero plural dimension identifiers remain in recurrentae/ source files
    - RecurrentAE instantiates with input_dim (singular)
    - RecurrentAE.representation_dim returns layers[-1]
    - layers tuple preserved unchanged
  </done>
</task>

</tasks>

<verification>
Run RecurrentAE-specific tests: `uv run pytest tests/unit/test_recurrent_autoencoder.py tests/unit/test_recurrent_encode_output.py -x -q`
</verification>

<success_criteria>
RecurrentAE config, model, and layers use input_dim (singular). layers tuple unchanged. representation_dim returns layers[-1]. Model-specific tests pass. Test updates for shared test files handled in plan 12.
</success_criteria>

<output>
Create `.planning/phases/12-standardize-representation-dim-across-all-models/12-08-SUMMARY.md` when done
</output>
