---
phase: 12-standardize-representation-dim-across-all-models
plan: 4
type: execute
wave: 1
depends_on: []
files_modified:
  - src/chronocratic/models/generative/timevae/config.py
  - src/chronocratic/models/generative/timevae/model.py
  - tests/unit/test_timevae_config.py
autonomous: true
requirements: [D-01, D-03, D-04, D-08]
must_haves:
  truths:
    - "TimeVAE config uses input_dim (singular) and keeps latent_dim unchanged"
    - "TimeVAE model exposes representation_dim property returning latent_dim"
  artifacts:
    - path: "src/chronocratic/models/generative/timevae/config.py"
      provides: "TimeVAEModelParameters with input_dim, latent_dim preserved"
    - path: "src/chronocratic/models/generative/timevae/model.py"
      provides: "TimeVAE model with representation_dim property"
  key_links:
    - from: "src/chronocratic/models/generative/timevae/config.py"
      to: "src/chronocratic/models/generative/timevae/model.py"
      via: "config field names must match model __init__ params exactly"
      pattern: "input_dim"
---

<objective>
Rename TimeVAE config from input_dims to input_dim, keep latent_dim unchanged (D-04), add representation_dim property, and update related tests.

Decisions: D-01 (plural to singular for input_dim), D-03 (representation_dim property returns latent_dim), D-04 (latent_dim stays), D-08 (test updates)
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsmodels/.claude/gsd-core/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/tsmodels/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-standardize-representation-dim-across-all-models/12-CONTEXT.md
@.planning/todos/specs/dimension-param-naming-unification-spec.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TimeVAE config + model rename + representation_dim property</name>
  <files>
    src/chronocratic/models/generative/timevae/config.py
    src/chronocratic/models/generative/timevae/model.py
  </files>
  <behavior>
    - Config: input_dim (was plural), latent_dim stays unchanged (D-04)
    - Model __init__: input_dim param, private attr _input_dim
    - Model: new representation_dim property returns self.latent_dim (D-03)
    - TimeVAE instantiation with new params succeeds
  </behavior>
  <action>
    1. In timevae/config.py: rename input_dims to input_dim. Keep latent_dim UNCHANGED per D-04. Update docstrings.

    2. In timevae/model.py: rename __init__ param input_dims to input_dim. Update private attr _input_dims to _input_dim. Update all internal references. Add representation_dim property returning self.latent_dim per D-03.

    3. Grep timevae/ directory for remaining plural dimension identifiers; rename any survivors in docstrings and comments.

    Anti-pattern: do NOT rename latent_dim to anything else. It is a distinct concept (stochastic bottleneck) per D-04.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_timevae_config.py -x -q</automated>
  </verify>
  <done>
    - Zero plural dimension identifiers remain in timevae/ source files
    - TimeVAE instantiates with input_dim (singular)
    - TimeVAE.representation_dim returns latent_dim
    - latent_dim kwarg preserved unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Update TimeVAE-specific test with new param names</name>
  <files>
    tests/unit/test_timevae_config.py
  </files>
  <action>
    1. In test_timevae_config.py: rename all TimeVAE constructor kwargs from input_dims to input_dim. Update any field name assertions.

    2. Shared test file updates (test_from_config.py, test_backbone_representation_dim.py, test_smoke.py) are handled in plan 12 to avoid file conflicts and enable parallel execution.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_timevae_config.py -x -q</automated>
  </verify>
  <done>
    - test_timevae_config.py uses input_dim (singular)
    - TimeVAE-specific tests pass
  </done>
</task>

</tasks>

<verification>
Run model-specific tests: `uv run pytest tests/unit/test_timevae_config.py tests/unit/test_from_config.py tests/unit/test_backbone_representation_dim.py tests/unit/test_smoke.py -x -q`
</verification>

<success_criteria>
TimeVAE config, model, and tests use input_dim (singular). latent_dim unchanged. representation_dim property returns latent_dim. Tests pass.
</success_criteria>

<output>
Create `.planning/phases/12-standardize-representation-dim-across-all-models/12-04-SUMMARY.md` when done
</output>
