---
phase: 12-standardize-representation-dim-across-all-models
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - src/chronocratic/models/convolutional/standard/series2vec/config.py
  - src/chronocratic/models/convolutional/standard/series2vec/model.py
  - src/chronocratic/models/convolutional/standard/series2vec/network.py
  - src/chronocratic/models/supervised/_adapters.py
autonomous: true
requirements: [D-01, D-02, D-03]
must_haves:
  truths:
    - "Series2Vec config uses input_dim and representation_dim (singular)"
    - "Series2Vec model exposes representation_dim property returning network.representation_dim"
    - "Series2Vec network uses representation_dim and branch_dim"
    - "Supervised _adapters docstring updated for Series2Vec"
  artifacts:
    - path: "src/chronocratic/models/convolutional/standard/series2vec/config.py"
      provides: "Series2VecModelParameters with input_dim, representation_dim"
    - path: "src/chronocratic/models/convolutional/standard/series2vec/network.py"
      provides: "Network with representation_dim and branch_dim"
  key_links:
    - from: "src/chronocratic/models/convolutional/standard/series2vec/model.py"
      to: "src/chronocratic/models/convolutional/standard/series2vec/network.py"
      via: "Network constructor call must use renamed params"
      pattern: "representation_dim"
---

<objective>
Rename Series2Vec config, model, network from representation_dims/branch_dims to representation_dim/branch_dim. Update existing representation_dim property. Update supervised _adapters docstring. Test updates handled in plan 12.

Decisions: D-01 (plural to singular), D-02 (representation_dims -> representation_dim), D-03 (property)
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsmodels/.claude/gsd-core/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/tsmodels/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-standardize-representation-dim-across-all-models/12-CONTEXT.md
@.planning/phases/12-standardize-representation-dim-across-all-models/12-RESEARCH.md
@.planning/todos/specs/dimension-param-naming-unification-spec.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Series2Vec config + model rename + property update</name>
  <files>
    src/chronocratic/models/convolutional/standard/series2vec/config.py
    src/chronocratic/models/convolutional/standard/series2vec/model.py
    src/chronocratic/models/convolutional/standard/series2vec/network.py
  </files>
  <behavior>
    - Config: input_dim, representation_dim (both singular)
    - Model __init__: singular params, private attrs
    - Model: existing representation_dim property updated to return self.network.representation_dim
    - Network: representation_dims -> representation_dim, branch_dims -> branch_dim
    - Network: existing representation_dim property updated
    - Series2Vec instantiation with new params succeeds
  </behavior>
  <action>
    1. In series2vec/config.py: rename input_dims -> input_dim, representation_dims -> representation_dim. Update docstrings and field descriptions. On the representation_dim config dataclass field, add rename-breadcrumb per spec §7.0: `# migration(a13): renamed from representation_dims — width of the vector encode() returns. See CHANGELOG v0.1.0a13.` (Note: old name is representation_dims for Series2Vec, not output_dims.) Expand reason in Google-style docstring Args entry for representation_dim.

    2. In series2vec/model.py: rename __init__ params to match config. Update private attrs. Update network constructor call-site kwargs. Update existing representation_dim property to return self.network.representation_dim. Update _encode_batch docstrings.

    3. In series2vec/network.py: rename representation_dims -> representation_dim, branch_dims -> branch_dim. Update all validators and their messages. Update internal references. Update existing representation_dim property. Update docstrings.

    4. Grep series2vec/ directory for remaining plural dimension identifiers; rename any survivors.

    Anti-pattern: embedding_dims is listed in spec §2.2 rename map. Rename it to embedding_dim if it exists in network.py or config.py. Breadcrumb goes on config field only, not encoder/network occurrences.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_series2vec_config_defaults.py tests/unit/test_series2vec_supervised.py -x -q</automated>
  </verify>
  <done>
    - Zero plural dimension identifiers remain in series2vec/ source files
    - Series2Vec instantiates with singular params
    - Series2Vec.representation_dim returns network.representation_dim
    - Network uses representation_dim and branch_dim
    - Breadcrumb comment present on representation_dim config field
  </done>
</task>

<task type="auto">
  <name>Task 2: Update supervised _adapters docstring for Series2Vec</name>
  <files>
    src/chronocratic/models/supervised/_adapters.py
  </files>
  <action>
    1. In supervised/_adapters.py: update Series2Vec-related docstring references from the old plural names to singular (e.g., 2*representation_dims -> 2*representation_dim).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_series2vec_supervised.py -x -q</automated>
  </verify>
  <done>
    - _adapters docstring uses singular names for Series2Vec
    - Series2Vec supervised tests pass
  </done>
</task>

</tasks>

<verification>
Run Series2Vec-specific tests: `uv run pytest tests/unit/test_series2vec_config_defaults.py tests/unit/test_series2vec_supervised.py tests/unit/test_series2vec_distance_normalizer.py tests/unit/test_series2vec_normalization_layer_type.py -x -q`
</verification>

<success_criteria>
Series2Vec config, model, and network use singular dimension names. representation_dim property returns network.representation_dim. branch_dim used internally. _adapters docstring updated. Breadcrumb present. Tests pass. Test updates for shared test files handled in plan 12.
</success_criteria>

<output>
Create `.planning/phases/12-standardize-representation-dim-across-all-models/12-10-SUMMARY.md` when done
</output>
