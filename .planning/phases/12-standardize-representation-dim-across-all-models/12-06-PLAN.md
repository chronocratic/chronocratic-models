---
phase: 12-standardize-representation-dim-across-all-models
plan: 6
type: execute
wave: 2
depends_on: [12-03]
files_modified:
  - src/chronocratic/models/convolutional/dilated/autotcl/config.py
  - src/chronocratic/models/convolutional/dilated/autotcl/model.py
  - src/chronocratic/models/convolutional/dilated/encoders/encoders.py
  - tests/unit/test_autotcl_augmentation.py
autonomous: true
requirements: [D-01, D-02, D-03]
must_haves:
  truths:
    - "AutoTCL config uses singular dimension names (input_dim, hidden_dim, representation_dim)"
    - "AutoTCL model exposes representation_dim property"
    - "AutoTCLTimeSeriesEncoder in encoders.py renamed to match"
  artifacts:
    - path: "src/chronocratic/models/convolutional/dilated/autotcl/config.py"
      provides: "AutoTCLModelParameters with singular dimension fields"
    - path: "src/chronocratic/models/convolutional/dilated/autotcl/model.py"
      provides: "AutoTCL model with representation_dim property"
  key_links:
    - from: "src/chronocratic/models/convolutional/dilated/autotcl/model.py"
      to: "src/chronocratic/models/convolutional/dilated/encoders/encoders.py"
      via: "AutoTCLTimeSeriesEncoder renamed in same plan to avoid intermediate breakage"
      pattern: "representation_dim"
---

<objective>
Rename AutoTCL config, model, and AutoTCLTimeSeriesEncoder in shared encoders.py. Add representation_dim property. Update AutoTCL-specific test. Per D-01, D-02, D-03.

Purpose: AutoTCL uses output_dims, input_dims, hidden_dims in config/model. Rename to representation_dim, input_dim, hidden_dim. Also rename AutoTCLTimeSeriesEncoder in encoders.py to avoid intermediate broken state (model uses new kwargs, encoder still expects old).
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsmodels/.claude/gsd-core/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/tsmodels/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-standardize-representation-dim-across-all-models/12-CONTEXT.md
@.planning/todos/specs/dimension-param-naming-unification-spec.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AutoTCL config + model rename + representation_dim property</name>
  <files>
    src/chronocratic/models/convolutional/dilated/autotcl/config.py
    src/chronocratic/models/convolutional/dilated/autotcl/model.py
  </files>
  <behavior>
    - Config: input_dim, hidden_dim, representation_dim (all singular)
    - Model __init__: singular params, private attrs
    - Model: new representation_dim property returns self._representation_dim
    - AutoTCL instantiation with new params succeeds
  </behavior>
  <action>
    1. In autotcl/config.py: rename input_dims -> input_dim, hidden_dims -> hidden_dim, output_dims -> representation_dim. Update docstrings and field descriptions. On the representation_dim config dataclass field, add rename-breadcrumb per spec §7.0: `# migration(a13): renamed from output_dims — width of the vector encode() returns. See CHANGELOG v0.1.0a13.` Expand reason in Google-style docstring Args entry for representation_dim.

    2. In autotcl/model.py: rename __init__ params to match config. Update private attrs. Update encoder constructor call-site kwargs to use new names (input_dim, hidden_dim, representation_dim). Add representation_dim property returning self._representation_dim per D-03.

    3. Grep autotcl/ directory for remaining plural dimension identifiers; rename any survivors in docstrings and comments.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_autotcl_augmentation.py -x -q</automated>
  </verify>
  <done>
    - Zero plural dimension identifiers remain in autotcl/ source files
    - AutoTCL instantiates with singular params
    - AutoTCL.representation_dim returns correct encode output width
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AutoTCLTimeSeriesEncoder rename in encoders.py + AutoTCL-specific test</name>
  <files>
    src/chronocratic/models/convolutional/dilated/encoders/encoders.py
    tests/unit/test_autotcl_augmentation.py
  </files>
  <behavior>
    - AutoTCLTimeSeriesEncoder __init__ params: input_dim, hidden_dim, representation_dim (singular)
    - Internal references updated
    - AutoTCL model and encoder kwargs match (no intermediate breakage)
  </behavior>
  <action>
    1. In encoders.py: rename AutoTCLTimeSeriesEncoder __init__ params from plural to singular (input_dims->input_dim, hidden_dims->hidden_dim, output_dims->representation_dim). Update all internal references within the AutoTCLTimeSeriesEncoder class only.

    Anti-pattern: do NOT rename TS2VecTimeSeriesEncoder or CoSTTimeSeriesEncoder here — they are handled in their own plans (03, 07). Do NOT rename encoder_dilations/channels/kernels (Pitfall 8).

    2. Rename all AutoTCL-related constructor kwargs from *_dims to *_dim in test_autotcl_augmentation.py. Shared test file updates are handled in plan 12.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_autotcl_augmentation.py -x -q</automated>
  </verify>
  <done>
    - AutoTCLTimeSeriesEncoder uses singular params
    - AutoTCL model and encoder kwargs match
    - AutoTCL-specific tests pass
    - TS2Vec/CoST encoder classes unchanged
  </done>
</task>

</tasks>

<verification>
Run AutoTCL-specific tests: `uv run pytest tests/unit/test_autotcl_augmentation.py -x -q`. Grep: `grep -rn "_dims" src/chronocratic/models/convolutional/dilated/autotcl/ --include="*.py"` returns 0.
</verification>

<success_criteria>
AutoTCL config, model, and AutoTCLTimeSeriesEncoder in encoders.py all use singular dimension names. representation_dim property exists. AutoTCL-specific tests pass. Breadcrumb present on config field.
</success_criteria>

<output>
Create `.planning/phases/12-standardize-representation-dim-across-all-models/12-06-SUMMARY.md` when done
</output>
