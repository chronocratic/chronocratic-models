---
phase: 12-standardize-representation-dim-across-all-models
plan: 5
type: execute
wave: 1
depends_on: []
files_modified:
  - src/chronocratic/models/recurrent/timenet/config.py
  - src/chronocratic/models/recurrent/timenet/model.py
  - tests/unit/test_timenet_config.py
autonomous: true
requirements: [D-01, D-03]
must_haves:
  truths:
    - "TimeNet config uses input_dim and hidden_dim (singular)"
    - "TimeNet model __init__ uses input_dim and hidden_dim (singular)"
    - "TimeNet model exposes representation_dim property returning hidden_dim"
  artifacts:
    - path: "src/chronocratic/models/recurrent/timenet/config.py"
      provides: "TimeNetModelParameters with renamed fields"
    - path: "src/chronocratic/models/recurrent/timenet/model.py"
      provides: "TimeNet with representation_dim property"
  key_links:
    - from: "src/chronocratic/models/recurrent/timenet/config.py"
      to: "src/chronocratic/models/recurrent/timenet/model.py"
      via: "config field names must match model __init__ params exactly"
      pattern: "input_dim"
---

<objective>
Rename TimeNet config and model from input_dims/hidden_dims to input_dim/hidden_dim. Add representation_dim property. Update TimeNet-specific test. Per D-01, D-03.

Purpose: TimeNet uses input_dims and hidden_dims in config/model. Rename to input_dim and hidden_dim. Add representation_dim property that returns hidden_dim (encode() output feature dimension).
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsmodels/.claude/gsd-core/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/tsmodels/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-standardize-representation-dim-across-all-models/12-CONTEXT.md
@.planning/phases/12-standardize-representation-dim-across-all-models/12-RESEARCH.md
@.planning/todos/specs/dimension-param-naming-unification-spec.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename TimeNet config + model + add representation_dim property</name>
  <files>
    src/chronocratic/models/recurrent/timenet/config.py
    src/chronocratic/models/recurrent/timenet/model.py
  </files>
  <behavior>
    - Config fields: input_dims->input_dim, hidden_dims->hidden_dim
    - Model __init__: same renames, private attrs _input_dims->_input_dim, _hidden_dims->_hidden_dim
    - New property: representation_dim returns self._hidden_dim
    - TimeNet instantiation with new params succeeds
  </behavior>
  <action>
    In config.py: rename input_dims->input_dim, hidden_dims->hidden_dim per D-01. In model.py: rename __init__ params, private attrs, docstrings, and all internal references. Add representation_dim @property returning self._hidden_dim per D-03. Update save_hyperparameters ignore list if needed. After rename, grep timenet/ for remaining _dims occurrences; fix any survivors.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_timenet_config.py -x -q</automated>
  </verify>
  <done>
    - config.py has zero *_dims fields (verified by grep)
    - model.py __init__ uses singular params
    - model.py exposes representation_dim property
    - TimeNet-specific tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Update TimeNet-specific test</name>
  <files>
    tests/unit/test_timenet_config.py
  </files>
  <behavior>
    - test_timenet_config.py uses new singular param names
  </behavior>
  <action>
    Rename all TimeNet-related constructor kwargs from *_dims to *_dim in test_timenet_config.py. Shared test file updates are handled in plan 12.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_timenet_config.py -x -q</automated>
  </verify>
  <done>
    - test_timenet_config.py uses new singular param names
    - TimeNet-specific tests pass
  </done>
</task>

</tasks>

<verification>
Run: `uv run pytest tests/unit/test_timenet_config.py -x -q`. Grep: `grep -rn "_dims" src/chronocratic/models/recurrent/timenet/ --include="*.py"` returns 0.
</verification>

<success_criteria>
TimeNet source files use singular dimension names. representation_dim property exists. Zero *_dims in timenet/ source. TimeNet-specific tests pass.
</success_criteria>

<output>
Create `.planning/phases/12-standardize-representation-dim-across-all-models/12-05-SUMMARY.md` when done
</output>
