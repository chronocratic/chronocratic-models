---
phase: 12-standardize-representation-dim-across-all-models
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/chronocratic/models/convolutional/standard/mcl/config.py
  - src/chronocratic/models/convolutional/standard/mcl/model.py
  - src/chronocratic/models/convolutional/standard/mcl/encoder.py
autonomous: true
requirements: [D-01, D-02, D-03]
must_haves:
  truths:
    - "MCL config uses singular dimension names throughout"
    - "MCL model exposes representation_dim property returning encode() output width"
    - "MCL encoder constructor accepts representation_dim (not old name)"
  artifacts:
    - path: "src/chronocratic/models/convolutional/standard/mcl/config.py"
      provides: "MCLModelParameters with singular dimension fields"
    - path: "src/chronocratic/models/convolutional/standard/mcl/model.py"
      provides: "MCL model with representation_dim property"
    - path: "src/chronocratic/models/convolutional/standard/mcl/encoder.py"
      provides: "FCNEncoder accepting representation_dim"
  key_links:
    - from: "src/chronocratic/models/convolutional/standard/mcl/config.py"
      to: "src/chronocratic/models/convolutional/standard/mcl/model.py"
      via: "config field names must match model __init__ params exactly"
      pattern: "representation_dim"
---

<objective>
Rename MCL config fields from plural to singular, rename model params, update encoder wiring, and add representation_dim property.

Decisions: D-01 (plural to singular), D-02 (unify output parameter to representation_dim), D-03 (representation_dim property)
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsmodels/.claude/gsd-core/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/tsmodels/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-standardize-representation-dim-across-all-models/12-CONTEXT.md
@.planning/phases/12-standardize-representation-dim-across-all-models/12-RESEARCH.md
@.planning/todos/specs/dimension-param-naming-unification-spec.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MCL config + model rename + representation_dim property</name>
  <files>
    src/chronocratic/models/convolutional/standard/mcl/config.py
    src/chronocratic/models/convolutional/standard/mcl/model.py
    src/chronocratic/models/convolutional/standard/mcl/encoder.py
  </files>
  <behavior>
    - Config: input_dim (was plural), representation_dim (was output_dims), projection_dim (was plural)
    - Model __init__: same param names as config, private attrs use singular
    - Model: new representation_dim property returns self._representation_dim
    - Encoder: constructor accepts representation_dim, stores as self.representation_dim
    - MCL instantiation with new params succeeds
    - MCL(**vars(MCLModelParameters(...))) round-trips without error
  </behavior>
  <action>
    1. In mcl/config.py: rename all plural dimension fields to singular (input_dim, representation_dim, projection_dim). Update docstrings and field descriptions. On the representation_dim config dataclass field, add rename-breadcrumb comment per spec §7.0: `# migration(a13): renamed from `output_dims` - width of the vector encode() returns. See CHANGELOG v0.1.0a13.` Also expand the reason in the Google-style docstring Args entry for representation_dim.

    2. In mcl/model.py: rename __init__ params to match config. Update private attrs (self._input_dim, self._representation_dim, self._projection_dim). Update all internal references to old private attrs. Update encoder constructor call to use new param names. Add representation_dim property returning self._representation_dim per D-03. Fix stale comments referencing old names.

    3. In mcl/encoder.py: rename constructor param from the old output parameter name to representation_dim. Update internal references. Update docstrings.

    4. Grep mcl/ directory for remaining plural dimension identifiers; rename any survivors in docstrings.

    Anti-pattern: ensure no partial rename leaves config saying one thing and model saying another. Verify MCL(**vars(MCLModelParameters(...))) works. Breadcrumb goes on config field only, not encoder occurrences.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_mcl_config_and_encoder.py -x -q</automated>
  </verify>
  <done>
    - Zero plural dimension identifiers remain in mcl/ source files
    - MCL instantiates with singular params
    - MCL.representation_dim returns correct encode output width
    - MCL(**vars(MCLModelParameters(...))) succeeds
    - MCL-related tests pass with updated param names
  </done>
</task>

</tasks>

<verification>
Run model-specific tests: `uv run pytest tests/unit/test_mcl_config_and_encoder.py -x -q`
Grep verification: `grep -rn "_dims" src/chronocratic/models/convolutional/standard/mcl/ --include="*.py" | grep -v "^#"` should return 0
</verification>

<success_criteria>
MCL config, model, encoder all use singular dimension names. representation_dim property exists and returns encode() output width. Tests pass.
</success_criteria>

<output>
Create `.planning/phases/12-standardize-representation-dim-across-all-models/12-01-SUMMARY.md` when done
</output>
