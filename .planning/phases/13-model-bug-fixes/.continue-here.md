---
context: phase
phase: 13-model-bug-fixes
plan: 1
task: 0
total_tasks: 0
status: paused_before_execution
last_updated: 2026-07-14T15:39:00
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: No backward compat shims for moved functions — user explicitly rejected. Clean cut only. Update all internal callers.
- [ ] CONSTRAINT: Verify commands MUST use `uv run python`, not bare `python` — this system has no `python` binary.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Importing from wrong encoder module | `DisjoinEncoder` lives in Series2Vec, not dilated encoders. Confused the two in plan verify. | blocking | Verify against actual `__all__` exports before writing import check |
| Backward compat shim without ask | Added masking.py re-export user didn't want. Rejected immediately. | advisory | Always confirm migration strategy before writing shim code |

## Current State

Phase 13 planning complete. 6 plans created and validated. Plan 1 (13-01) is current focus — shared NaN helpers. Working tree clean. Plan 1 has been through plan checker 3 rounds, all blockers fixed.

## Completed Work This Session

- Reviewed Phase 13 plan structure (6 plans, 4 waves)
- Explained `masked_reconstruction_loss` purpose to user
- User requested clean cut for `generate_not_nan_mask` migration (no backward compat)
- Updated Plan 1:
  - Added `generate_not_nan_mask` move from dilated/masking.py → utils.py
  - Task 3: barrel exports in __init__.py
  - Task 4: update encoders.py import + remove from masking.py
  - Added 3 test files (generate_not_nan_mask, zero_fill_padding, masked_reconstruction_loss)
- Plan checker runs: 3 iterations, all blockers/warnings fixed:
  - Blocker: Task 3 verify used bare `python` → fixed to `uv run python`
  - Blocker: Task 4 verify imported non-existent `DisjoinEncoder` from dilated → fixed to `TS2VecTimeSeriesEncoder`
  - Warning: Task 2 verify missing generate_not_nan_mask test → fixed
  - Warning: artifact list incomplete → fixed masking.py __all__ mention

## Remaining Work

- **Plan 1 (13-01):** Ready to execute — creates shared helpers
- **Plan 2 (13-02):** Ready — Series2Vec kernel rename + auto-clamp
- **Plan 3 (13-03):** Ready — TSTCC encoder output formula + auto-clamp
- **Plan 4 (13-04):** Ready — TimeVAE stride auto-clamp
- **Plan 5 (13-05):** Ready — RecurrentAE, TimeNet, TST NaN defense
- **Plan 6 (13-06):** Ready — MCL NaN defense + integration smoke

## Decisions Made

- Clean cut migration — no backward compat for `generate_not_nan_mask`
- All 3 functions live in `utils.py`, exported via barrel `__init__.py`
- encoders.py import updated to `chronocratic.models.utils`

## Blockers

None. Plan 1 ready for execution.

## Required Reading

1. `.planning/phases/13-model-bug-fixes/13-01-PLAN.md` — Current plan, ready to execute
2. `.planning/phases/13-model-bug-fixes/13-CONTEXT.md` — Phase context and decisions (D-01 through D-21)
3. `.planning/phases/13-model-bug-fixes/13-RESEARCH.md` — Research findings
4. `.planning/phases/13-model-bug-fixes/13-VALIDATION.md` — Nyquist validation requirements

## Infrastructure State

- Branch: `bugs-fixes`
- Working tree: clean
- All plan files committed

## Next Action

Execute Plan 1 (13-01) — `/gsd-execute-phase` or run plan directly. TDD flow: Task 1 creates failing tests → Task 2 implements → Task 3 barrel exports → Task 4 migration.
